// 회원 신청 — 8레일 골격(준비중). 정본 org-pages ComingSoon(page="applications" ← 정본 id "apps").
import { ComingSoon } from "../_coming-soon";

export default function OrgConsoleApplicationsPage() {
  return <ComingSoon page="applications" />;
}
