// 대회·리그 — 8레일 골격(준비중). 정본 org-pages ComingSoon(page="leagues").
//   ⚠ 브리프 §2-2 #7 은 [기존 DB]로 분류되나(시리즈/마법사 이미 존재), 이번 P1 범위는
//   "대시보드 + 단체 정보"만 실동작 — 대회·리그 콘솔 연동은 P1 후속으로 보류(orchestrator 지시).
import { ComingSoon } from "../_coming-soon";

export default function OrgConsoleLeaguesPage() {
  return <ComingSoon page="leagues" />;
}
