// 멤버 — 8레일 골격(준비중). 정본 org-pages ComingSoon(page="members").
import { ComingSoon } from "../_coming-soon";

export default function OrgConsoleMembersPage() {
  return <ComingSoon page="members" />;
}
