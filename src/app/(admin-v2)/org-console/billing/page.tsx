// 회비 정산 — 8레일 골격(잠금: 정산 출금은 인증 완료 후). 정본 org-pages ComingSoon(page="billing").
import { ComingSoon } from "../_coming-soon";

export default function OrgConsoleBillingPage() {
  return <ComingSoon page="billing" />;
}
