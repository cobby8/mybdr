# BDR 디자인 시스템 전수조사 보고서

> 작성: 2026-05-07
> 작업자: PM 수빈 + Claude
> 범위: src/ 전체 578 tsx/css 파일 (사용자 영역 + admin + referee)

---

## 1. Executive Summary

### 1-1. 한눈 요약

| 지표 | 수치 | 상태 |
|------|-----|-----|
| 전체 tsx/css 파일 | 578 | — |
| BDR v2 토큰 적용 (Clean) | 249 | 43% |
| 폐기 `--color-*` 토큰 잔존 | **329** | **57% 남음** |
| `_v2` 폴더 박제 완료 영역 | 16 | A등급 핵심 |
| AppNav Phase 19 frozen 위반 | **0** | ✅ Clean |
| lucide-react import 잔존 | **0** | ✅ Clean |
| 9999px 직사각형 적용 | **0** | ✅ Clean (코멘트만 잔존) |
| `예) ` placeholder 잔존 | **0** | ✅ Clean |
| 핑크/살몬 색상 잔존 | **0** | ✅ Clean |
| 하드코딩 hex (의도 외) | 5건 | 경미 |

### 1-2. 결론

**디자인 시스템 룰 위반은 최소화 (5/13 룰 100% 준수)** — 사용자가 직접 디테일 수정한 부분이 모두 룰 안에 들어감. 핵심 위반은 **폐기 토큰 (`--color-*`) 마이그레이션 미완료** 한 가지로 수렴.

**박제 시안 적용 (`_v2` 폴더)**: 16 영역 — A등급 핵심 사용자 페이지 (`/profile`, `/teams/[id]`, `/games/[id]`, `/users/[id]`, `/live/[id]` 등) 이미 마이그레이션 완료.

**잔여 작업**: 329 파일의 `--color-*` → BDR v2 토큰 매핑 + (web) 영역 _v2 미적용 페이지의 마크업 박제.

---

## 2. 폐기 토큰 (`--color-*`) 잔존 — 상세 분포

> 글로벌 CSS L9 주석 ("기존 `--color-*` 변수는 전면 폐기. 페이지들은 Phase 1~9 에서 순차 교체") 따라 마이그레이션 대상.
> 매핑: `02-design-system-tokens.md §9` 참조.

### 2-1. 영역별 파일 수

| 영역 | 파일 수 | 우선순위 | 비고 |
|------|--------|---------|------|
| `(web)/*` | **157** | **P1** | 사용자 직접 노출 |
| `src/components/*` | **75** | **P1** | 글로벌 컴포넌트 (홈 / shared / admin) |
| `(admin)/*` | 49 | P2 | E등급 — 토큰 일치만 하면 OK |
| `(referee)/*` | 31 | P2 | E등급 |
| `live/*` 등 기타 | 17 | P3 | live는 별도 셸 |
| **합계** | **329** | | 56.9% 잔존 |

### 2-2. 핫스팟 (web 영역) — Top 디렉터리

```
36  tournaments     ← P1 (대회 영역 — [id] 하위 29 파일)
25  profile         ← P1 (프로필 — _v2 외 부분)
19  teams           ← P1 (팀 — _v2 외 부분)
17  games           ← P1 (경기 — wizard 새 경기 / step 등)
15  courts          ← P1 (코트 — 핵심 사용자 라우트)
11  organizations   ← P2 (단체)
 9  community       ← P2 (커뮤니티)
 5  partner-admin   ← P3 (E등급)
 4  users           ← P1 (선수 프로필 — _v2 외)
```

### 2-3. 핫스팟 (src/components) — Top 컴포넌트

#### 홈 영역 (사용자 첫 진입 — P0 우선순위)

```
32  src/components/home/my-summary-hero.tsx       ← P0 (홈 진입 시 즉시 노출)
21  src/components/home/profile-widget.tsx        ← P0
16  src/components/home/news-feed.tsx             ← P0
13  src/components/home/recommended-videos.tsx    ← P0
 9  src/components/home/notable-teams.tsx
 8  src/components/home/recommended-tournaments.tsx
 8  src/components/home/quick-actions.tsx
 7  src/components/home/recommended-games.tsx
 5  src/components/home/right-sidebar-logged-in.tsx
 4  src/components/home/recent-activity.tsx
 4  src/components/home/home-greeting.tsx
 3  src/components/home/home-hero.tsx
 3  src/components/home/home-community.tsx
 2  src/components/home/hero-bento.tsx
─────
145  홈 합계 — 사용자 첫 진입에 즉시 노출되는 145건의 폐기 토큰
```

#### 공유 컴포넌트 (글로벌 영향)

```
37  src/components/shared/search-autocomplete.tsx    ← P1 (헤더 검색 모달)
19  src/components/shared/push-permission.tsx
19  src/components/shared/floating-filter-panel.tsx
19  src/components/shared/edition-switcher.tsx
18  src/components/shared/profile-dropdown.tsx
14  src/components/shared/slide-menu.tsx
14  src/components/shared/profile-accordion.tsx
12  src/components/shared/kakao-map.tsx              ← 하드코딩 hex 도 잔존
10  src/components/shared/user-dropdown.tsx
10  src/components/shared/image-uploader.tsx
 9  src/components/shared/header.tsx
 8  src/components/shared/region-picker.tsx
 8  src/components/shared/pwa-install-banner.tsx
```

> ⚠️ **shared 컴포넌트는 글로벌 영향** — 한 파일 마이그레이션 = 여러 페이지 동시 개선.

---

## 3. 하드코딩 hex (의도 외 잔존) — 5 건

### 3-1. globals.css 내부 (의도 — 토큰 정의)

```
src/app/globals.css L1048: background: #0B0D10;     ← 다크 mode 첫 페인트 (의도)
src/app/globals.css L1356: color: #E31B23 !important;   ← 강제 가드 (의도)
src/app/globals.css L1373: background: #E31B23 !important;
src/app/globals.css L1379: background: #E31B23;
src/app/globals.css L1384: color: #E31B23 !important;
```

→ 의도된 brand color 강제 가드. 룰 위반 X.

### 3-2. 위반 — 마이그레이션 필요

```
src/components/shared/kakao-map.tsx L208: color:#22C55E (활발 — 그린)
                          L209: color:#F59E0B (적당 — 황색)
                          L316: background:#E31B23
```

→ 카카오맵 인포윈도 HTML 문자열 안에서 직접 hex 사용 (CSS 미접근). **권장 fix**: CSS 클래스 정의 + className 사용 또는 토큰 fallback hex (`#1CA05E` 그린 / `#E8A33B` 황색) 로 교체.

### 3-3. nav-badge.css fallback (마이그레이션 진행 중)

```
src/components/bdr-v2/nav-badge.css L24, L35, L46:
  background: var(--color-accent, #E31B23)
                    ↑
        폐기 토큰 fallback 잔존
```

→ **권장 fix**: `var(--accent)` 로 직접 교체. fallback hex 제거.

```css
/* 변경 전 */
background: var(--color-accent, #E31B23);
/* 변경 후 */
background: var(--accent);
```

### 3-4. nav-badge.css L56 — 하드코딩 그린

```
src/components/bdr-v2/nav-badge.css L56:
  background: #16a34a  ← 토큰 미사용 (live pulse 그린)
```

→ **권장 fix**: `var(--ok)` (= `#1CA05E`) 사용.

---

## 4. AppNav / 더보기 / 컴포넌트 표준 — Phase 19 적용 상태

### 4-1. AppNav 7+6 룰 (Phase 19) — 모두 ✅

- 9 메인 탭 (홈/경기/대회/단체/팀/코트/랭킹/커뮤니티/더보기) ✅
- utility bar 좌측 = BDR 로고 이미지 (height 18px) ✅ (Phase 19 갱신)
- main bar 우측 = 다크 / 검색 / **쪽지** / 알림 / 햄버거 = 5개 ✅
- ThemeSwitch PC 듀얼 / 모바일 단일 ✅
- 검색·쪽지·알림 = `app-nav__icon-btn` 클래스 (border/bg X) ✅
- 햄버거 R3 강조 = `<NavBadge variant="dot"/>` 신규 컨텐츠 시 ✅
- 더보기 = drawer 토글 (PC/모바일 동일, dropdown 패널 X) ✅

### 4-2. 더보기 5그룹 IA — 슬림화 완료 (15 항목)

| 그룹 | 항목 수 | 항목 |
|------|--------|------|
| 내 활동 | 3 | 내 일정, 보관함, 스탯 분석 |
| 경기·대회 | 2 | 라이브 중계, 연습경기 매칭 |
| (등록·예약) | 0 | **그룹 자체 제거** (메인 페이지 하위 통합) |
| 둘러보기 | 6 | 심판 센터 안내, 코치·트레이너, 리뷰, 수상 아카이브, 갤러리, 샵 |
| 계정·도움 | 4 | 안전·차단, 소개, 요금제, 도움말 |
| **합계** | **15** | (이전 30 → -50%) |

→ 가짜링크 4건 (gameResult, gameReport, guestApps, referee) 영구 제거 유지.

### 4-3. 신규 컴포넌트 표준 (박제됨)

| 컴포넌트 | 경로 | 상태 |
|---------|------|------|
| NavBadge (dot/count/new/live) | `src/components/bdr-v2/nav-badge.tsx` | ✅ 박제 (02-tokens §10-1) |
| PasswordInput (보기 토글 통합) | `src/components/bdr-v2/password-input.tsx` | ✅ 박제 |
| ForceActionModal (jersey/withdraw) | `.../force-action-modal.tsx` | ✅ 박제 |
| Hero 헤더 grid 1fr auto 패턴 | 인라인 표준 | ✅ 박제 |
| fade chevron 칩바 (FilterChipBar) | `src/components/bdr-v2/filter-chip-bar.tsx` | ✅ 박제 |
| 본인 카드 dropdown (overflow visible) | `_v2` 폴더 카드 | ✅ 박제 |
| 신청 중 뱃지 (4종) | 본인 회원 카드 좌하단 | ✅ 박제 |

---

## 5. _v2 폴더 미적용 (web) 페이지 — 시안 박제 후속 의뢰 후보

> 다음 페이지는 사용자 영역이지만 `_v2` 폴더 미생성. legacy 마크업 + `--color-*` 잔존 가능성 큼.
> 향후 디자인 의뢰 시 우선순위 후보.

### 5-1. P1 — 사용자 핵심 사용 (자주 진입)

```
/games (목록)         ← 메인 9개 탭 중 1
/teams (목록)         ← 메인 9개 탭 중 1
/tournaments (목록)   ← 메인 9개 탭 중 1
/community (목록)     ← 메인 9개 탭 중 1
/courts (목록)        ← 메인 9개 탭 중 1
/messages             ← 헤더 쪽지 진입점 (Phase 19 신규 진입 강조)
/notifications        ← 헤더 알림 진입점
/profile/edit         ← 프로필 편집 (자주 사용)
/profile/settings     ← 설정 (자주 사용)
/search               ← 헤더 검색 진입
```

### 5-2. P2 — 보조

```
/about        /awards       /coaches      /forgot-password
/gallery      /help         /onboarding/setup
/organizations  /privacy    /reset-password
/scrim        /series/*     /shop         /stats
/terms        /verify       /profile/billing
/profile/activity
```

---

## 6. 클로드 디자인 의뢰 우선순위 (추천)

### 6-1. 의뢰 #1 — 홈 페이지 마이그레이션 (P0, 사용자 첫 진입 145건)

**의뢰 대상**: `src/components/home/*` 14 파일
**작업**: `--color-*` → BDR v2 토큰 (`02-design-system-tokens.md §9` 매핑) 일괄 교체 + 다크 brutalism 적용 검증
**예상 영향**: 사용자 진입 시 첫 화면의 100% 디자인 시스템 정합

### 6-2. 의뢰 #2 — 메인 9개 탭 목록 페이지 _v2 박제 (P1)

**의뢰 대상**: `/games`, `/teams`, `/tournaments`, `/courts`, `/community` 목록 페이지 (5개)
**작업**: `_v2` 폴더 신설 + 시안 1:1 박제 + 메인 카드/필터/페이지네이션 표준화
**참고**: 기존 상세 페이지 (`/games/[id]/_v2` 등) 와 일관성 유지

### 6-3. 의뢰 #3 — 헤더 진입점 페이지 (Phase 19 후속, P1)

**의뢰 대상**: `/search`, `/messages`, `/notifications`
**작업**: AppNav Phase 19 의 검색/쪽지/알림 진입점에 맞춰 페이지 자체 시안 박제
**연계**: messages unread API (현재 미연동) 활성화 + 알림 그룹화 패턴

### 6-4. 의뢰 #4 — 프로필 편집/설정 (P1)

**의뢰 대상**: `/profile/edit`, `/profile/settings` (settings 는 일부 _v2 적용)
**작업**: 일부 _v2 미적용 영역 박제 + 폼 스타일 표준화

### 6-5. 의뢰 #5 — shared 컴포넌트 마이그레이션 (P1, 글로벌 영향)

**의뢰 대상**: `src/components/shared/*` 폐기 토큰 잔존 컴포넌트 (특히 search-autocomplete, profile-dropdown, slide-menu, header)
**작업**: 토큰 매핑 일괄 교체 + 다크 mode 검증
**효과**: 한 파일 = 여러 페이지 동시 개선

### 6-6. 의뢰 #6 — admin/referee 토큰 일괄 매핑 (P2)

**의뢰 대상**: `(admin)/*` 49 + `(referee)/*` 31 = 80 파일
**작업**: 시안 박제 X / 토큰만 BDR v2 매핑 (E등급 자체 셸 유지)
**작업 방식**: 일괄 sed/grep 가능 — 시안 작업보다 "리팩터링" 성격

---

## 7. 디자인 시스템 문서 갱신 결과 (이번 세션)

✅ **02-design-system-tokens.md** — 실측 동기화 (2026-05-07)
- 듀얼 테마 구조 (라이트/다크) 명시 §0
- 토큰 라이트/다크 별 값 듀얼 컬럼 §1
- 신규 토큰 박제: `--bg-head`, `--cafe-blue-soft/hair`, `--link/--link-visited`, `--fs-*` 7종, `--lh-body`, `--sh-xs/sm/md/lg`, `--r-0/1/2/3/4`, `--radius-card` 듀얼
- 색상 표준: `--ok = #1CA05E`, `--warn = #E8A33B`, `--danger = #E24C4B`, `--info = #0079B9` 4종 확정
- 폐기 토큰 매핑 표 §9 (12 매핑)
- 신규 컴포넌트 표준 §10 (NavBadge / PasswordInput / ForceActionModal / PageBackButton 정책 / Hero grid / FilterChipBar / 본인 카드 dropdown)

✅ **03-appnav-frozen-component.md** — Phase 19 frozen 갱신
- 보존 룰 7 → **13 룰** (Phase 19 추가 6 룰: utility 로고 이미지 / 메인 텍스트 로고 / 햄버거 NavBadge / 메인 탭 NEW 뱃지 / 알림 빨간 점 / 쪽지 아이콘)
- 운영 props (TypeScript) 명시
- utility bar 좌측 = BDR 로고 이미지 (height 18px) — 텍스트 "MyBDR 커뮤니티" 폐기
- 메인 바 로고 = `MyBDR.` 텍스트만
- 햄버거 R3 강조 NavBadge dot 명시
- NavBadge 4 변형 (dot/count/new/live) 표준 §7
- LogoutLink helper §8

✅ **04-page-inventory.md** — 마이그레이션 상태 박제
- 더보기 5그룹 슬림화 박제 §5-A (30 → 15 항목)
- _v2 폴더 박제 완료 16 영역 §5-B-1
- 폐기 토큰 잔존 분포 §5-B-2 (영역 / 우선순위)
- _v2 미적용 (web) 페이지 후속 의뢰 후보 §5-B-3

---

## 8. 다음 액션 (PM 결정 필요)

1. **6-1 ~ 6-6 의뢰 우선순위 확정** — 본 보고서의 6개 의뢰 중 어느 것을 먼저 진행할지
2. **마이그레이션 일괄 vs 점진** — 폐기 토큰 329 파일을 한 번에 sed replace 할지 vs 영역별 시안 박제와 함께 점진 진행할지
3. **하드코딩 hex 5건 정리** — kakao-map / nav-badge.css fix 단독 commit 으로 진행 여부

---

## 9. 출처 / 검증

- `src/app/globals.css` (3022 lines, source of truth)
- `src/components/bdr-v2/*` (frozen 컴포넌트들)
- `src/components/bdr-v2/more-groups.ts` (슬림화 5그룹 IA)
- `Dev/design/claude-project-knowledge/00~07.md` (이번 세션에서 갱신)
- 검증 grep 명령:
  ```bash
  # 폐기 토큰 카운트
  grep -rln "var(--color-" src/ | wc -l   # = 329
  # lucide-react
  grep -rn "from 'lucide-react'" src/      # = 0
  # 9999px 직사각형
  grep -rn "9999px" src/ | grep -v "//\|/\*"  # = 0 (코드)
  ```
