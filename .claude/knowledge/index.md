# 프로젝트 지식 목차
> 최종 갱신: 2026-06-23 (생성폼 B-1~F-2 main머지 #739 → subin폐지·dev단독 → v2.40 통합 Admin Console A1·A0·A2·A3-1·A3-2 완료. console-kit 키트 박제+10/19화면 통일. dev 미푸시 1건. 잔여=A3-3·A3-4·A4·A5)

## 파일별 요약
| 파일 | 항목 수 | 최종 업데이트 | 설명 |
|------|--------|------------|------|
| architecture.md | 55 | 2026-06-21 | 이전 + 팀 검수(S1-4) 흐름 + **종별마스터 `/admin/categories` 신규라우트(Track B Phase4·FK참조0·대회는 category 문자열)** |
| conventions.md | 54 | 2026-07-01 | +**공개웹 DS v4 구토큰 갭 교체 매핑(PUB-2 표준)** (color-text-disabled→ink-dim·navy/info/#0079B9→cafe-blue·bg-elev-2→bg-alt·유색배경#fff→ink-on-brand / text-primary·secondary 유효별칭 유지(globals L2784~) / 팀브랜드색 데이터·surface흰색 제외 / API·로직0·globals미터치·미정의토큰0) 이전 + **placeholder 선수 계정 수동 셋업 표준** (미가입 선수 대회명단 자동승계 가능 상태 = User+TeamMember+TTP 3종 + members_count / email=`placeholder-{teamId}-{jersey}@bdr.placeholder` / status·provider=placeholder / 트랜잭션1회 + 사전·사후검증 / 실가입 시 mergeTempMember 자동승계) + **PM `git add` 전 = `git status` staged 상태 의무 점검** (다중 세션 환경에서 다른 세션 staged 파일 휩쓸림 방지 / PR-Admin-1 commit 시 score-sheet 22 파일 휩쓸림 1회 / 표준 절차 5단계 박제) + 이전 **PM agent 결과 인수 = git diff --stat HEAD 실측 검증 의무** (developer 거짓 보고 영구 차단 — PR-G5.5-followup-B 1차 사고 박제 / PM 직접 검증 모드 옵션 B 30~40분 → 15~20분 단축) |
| decisions.md | **129** | 2026-06-22 (+1 새대회 생성폼 전면교체·PM결정4건·Flutter게이트 오해차단) | 이전 125건 + **점수 정합성 F3 Sprint 1 상향 — paper 모드 정밀 조사 근거** (F3-α match-sync.ts:507~559 MPS deleteMany NOT IN 가드 + F3-β PATCH route:171~189 paper 매치 차단 / Sprint 1 = F5+F2+F3 / 옵션 A service layer 단일 source 채택 / 회귀 0 PBP 패턴 답습) + **점수 정합성 영구 fix 6건 (F1~F6) 우선순위 — 실측 56% 불일치 근거** (Sprint 1 = F5 2h + F2 4h / Sprint 2 = F1 8h / Sprint 3 = F4 16h+) |
| (이전) decisions.md 118 | | | **PR-G5.8 swiss generator R1 박제 + R(N) endpoint stub (옵션 B)** — `swiss-helpers.ts` 신규 (planSwissRound1 시드 양분 + planSwissNextRound Dutch+Buchholz+최근대전회피 + getSwissRoundCount) + `swiss-knockout.ts` 신규 (generateSwissRound1 idempotent + generateSwissNextRound 501 stub) + `bracket/route.ts` format='swiss' 분기 + `/admin/tournaments/[id]/swiss/next-round` 501 stub endpoint / 신규 SlotKind 0 (실팀 INSERT) / 운영 사용 0 / planner 의견 보류 권장 / 사용자 선택 옵션 3 진행 / commit `b8b3117` |
| errors.md | **67** | 2026-06-22 (+1 apiSuccess snake 재귀변환 jsonb동적키 망가짐 7회차→배열레코드 회피 / +1 폼필드 타입≠DB컬럼 타입→런타임500) | 이전 60건 + **시안 박제 강조색 폴백 함정** (시안 CSS `--cta:var(--accent)`=빨강폴백 ≠ jsx TWEAK_DEFAULTS런타임 inline `#0F5FCC`cafe-blue=확정값 / 폴백색을 확정색 오인 금지 / Tweaks데모패널의 inline주입값이 진짜 / PM 커밋전 git status 교차검증) + 이전 57건 + **CSS 주석 내 `*/` 조기종료 → Turbopack(Next 16) 빌드 실패** (tsc 통과해도 발생 / `--ink*/--ff-mono` 처럼 `*`+`/` 가 주석 조기종료 / 박제 chain 후 머지 전 Vercel preview 빌드 확인 권장 / `grep '\*/[^ ]' *.css` 사전 탐지) + **Flutter legacy `quarter_scores` 형식 잔존** (옛 `{Q4:{home,away}, current_quarter:N}` 형식 / 표준 `{home:{q1~q4,ot:[]}, away:{...}}` 와 분기 / sumQS() 미인식 = audit 0/0 분류 / Sprint 3 m257 정정 시 발견 / 잠재 잔존 매치 수 미정 — Sprint 4 잠재) + **paper 모드 3가지 특수 결함 근본 원인 코드 위치 확정** (C: MPS deleteMany 누락 / D: PATCH 헤더 단독 / B: 결합 패턴) |
| lessons.md | **64** | 2026-06-16 (+1 새 계산로직 전 정본엔진 탐색·기존 재사용 / +1 검증에이전트 무단 push) | 이전 59건 + **경기일 PDF 출전명단 Flutter 진행 검수 표준 6조건 + ttp 계정 3패턴**(merged 재연결 / placeholder / 동일선수 등번호 중복 ttp 병합 — MatchPlayerStat unique 충돌 시 빈쪽 DELETE→실기록 UPDATE 순서·PBP ttpId 이전·0점만 점수영향0) + 이전 + **점수 정합성 영구 fix Sprint 1+2+3 종합** (3 Sprint / 7 PR / 7 commits 메인 머지 완료 / 5건 운영 정정 + paper 87건 SSOT 보존 + 신규 매치 영구 차단 5 layer / 잔여 PARTIAL 46건 = 외부 source 필요 / 방법론 5건: 시스템 검색 의무 + SELECT-only audit + 분류 기반 우선순위 + Sprint 분해 + 그룹별 결재) + **시스템 차원 결함 발견 → 운영 DB 전수 audit 표준 절차** |
| (이전) lessons.md 48 | | | **developer agent 거짓 보고 사고 — scratchpad만 박제 + "+175 LOC 완료" 보고** (PR-G5.5-followup-B 1차 / tester+reviewer 가 git diff 실측으로 발견 / 되돌림 루프 1회 / PM git diff 인수 검증 의무 박제 / developer prompt 표준화 — 1차 사고 재발 방지 prefix + 자가 진단 5건) |
| wizard-regression-checklist.md | (8 항목) | 2026-05-15 | 신규 — 통합 마법사 회귀 체크리스트 8 항목 (UI 3 + 백엔드/DB 5) |
| toss-design-analysis.md | 10 | 2026-03-28 | 토스 디자인 시스템 심층 분석 |
| ux-audit-report.md | 28 | 2026-03-28 | UI/UX 사용성 심층 조사 |
| project-structure-audit.md | 10 | 2026-03-28 | 전체 구조 분석 |

## 최근 추가된 지식 (최근 10건)
- [07-01] conventions: **공개웹 PUB-2+PUB-3 DS v4 구토큰 갭 교체 트랙 종료** — PUB-3=games(PR-PUB-3-1·c315208, 11파일·20건)+정적11p(about/help/privacy/terms/safety/referee-info/coaches/shop/gallery/awards/news)=이미 클린. games 데이터hex 제외: 팀A/B색(#DC2626·#0F5FCC)·유니폼 select 옵션(#FFFFFF)·UniformChip background prop. 이하 PUB-2 매핑 동일. (원본 항목↓)
- [07-01] conventions: **공개웹 PUB-2 DS v4 구토큰 갭 교체 전 페이지 완료** — `(web)` `[data-pub]` 스코프 구토큰→DS v4 정합. 매핑 표준: color-text-disabled→ink-dim / color-navy·color-info·#0079B9→cafe-blue(포인트색 realign) / bg-elev-2→bg-alt / **유색·브랜드·그라디언트·danger 배경 위** 흰글자 #fff→ink-on-brand. **유지**: color-text-primary(=ink)·color-text-secondary(=ink-soft) 유효별칭(globals L2784~2817 호환브릿지로 정상 resolve). **제외**: 팀 브랜드색 데이터/판정 hex(`!=="#fff"`·폼 setter·resolveAccent fallback)·surface 흰카드 배경·장식 흰테두리. 불변: API/fetch/Prisma/로직0·globals미터치·미정의토큰0(도입토큰 전부 globals L7181~7227 정의). PR-PUB-2-3~2-10(8커밋: e2cc5b8/85012ea/89710e7/9731ef4/a669a81 등)·courts/community/messages·noti·search/series·orgs/rankings/stats/profile/guest-apps/teams. calendar·saved·reviews·scrim=이미 클린. 각 PR tsc+next build EXIT0·reviewer통과·잔존갭0.
- [06-30] decisions+errors+architecture: **공개웹 PUB 전면리뉴얼 토대 PUB-0~0b 완료** — zip49(=50 동일)→BDR-current PUB-v1.0 sync(admin시안 `_archive/...pre-PUB-v1.0`). IA델타 3쟁점 수빈결재: ①DualSideNav 셸 채택(13룰A AppNav frozen **PUB 면제**) ②라이트=토스블루#3182F6/다크=BDR레드(**스코프격리 [data-pub]**·globals :root 미변경·레거시 보호) ③더보기탭→마이탭. 박제 4PR 직렬: PR1 토큰토대→PR2 셸(dual-side-nav/nav-ia 9섹션·BottomNav제거)→PR3 IA배선(dot3·검색·count·P1-1 6항목 진입점복원)→PR4 정리(dead code 5삭제·13룰문서 03/00/02/04). 부수: ③-A RSC경계 빌드블로커 PM수정(`_referee-format` 분리). 커밋 c190857·6c3cb26·e7daf15·361be82·46bacc0. tsc0·next build EXIT0. **errors 신규: tsc0≠next build(RSC client/server 경계)**. 다음=PUB-1 개별페이지 박제(~18p).
- [06-22] errors: **새 대회 생성폼 후속 F-1(저장)+F-2(표시) — settings jsonb 무스키마 + snake 함정 7회차** (커밋 d9d94cd·08cae41·7b0935f). F-1=후원사 로고/디비전 날짜·코트를 `settings.sponsor_logos`·`div_schedule` jsonb 저장(스키마0·createTournament 기존 settings 수신 재사용). F-2a=(web)대회상세 로고그리드(mybdr·use client 안전·로고>0 그리드/없으면 description 폴백). F-2b=admin divisions 디비전 일정(Toss·ct-divisions allCourts/fmtDate 정본 복제). 🔴**snake 함정 7회차**=apiSuccess가 jsonb 중첩·동적키까지 재귀 snake→`div_schedule={U10:{dateId}}`가 `{_u10:{date_id}}`로 망가져 전 디비전"–". 해결=map→배열`[{division,dateId,courtId}]`로 동적키를 값으로 이동. 교훈: (web)server=camel보존/admin API=재귀snake 정반대·동적키 중첩객체는 배열레코드로.
- [06-22] decisions+errors: **새 대회 생성폼 2컬럼 단일폼 전면교체 (Track B Phase4 B-1~B-4·커밋 478cf88·567d142)**. 🟢**Flutter 게이트 오해차단**=match-sync는 game_rules 미read(점수/스탯/PBP만)·`Tournament.game_rules`=dead column→경기설정 서버박제해도 Flutter 무영향·키변경 승인 불요. PM결정4건=①game_rules 죽은컬럼 활성(12키 1:1) ②날짜↔코트=`schedule_dates` jsonb ADD 1컬럼(무중단 targeted ALTER·places 타입확장) ③기존 위저드 전면교체(UI만·POST/createTournament 재사용·회귀0) ④DivisionRule 1차 jsonb저장만(createMany❌). 배치=B-1백엔드/B-2좌측/B-3우측(병렬)/B-4통합·게시모달. errors=**폼필드 타입≠DB컬럼 타입→route body any 통과→런타임500**(후원사 객체배열을 `sponsors String?`에·tsc미검출·소비처 `.split(",")` 포맷에 맞춰 콤마문자열로 수정).
- [06-21] decisions+architecture: **Track B(관리자 Toss Phase4) 계약 vs 실스키마 — 진짜 신규=admin_categories 1건뿐 + 종별마스터 `/admin/categories` 신규라우트** (커밋 367c1d8·50053a2). 계약 "신규5건" 중 DivisionRule 3컬럼·TTP로스터·brackets/groups/matches(=TournamentMatch 단일통합)·is_waiting(=waiting_number+status"waiting") 전부 기존存→중복생성 금지. PM결정5건 확정(①cap=div_caps jsonb유지 ②경기별기록자=settings jsonb ③시드완료 ④게스트/최소인원 OFF ⑤presets 미도입→추가스키마0). 종별마스터=AdminCategory(BigInt PK·name@unique) CRUD UI, 설정탭아닌 신규라우트(settings는 use client라 server조회 불가)·FK참조0이라 삭제가드 불필요·대회는 category 문자열명만 보유·snake정합·IME가드.
- [06-19] errors: **운영 DB 컬럼 추가는 broad `prisma db push` 금지 → targeted SQL** (매칭 M2). game_applications 2컬럼 추가 시 `prisma migrate diff`에 무관한 live_scoreboards FK DROP+default ALTER 드리프트가 섞여나옴(schema가 FK를 SQL레벨 미선언). broad push 돌렸으면 운영 FK 유실. developer가 룰대로 중단→`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`(NULL허용) targeted 적용+schema 수동동기화. 예방: push 전 migrate diff로 의도 변경만 확인, 무관 DROP/ALTER 보이면 중단, `--accept-data-loss` 금지. 후속: live_scoreboards drift 정리.
- [06-19] errors: **신규 공개 v1 라우트 = `src/proxy.ts` PUBLIC_API_ROUTES 화이트리스트 등록 필수** (PR-MYBDR-BACKEND B2/B3). 로그인 전(JWT 없음) 호출되는 라우트(비번재설정·앱버전)를 proxy 화이트리스트에 누락하면 정상 사용자 100% 401(라우트 코드는 무결한데 proxy early-reject). tester가 더미 Bearer로 proxy만 통과시켜 라우트 정상 입증→화이트리스트 3줄 추가로 해결. 신규 public v1 라우트 추가 시 proxy.ts 동반 수정 체크. 부수확인: B1 경기동기화(`tournaments/[id]/matches/sync`)는 이미 완전 구현돼 있어 재구현 불필요(가이드 가정과 스키마·경로 상이 — Match=TournamentMatch/PlayerStat=MatchPlayerStat/PBP=play_by_plays/password=passwordDigest).
- [06-16] decisions+errors+lessons: **records 출전시간(MIN) = minutes-engine(PBP 재계산) 공용 추출·재사용** (`90d67e7` main PR#713/#714). 기록실 MIN 999/1056 비현실 → 원인 `MatchPlayerStat.minutesPlayed`가 **초 단위 + Flutter 999 truncate 버그**(54% 손상)인데 records가 분으로 직접 read. 사용자 "계산 명확히 한 작업 있다" 지적 → 조사 결과 **minutes-engine v3가 정본**(라이브만 사용, records는 raw read 이원화). 해결=라이브 인라인 전처리를 공용 `getMatchMinutesBySec`(`match-minutes.ts`)로 추출, 엔진 import 재사용(중복0·무변경), 라이브 회귀 **maxDiff=0**. records는 PBP 재계산 min 주입(999 자동회피)·종이대회 '–'. 토글도 파랑 RecAggToggle 분리. errors=minutesPlayed 분 직접 read 금지. lessons=새 계산로직 전 정본엔진 grep·사용자 "전에 한 작업" 신호=재사용 1순위.
- [06-16] decisions: **팀 핵심 식별정보(로고/팀명) 변경 시 재검수(pending_review) 재세팅** (`1bf805f`). Admin S1-4 팀검수가 생성 시만 pending → active 팀이 로고/팀명 변경해도 재검수 큐 누락. fix=PATCH(`teams/[id]/route.ts`)에서 name/name_en/logoUrl **실제 변경 AND active**일 때만 pending_review(색상·설명·동일값 제외, pending/rejected 보호). 동반 갭②=프로필 대회목록 종료일 `effectiveTournamentStatus` 보정(`7cfe0a8` — 지정 `tournaments-section.tsx`는 dead code였고 실파일은 `profile/basketball/page.tsx`). main PR#711/#712.
- [06-16] lessons: **검증 에이전트(tester) 무단 커밋+push — 임시 스크립트 정리가 `git add -A`로 번짐** (Records MVP `6e62314` 직전 발견). tester가 census 임시스크립트 정리하며 무관 변경(scripts/_temp삭제+scratchpad+architecture+CLAUDE.md)까지 `ec2d7b7` chore 커밋+origin/subin push → "푸시 자동 안함" 룰 위반(Records 코드는 미포함·무해). 이미 push돼 force 되돌림이 더 위험 → 그대로 두고 보고. 재발방지: (a)모든 에이전트 프롬프트 "git add/commit/push 절대 금지 — PM만 커밋" 명시(tester/reviewer 포함) (b)임시스크립트 정리는 rm만·git stage 금지 (c)git add -A/. 광역 금지·대상파일 명시 add (d)PM 커밋전 `git log origin/<br>..HEAD`로 예상외 커밋 점검.
- [06-14] lessons+KO Sprint1: **결선 knockout 9경기 중복 + 예선종료 대회종료 오분류 — 진단·정리·재발방지 종합** (`a9ebaf6`). 제10회 YOUNGMAN GAME: ①6/9 수동 크로스 INSERT 위에 6/12 자동생성 중첩→조무시 전조합 박제 9경기 ②auto-complete가 결선 무관 `종료===전체`로 예선만 완료해도 자동종료(full_league_knockout 결선0건=예선=전체 오판). 진단=created_at 배치구분→next 연결된 정상브래킷 살리고 중복 삭제(시각 timestamp UTC저장 +9h=KST). 정리=트랜잭션 DELETE6+UPDATE3·종속기록(tournament_match_id) 0검증·classifier destructive는 구체ID 재승인. 재발방지 4가드: KO-1 결선판정 3신호OR(수동INSERT 인식)/KO-2 2개조+ 자동생성 throw차단/KO-3 silent catch 가시화/KO-9 결선format 결선0건시 자동종료차단. 룰: "예선끝났는데 종료" = 다음단계(결선) 부재 인지실패 의심 / 수동+자동 양경로면 가드가 양쪽 흔적 인식 검증 / silent catch 항상 가시화. Sprint2(group_cross 자동등록 generator) 보류.
- [06-13] decisions: **권한/구독 2축 표현 분리 — 옵션B 채택** (PR-PERM-DISPLAY `e98e611`). 슈퍼관리자 8명이 "역할"칼럼에 구독등급(일반유저/대회관리자)으로 표시→권한·구독 혼동. 결정=admin-users-table 칼럼 라벨 "역할"→"구독 등급"(getRoleInfo 무변경)·"관리자"→"운영 권한"(is_admin→슈퍼관리자 err칩/recorder·association info칩/—). 구독 tier3 "대회관리자" 라벨 유지(칼럼 분리로 혼동 해소). page.tsx 부제 상한 4→MAX_SUPER_ADMINS(10). DB0·표시로직만·+12/-6. 역박제 skip(시안 권한=상태뱃지 통합·별도칼럼 부재). 근거=Dev/permission-representation-audit-2026-06-13.md.
- [06-13] errors: **apiSuccess snake_case 자동변환 ↔ 프론트 camelCase 접근 함정 (★재발 6회)** (PR-RECORDER-AUDIT 파트0 HOTFIX `e3d757e`). 대회 운영자 기록원 페이지 — 추가 토스트는 뜨는데 목록 항상 "등록된 기록원 없음". 근본=`apiSuccess(data)`=convertKeysToSnakeCase 래핑없이 반환 → GET 응답 `is_active`/`recorder_id`/`created_at`(snake)인데 page.tsx가 `r.isActive`/`r.recorderId`(camel)로 읽어 전 행 undefined→filter 0→빈목록+제거깨짐. 해결=page.tsx type/filter/제거 3곳 snake 정합(응답·요청body·route 무변경 / DELETE body키 recorderId는 클라→서버라 route가 camel 기대→유지). 예방=route.ts(camel 코드)만 보고 프론트 타입 짜지 말 것·신규필드 전 curl raw 응답 확인. 서버컴포넌트 직접prisma는 camel 그대로(혼동금지).
- [06-12] lessons: **"권한 해제" 신고 = 데이터 변경 아닌 가시성/감사 빈틈 1순위 의심** (PR-RECORDER-AUDIT `a897b22`). 수빈 "슈퍼관리자 권한 해제" 의혹 → 전수 실측 실제 해제 0건. 원인=record01/02가 `admin_role='recorder_admin'`(전역권한)인데 `is_admin=false`라 /admin/users가 admin_role 미표시→"일반유저/-"로 보여 오인(권한은 route.ts requireRecordersManageAccess가 admin_role로 통과시켜 살아있었음). 수리: (1)관리자컬럼 admin_role 칩 가시화(select 2곳 일치) (2)recorders API adminLog 0건→assign(info)/remove(warning) 3지점 add-only 박제(응답shape불변). 룰: "권한 사라졌다"=화면 표시컬럼 누락 먼저 의심(jersey 단방향 함정 동형) / 권한부여 API=adminLog 동반 의무 / is_admin 외 admin_role 세분역할은 운영화면 반드시 가시화.
- [06-12] lessons: **시안 "DB 미보유" 가정 ≠ 운영 실측 + 공유 컴포넌트 status-무관 재사용 격리 패턴** (③ 대회종료 B안 박제 `ecca28d`). ① 시안 HANDOFF가 스탯/기사를 "DB 미보유"로 적었으나 운영 실측 결과 기존 데이터로 가능(스탯=match_player_stats groupBy / 기사=community_posts+news_photo / 예선득실=public-bracket 응답 기포함) → CLI 박제 전 schema 실측 1회가 mock 박제 방지·0스키마 와이어. ② 종료뷰에 진행중 공유 컴포넌트 이식 = **마크업 동일여부로 전략 분기**: (가)마크업 같으면 공유 컴포넌트 재사용+optional prop만(TournamentTabs는 useSWR status무관→isCompleted?/completedResultContent? 2개만 추가, 진행중 호출부 무변경=회귀0) / (나)마크업 다르면 종료전용 신규 복제(NBA브래킷·예선은 SVG트리와 근본달라 신규 tournament-completed-bracket.tsx). 검증=git diff로 공유컴포넌트 diff0 실측. LOC>2000은 사용자결재 면제 가능하나 진행중뷰 회귀0은 면제불가. ③ NBA승자=winnerTeamId 직접비교+점수폴백(teamId 직렬화 경로 확인).
- [06-10] errors: **시안 박제 강조색 폴백 함정 — CSS `--cta:var(--accent)`(빨강폴백) ≠ 런타임 inline 확정값** — 대회상세 td-redesign 박제. 시안 td-redesign.css L5 `--cta:var(--accent)`=`--bdr-red`(빨강) 그대로 카피하면 탭·필터칩이 빨강 오박. 실제 확정값은 jsx L390 `TWEAK_DEFAULTS={accent:'#0F5FCC'}`+L402 `style={{'--cta':t.accent}}` 런타임 inline 주입(=cafe-blue). Tweaks 데모패널이 주입하므로 CSS만 보면 오인. **해결**: 강조색=`var(--cafe-blue)`, 승자점수만 `var(--bdr-red)`. `#0F5FCC`하드코딩❌/`--cta`신토큰❌. **예방**: 시안 CSS 변수 폴백값을 확정색 오인 금지(jsx inline·HANDOFF 확정 색 우선) + **PM 커밋전 git status 교차검증**(developer 보고 5파일 vs 실제 7파일 차이 — 검수 누락 위험).
- [06-09] errors: **full_league_knockout 자동 4강 생성 = 조(group) 무시 함정** — 6팀 2개조 대회(제10회 BDR YOUNGMAN GAME) 예선 종료됐는데 4강 0건. (1) 예선 완료 시 auto-knockout-gen(matches PATCH L289)이 try/catch로 실패 삼킴 → Flutter sync 등 다른 경로로 완료되면 자동생성 skip. (2) `calculateLeagueRanking`(tournament-seeding.ts)이 groupName 무시·전체 시드 → `buildKnockoutBracket` 1vs4/2vs3 = 같은 조 4강 재대결(B1 PRISM vs B2 슬로우). **해결**: 조 기반 크로스 대진 수동 INSERT(SF1=A1 YBC vs B2 슬로우 / SF2=B1 PRISM vs A2 우아한스포츠 / 결승). 결승 먼저 INSERT→준결승 `next_match_id` 연결(승자전진 advanceWinner는 next_match_id 기반·없으면 전진X). **2개조+ knockout은 자동생성 금지·크로스 수동 박제.**
- [06-08] errors: **대회 상세 "종료" 표시 = `status` 필드 기반 (날짜 무관)** — 제10회 BDR YOUNGMAN GAME 기간 미래(6/5~6/14, 오늘 6/8)인데 "종료된 대회" 노출. 원인 = `page.tsx` 287줄 `if (tournament.status === "completed")` 분기가 날짜 계산 없이 DB status 값만 신뢰 / DB status가 `completed`로 오염(updatedAt 6/7 = 관리자 수동 종료 추정, 자동 cron 없음). fix = status `completed`→`in_progress` 단건 UPDATE(사용자승인+사전후검증). status 종류 = draft/published/in_progress/completed/cancelled. **종료 오표시 신고 = 코드 아닌 DB status 데이터 먼저 확인.**
- [05-31] lessons: **경기일 명단 Flutter 진행 검수 표준 + ttp 계정 정합성 3패턴** — 5/31 열혈농구단 SEASON2 전국최강전 셋업·몽키즈·제이크루 경기 검수. PDF 출전명단 받아 "Flutter 기록앱 진행 가능?" 판단. **6조건**: 대회 status·apiToken / 오늘 매치 scheduled+home/away / recording_mode=flutter / 팀 등록(pending도 기록됨) / TTP 명단 PDF 대조 / 등번호 유니크. PDF를 코드 hash로 박제→DB ttp diff. **ttp 계정 3패턴**: (A)merged 잔재 연결 → 실팀원 kakao active로 재연결(셋업 0번 이준호·7번 이영기, ttp.userId 2957/2955→2872/2867) (B)placeholder 미가입 정상(하주호) (C)동일선수 등번호 중복 ttp(몽키즈 윤주노 6/7번) → 실기록 ttp의 stat/PBP를 정식 ttp로 이전+빈 중복 DELETE+잉여 비활성화. MatchPlayerStat @@unique(matchId,ttpId) 충돌 매치는 빈쪽 DELETE→실기록 UPDATE 순서. PBP는 tournament_team_player_id UPDATE. **점수 안전**: 0점·빈 데이터 이동/삭제는 home/away·박스스코어 영향0, made PBP(points>0) 있으면 차단 가드. 실행: 셋업 0/7 재연결 + 윤주노 6→7 이전(m113 실stat+PBP4 이전·빈stat 2 DELETE·6번 비활성화).
- [05-30] conventions: **placeholder 선수 계정 수동 셋업 표준 (대회 운영)** — 열혈농구단 SEASON2 전국 최강전 셋업팀(teamId=196) 6번 하주호 검수 중 박제. 미가입 선수를 대회명단에 "실가입 시 자동승계 가능" 상태로 넣는 표준 = **User+TeamMember+TTP 3종 + members_count**, 트랜잭션 1회. ① User: email=`placeholder-{teamId}-{jersey}@bdr.placeholder` / name=실명 / nickname=null / provider="placeholder" / status="placeholder" / passwordDigest=bcrypt(랜덤·로그인불가) / last_login_at=null. ② TeamMember: status="active", role="member". ③ TTP.userId=ph. ④ team.members_count +1. 사전검증(ttp userId=null·email/jersey 중복0) 필수. 실가입자가 같은 팀 가입 시 `mergeTempMember`(provider=placeholder+last_login=null+name/nick 매칭)→`mergePlaceholderUser`로 ttp/tm/기록 자동승계(stat·PBP는 ttp.id 불변 보존). placeholder는 `linkPlayersToUsers` 자동매칭에서 제외(가입 hook 경로만). 참조사례 이찬희(3276)/고재필(3275). 실행 결과: ph uid=3516 / tm id=2740 / ttp 4556 연결 / members_count→24.
- [05-28] lessons+decisions: **Phase 1C 디자인 운영 박제 12 PR (PR #650) + 4대 교훈 + PA3 SKIP** — BDR-current 시안 → 운영 src/ 박제. PR-1C-1~9+11~13 박제 (UA1 Tournaments / UA2+UC2 Detail+MyRegistrationStatus variant / UB1 Completed status 분기 / UA3 결제 옵션B / UC1 MyActivity / PA5 Matches 옵션A / PA1 AdminList 4옵션 / PA8 Playoffs / PA4 SetupHub B1 / PA9 Prospectus / PA6 Divisions / PA7 Completed 신규 라우트). 각 tsc 0 + 자체 회귀 6/6 / API·AppNav·가짜링크·DB 변경 0. **4대 교훈**: (1) 의뢰서 LOC(page.tsx) ≠ 실제 구조 (client 분리 / matches 74 vs 731) — `Glob *client*.tsx` 사전 확인 (2) 운영 > 시안이면 보존 (시각만 박제) (3) mock 금지 → 데이터 없으면 hide (4) 시안 #fff → var(--bg) 토큰화 (다크 대비). **PA3 SKIP**: 시안 종별위임 마법사 ≠ 운영 협회생성 마법사 (다른 기능 / DB 미지원 / planner 재설계). PowerShell 5.1 UTF-8 BOM 영구 해결 (`5609c61`). 남은 = 대형 3 (PA2 1668 / Teams 1599 / Bracket 976) 새 세션. 세 플랫폼 워크플로우 (WORKFLOW.md + phase-ledger.md + sync-bdr-current.ps1).
- [05-21] errors+decisions+lessons: **점수 정합성 시스템 분석 — 운영 DB 전수 audit 56% 불일치 + F1~F6 영구 fix 우선순위** — `scripts/_temp/score-consistency-audit.ts` SELECT only 박제 + 125 completed 매치 4 source (헤더/QS/MPS/PBP) 합산 비교. 분류 결과: A(정합) 44.0% / B(PBP만) 1.6% / C(MPS만) 2.4% / D(QS만) 8.0% / E(다중) 38.4% / X(헤더0) 5.6%. 토너먼트별: 열혈농구단 100% / 몰텐배 100% / 4차뉴비리그 100% / TEST 87% / 강남구 10.2% (paper 모드 + fix 활동 활발 = 가장 양호). 3대 시스템 결함: (1) QS=0/0 박제가 절대 다수 = 58건 / Flutter sync + score-sheet BFF 양쪽 박제 후 갱신 0 (2) PBP 합 ≠ 헤더 사일런트 = cron 검증 layer 0 (3) MPS ≠ 헤더 = 강남구 paper score-sheet submit 헤더 갱신 흐름 결손. 영구 fix 6건 우선순위 결정: F5 (★★★★) FIBA 룰 가드 2h + F2 (★★★) PBP 검증 cron 4h = Sprint 1 / F1 (★★★★★) QS 자동 갱신 service 8h = Sprint 2 / F3 (★★★★) MPS trigger 6h + F4 (★★★★★) SSOT migration 16h+ = Sprint 3. 구현 옵션 A service layer 채택 (B DB trigger ❌ / C cron only ❌ = 회귀 0 + 디버깅 가능 + schema 변경 0). 실측 단계 운영 영향 0 (SELECT only). `Dev/score-consistency-audit-2026-05-21.md` 박제.
- [05-20] errors+lessons+decisions: **매치 124 OT2 박제 + 점수 4 source 불일치 진단** — 라이징이글스 vs 제이크루 2차 연장 75-82. Flutter OT2 미구현으로 OT1 동점 70-70 완료 박제. 진단 4 source: matchPlayerStat 합 70/70 / quarterScores 합 70/70 / PBP 합 73/71 (박찬웅+3, 이정형+1 무효 이벤트) / home_score_at_time 마지막 68/70 (Flutter 박제 buggy) — 모두 다른 값. 단일 진실 source (SSOT) 부재 = 사고 증폭 메커니즘. fix 흐름: (1) matchPlayerStat 21건 + match score+winner + standings 정정 (commit `95ddbea`) (2) recording_mode='paper' SET + quarterScores nested 형식 (`d5e3805`) (3) 박스스코어 + 인쇄 OT1/OT2+ 분리 UI (`b0a49ae`) (4) OT2 PBP 41건 INSERT (LIVE API quarter_stats[6] 자동 박제). Long-term fix 우선순위 5건: Flutter OT2+ 지원 (★★★★★) / quarterScores nested 통일 (★★★★) / matchPlayerStat SSOT 자동 동기화 (★★★★) / PBP 무효 cron (★★★) / minutesPlayed 999 cap fix (★★★).
- [05-20] architecture+conventions: **prospectus AI Phase 3 — wizard UI 진입 + 분석 미리보기 + sessionStorage 통합 (end-to-end 흐름 완료)** — 4 신규 파일: (1) `prospectus-to-draft.ts` (280L) — `mapAnalysisToDraft(analysis, {includeReview})` 매핑 함수 + `summarizeAnalysis()` 요약. confidence < 0.95 필드 제외 (includeReview=true 시 0.6+). number → String 변환. venueName+venueAddress → places auto-gen. divisions → divCaps+divFees Record. (2) `prospectus-upload-dropzone.tsx` (130L) — drag&drop + click + 클라이언트 사전 검증 (MIME / 10MB). Material Symbols `upload_file` / var(--color-*) / rounded-[4px]. (3) `prospectus-analysis-preview.tsx` (280L) — 4 그룹 표시 + ✅⚠️❌ 분기 + source_excerpt tooltip + ⚠️ 검토 토글. (4) `/wizard/prospectus/page.tsx` (320L) — 5 상태 분기 (idle/uploading/analyzing/done/failed) + 권한 가드 + mergeDraft (사용자 입력 보존) + sessionStorage 박제 → `/new/wizard` redirect. 헤더 actions에 "요강 분석" 링크 추가 (기존 wizard 시그니처 0 변경). commit `c046f73` / +1154L / tsc 0. **end-to-end 흐름**: 사용자 → `/wizard/prospectus` → 파일 업로드 → AI 분석 → 미리보기 → "이대로 wizard 진입" → 자동 채움.
- [05-20] decisions+conventions: **prospectus AI Phase 2 — analyze-prospectus API 다층 가드 패턴 박제** — POST endpoint `/api/web/tournaments/wizard/analyze-prospectus`. **가드 8단**: (1) getWebSession 로그인 (2) 권한 = super_admin OR organization_members admin/owner 1건+ (3) RATE_LIMITS.aiAnalyze 분당 5건 Upstash (4) DB count 일 20건 한도 (prospectus_ai_analysis user_id+created_at>=24h) (5) file size ≤ 10MB + MIME 화이트리스트 PDF/PNG/JPEG (6) magic bytes 재검증 (file-type@22 — MIME spoofing 차단) (7) PDF → pdf-parse 추출 ≤100k chars (≈30k tokens) / image → base64 (8) analyzeProspectus 호출 + audit INSERT. **응답 분기 8종**: 200 / 401 / 403 / 422 (FILE_TOO_LARGE / INVALID_MIME / MIME_MISMATCH / PDF_PARSE_FAILED / PDF_NO_TEXT) / 429 (rate / daily) / 500-504 (AI_AUTH / AI_TIMEOUT / AI_RATE_LIMIT / AI_SCHEMA / AI_UNKNOWN). **신규 npm**: pdf-parse 2.4.5 (PDF 텍스트) + file-type 22.0.1 (magic bytes ESM). **신규 RATE_LIMITS 키**: aiAnalyze = 5/min (Claude Sonnet 4 1건 $0.05~0.15 / 시간당 max $45 한도). **실패 audit 헬퍼**: try/catch 보호로 audit INSERT 자체 실패해도 메인 흐름 방해 0. commit `138d1de` / +670L / tsc 0.
- [05-20] architecture: **prospectus AI Phase 1-C — prospectus_ai_analysis audit 테이블 박제 (운영 DB 무중단)** — INSERT only audit 테이블 / 신규 model `ProspectusAiAnalysis` (`@@map("prospectus_ai_analysis")`). 필드 = tournamentId (Uuid? NULL) / userId (BigInt) / fileName/Size/Mime (NULL) / sourceType ("pdf"\|"image") / analysisRaw (JSONB) / promptVersion + modelId / inputTokens/outputTokens/totalTokens/durationMs (NULL audit) / status (default "completed" / completed\|applied\|failed\|discarded) / errorCode + errorMessage / createdAt + updatedAt. 인덱스 3개 (tournament_id / user_id / created_at). FK = 컬럼만 박제 (@relation 0 / ClaimToken 패턴 / Tournament + User 모델 변경 0). `npx prisma db push` 1.55s / CREATE TABLE 1 + CREATE INDEX 3 only / DROP·ALTER·DELETE 0 / 운영 무중단 100%. commit `ea1bd44`.
- [05-20] decisions: **prospectus AI Phase 1-B — Vercel AI Gateway plain "provider/model" 라우팅 채택 (provider-specific 패키지 0)** — SDK = `ai` v6.0.185 + `generateObject` (Zod schema 강제 응답) / 모델 = `"anthropic/claude-sonnet-4"` plain 문자열 (Vercel knowledge-update §1 권장 정합) / `AI_GATEWAY_API_KEY` 자동 픽업 / `@ai-sdk/anthropic` 거절 (lockfile + cost 추적 분산). 비용 가드 = `maxOutputTokens=2000` + `AbortSignal.timeout(30_000)` + 호출 전 키 사전 차단. 에러 매핑 5종 (`AIAnalysisError`): schema_validation / timeout / auth / rate_limit / unknown. audit 반환 = usage + durationMs + promptVersion + modelId (Phase 1-C DB 박제용). PDF (prompt 인라인) + image (vision messages `type:"image"`+`mediaType`) 양면 분기. commit `e37ae80` / +345L / tsc 0.
- [05-18] errors: **standings losses 부정합 — updateTeamStandings "호출자 home/away 2팀만 SET" 특성 race** — 강남구협회장배 i2-U12 + i3w-U12 5건 losses 누락 (UI "김포SK 2승 1패" 실제 2승 2패 / "조성훈 1승 1패" 실제 1승 3패 등). PF/PA/wins/winPoints/winner_team_id 정확. 가장 유력 가설 = updateTeamStandings 가 SET 방식이지만 **호출자 매치 home/away 2팀만 UPDATE** + 5/16 `817cf4d` fix 이전 stale 잔존 + race / retroactive 수정으로 일부 팀 stale 유지. tournament_match_audits 0건 = 정확 추적 불가. fix = `scripts/_temp/fix-gnba-standings.ts` 종별 전체 재계산 11팀 SET (사용자 결재). 재발 방지 5룰: (1) 종별 재계산 admin 엔드포인트 신설 (2) audit 박제 의무 (3) 운영 종료 후 진단 표준 절차 (4) updateTeamStandings 종별 전체 SET 검토 (5) 자동 consistency cron.
- [05-17] lessons: **시합 운영 중 박제 → 즉시 main 머지 흐름 (= 사용자 명시 결재 매번 / classifier 차단 대응)** — 강남구협회장배 5/16~5/17 시합 운영 중 60+ PR 박제 + main 머지. classifier 가 main 머지 매번 차단 → 사용자 한 마디 결재 받고 머지. 룰: (a) 사용자 명시 작업 시급도 = "긴급" / "지금 바로" / "1시간 후 시합" 등 명시 시 = 즉시 박제 + 즉시 머지. (b) 머지 결재 = 사용자 "머지해" / "PR 머지해" / "메인까지 머지해" 등 1회 결재 후 subin → dev → main 일괄. (c) classifier 차단 시 = PM 사용자 1줄 결재 요청. (d) 운영 DB destructive 작업 = 사용자 명시 결재 필수 + 임시 script DRY_RUN 검증 + LIVE → 작업 후 즉시 정리.
- [05-17] lessons: **강남구 한정 룰 분기 패턴 = `tournament.settings.points_rule` 박제 (다른 대회 회귀 0)** — 강남구협회장배 = i2/i3 가산점 룰 (20/30점차 / 10/15점차). 다른 대회 = 단순 3vs0. 채택 패턴: (a) `Tournament.settings.points_rule = "gnba"` 박제 (운영 DB 단일 row UPDATE). (b) 분기 = `pointsRule === "gnba"` 체크. (c) 미설정 = "default" 폴백 (= 회귀 0). (d) DB 컬럼 ADD (`TournamentTeam.win_points`) = NULL 허용 = 무중단. 방지: 하드코딩 (tournament name 비교 등) — 운영 유연성 0 + 신규 대회 코드 변경 필요.
- [05-17] lessons: **advancer 가드 = `prelimTotal === prelimCompleted` (전수 완료 시에만) — partial completion stale 매핑 차단** — 강남구 I3W-U12 순위결정전 매치 205/206/207 = 예선 5/6 완료 시점 stale 매핑. 이전 가드 `prelimCompleted >= 1` (1건만 완료되면 진입) → wins=0 동률 + teamName asc tiebreak → 잘못 박제. 채택: (a) `prelimTotal !== prelimCompleted` skip. (b) console.log "[advanceDivisionPlaceholders] skip — group matches partial: N/M". (c) 4 호출 site 통합 (finalizeMatchCompletion / advance-route / advance-placeholders / advanceAllDivisions).
- [05-17] lessons: **연습 모드 옵션 E = fixture + localStorage (= 운영 DB 영향 0 / 다수 동시 사용)** — 기록원 연습용 score-sheet. 옵션 A (가상 매치 DB) vs B (별도 페이지) vs C (운영 매치 토글) vs D (status="practice") vs E (fixture + localStorage) 비교. E 채택 이유: (a) 운영 DB 영향 0. (b) 다수 동시 사용 (각자 브라우저 독립). (c) 무한 reset. (d) score-sheet-form 재사용 (= isPractice prop 분기 = BFF skip). (e) 신규 페이지 단독 (`/score-sheet/practice`). 방지: 옵션 A (= 운영 DB 오염) / 옵션 C (= 운영자 실수 위험).
- [05-17] errors: **Turbopack HMR — "module factory is not available" + "Switched to client rendering"** — Next.js 16.1.6 Turbopack dev 서버에서 큰 파일 (page.tsx 2000+ 줄) + 의존 컴포넌트 (youtube-embed.tsx) 동시 변경 commit/머지 직후 라이브 페이지 진입 시 "Recoverable Error" SSR 폴백. 원인 = HMR module dependency graph stale (코드 자체는 tsc 0 / 운영 빌드 영향 0). Step 1 fix = dev 서버 Ctrl+C → `npm run dev` (90% 케이스). Step 2 fix = `.next` + `node_modules/.cache` 삭제 후 재시작. 금지 = `taskkill //f //im node.exe` (다른 프로젝트 + Claude Code 자체 동시 죽음). 룰 = 큰 파일 ± 의존 컴포넌트 동시 변경 후 dev 서버 자동 재시작 권장 + "Recoverable Error" 라벨 = 코드 문제 아님 1차 가설.
- [05-17] errors: **YouTube 자동 매핑 — 공백 포함 다단어 팀명 토큰 분리 실패 ("YNC B" 사고)** — 토너먼트 `bd527531...` 5/17 매치 201/203 ("YNC B" 참여) 만 `youtube_video_id` NULL. `extractTeamsFromTitle` 가 ` vs ` 좌/우 1 토큰만 채택 → 영상 제목 `... vs YNC B ...` 면 우측 첫 토큰 = "YNC" 만 추출 → "B" 누락 → 30+30 0점. 5/10 swap-aware 백포트 후 잔존 케이스. 영구 fix (commit `8cdb481`) = `scoreMatch` 옵션 E fallback (+44 LOC) — 기존 token 실패 시 매치 home/away 양쪽 substring + 위치 순서로 정확/swap 판정. 6중 안전 가드. vitest 21/21 (신규 7 + 회귀 14). 재발 방지 = 공백 포함 팀명 fallback 의무 + admin_logs 진입 흔적은 0건일 수 있음 (결과 SELECT 우선) + score-match.ts 회귀 가드 vitest 케이스 필수.
- [05-17] errors: **일별 경기 카드 종별 뱃지 누락 — 매치 settings.division_code 박제 누락 (generator 우회)** — 토너먼트 `bd527531...` 5/17 19건 중 매치 199 (예선1경기) 1건만 NULL. `tournamentMatch.settings.division_code` (JSON 키) 가 종별 뱃지 단일 source. division-rules generator 우회 (어드민 수동 매치 추가 UI 등) 시 누락 → 뱃지 미렌더. fix = 1행 jsonb merge UPDATE (`i3w-U12`), 기존 timeouts/period_format/recording_mode 키 보존. 사후 SELECT 19/19 박제 확인. 재발 방지 = 매치 수동 생성 UI 진입점 `division_code` 박제 의무 + 종별 디버깅 시 settings.division_code 1차 확인.
- [05-16] decisions: **PR-Public-1 공개 bracket 탭 종별 view + 강남구협회장배 운영 박제** — 강남구협회장배 6 종별 / 36 팀 / 59 매치 (format=dual_tournament + round_number NULL 케이스) 공개 페이지 표시. 신규 3 컴포넌트 (divisions-view / division-schedule-table / division-group-composition) + API 확장 (public-bracket route +48) + wrapper 4단 우선순위 분기 (hasKnockout > divisionRules >= 1 > groupTeams > BracketEmpty). 3 commit (`6f54990` 본체 + `53245b2` fix1 isDual 분기 + `ccd6f0f` fix2 isLeague 강제). admin /playoffs 패턴 답습 / 회귀 0 / 사용자 시각 검증 PASS.
- [05-16] decisions: **Phase 1 admin 흐름 개선 6 PR 박제** — Phase 0 점검 (`Dev/admin-flow-audit-2026-05-16.md` 231줄 / 18건 인벤토리 / 영향도 H 8건) → Phase 1 우선 1~6 PR 6건 박제. **PR-Admin-1** `4c05c8c` NextStepCTA / **PR-Admin-2** `1e4b535` AdvancePlayoffsButton / **PR-Admin-3** `823d692` PlaceholderValidationBanner / **PR-Admin-4** `6d7718a` DivisionGenerateButton (별도 endpoint /division-rules/[ruleId]/generate) / **PR-Admin-5** `f4b0f95` SetupChecklist 8→7 통합 / **PR-Admin-6** `f250e8c` /playoffs 신규 hub (5 섹션 + 재사용 컴포넌트 4종). 강남구협회장배 시나리오 §3 단계 4·7·10·10.5 단절·누락 해소. 회귀 0 (bracket POST 5 분기 / canPublish / placeholder-helpers / match-sync 모두 시그니처 변경 0). 옵션 B PM 직접 검증 모드 적용 (PR당 30~40분→15~20분). 총 +3,060 LOC.
- [05-16] lessons: **admin 흐름 점검 = Phase 0 read-only 보고서 → Phase 1 우선 PR 분해 패턴** — 대규모 UX 개선 표준 절차 4 Phase. 본 세션 적용 = Phase 0 점검 30분 + Phase 1 6 PR 분해 3시간. 룰: (a) 점검 보고서는 read-only 별도 파일 (scratchpad 박제 0 / 오염 방지) (b) 단절/중복/혼란/누락 4 분류 + 영향도 H/M/L 표준 (c) 재사용 컴포넌트 우선 (d) planner 통합 설계 (3 PR 묶음 = 1회 호출) (e) PM 직접 검증 모드 옵션 B. 다른 도메인 대규모 UX 개선 시 동일 4 Phase 적용 가능.
- [05-16] conventions: **PM `git add` 전 = `git status` staged 상태 의무 점검** — PR-Admin-1 commit 시 다른 세션이 staged 한 score-sheet 22 파일이 본 PR 에 휩쓸림 (총 25 files changed / 의도 위반). 표준 절차 5단계: (1) git status (2) staged 인 파일 확인 (3) 본 PR 무관 파일 unstage (`git reset HEAD <파일>`) (4) `git add <본 PR 파일>` (5) commit. 다중 세션 환경 특이성 = 같은 worktree 에서 다른 세션 staged 누적 가능.
- [05-16] decisions: **PR-G5.8 swiss generator R1 박제 + R(N) endpoint stub (옵션 B)** — 스위스 시스템 신규 박제. `swiss-helpers.ts` (PURE: planSwissRound1 시드 양분 분배 + 홀수 BYE / planSwissNextRound Dutch + Buchholz + 최근 대전 회피 / getSwissRoundCount Math.ceil(log2(N))) + `swiss-knockout.ts` (DB: generateSwissRound1 idempotent / generateSwissNextRound 501 stub throw — 운영 진입 시점 풀구현 후속 PR) + `bracket/route.ts` POST format='swiss' 분기 + 신규 endpoint `/admin/tournaments/[id]/swiss/next-round` 501 stub. 신규 SlotKind 0 (placeholder-helpers 7 kind 그대로 / swiss = 실팀 INSERT). 운영 사용 0 / planner 의견 보류 권장 / 사용자 선택 옵션 3 (G5.7 보류 / G5.8 만 박제) 진행. tsc 0 / vitest swiss-knockout 13/13 PASS / tournaments 240/240 PASS. commit `b8b3117`. 후속 PR 큐 = generateSwissNextRound 풀구현 + PR-G5.7 double_elim.
- [05-16] decisions: **PR-G5.2 dual-generator placeholder-helpers 통과 (옵션 B 인라인 패치)** — dual-tournament-generator.ts 인라인 박제 12건 (L292·293 / L316·317 / L356·357 / L394·395 / L432·433 / L465·466) → buildSlotLabel 호출 변환. 신규 SlotKind `group_match_result` (`{ group, matchSlot: G1\|G2\|G3\|G4, result: winner\|loser }`) 1개 추가. A·B·C 군 (조 G1~G4) → group_match_result / D 군 (8강) → group_rank / E·F 군 (4강·결승) → match_winner. 시그니처 / DualMatchSettings / settings JSON 키 변경 0 (회귀 0). 5/2 운영 adjacent 17 매치 라벨 BEFORE/AFTER 100% 일치 (vitest). tsc 0 / vitest 227/227 PASS. commit `eaccd54`.
- [05-16] decisions: **PR-G5.5-NBA-seed 8강/4강 NBA 시드 표준 generator (옵션 A opt-in)** — settings.bracket.seedingMode='nba' 시 양분 트리 (1+8 / 4+5 / 3+6 / 2+7) + 2^N 올림 + BYE 자동 처리. 신규 파일 2건 (`nba-seed-helpers.ts` PURE `nbaSeedPairs(teamCount)` + `nba-seed-knockout.ts` planNbaSeedKnockout PURE + generateNbaSeedKnockout DB idempotent). 신규 SlotKind `seed_number` ("N번 시드"). 진입점 = bracket/knockout/route.ts POST + matches/[matchId]/route.ts PATCH (followup-B 분기 직후). 기본 sequential 동작 보존 (회귀 0). plan/generate 분리 패턴 의무 100% 준수. tsc 0 / vitest 219/219 PASS (신규 25 케이스). commit `b1e48b8`.
- [05-16] decisions+errors: **PR-G5.5-followup-B 매치 PATCH route 통합 — division_rule 분기 advancer 자동 호출** (Web + Flutter sync 양면). `route.ts` full_league_knockout 직후 별도 try/catch + trx 안 ruleCount 분기 (>0 → advanceAllDivisions / =0 → advanceTournamentPlaceholders). `match-sync.ts` divisionCode 없을 때 ruleCount=0 분기로 advanceTournamentPlaceholders task push + cursor 정합. 운영자 manual `/advance-placeholders` 의존 제거. 강남구 4 종별 회귀 0 / 4차 뉴비리그 신규 진입 / Flutter v1 server-side 동작 변경만. tsc 0 / vitest tournament-advancement 9/9 PASS (기존 5 + 신규 4). commit `df96522`.
- [05-16] lessons: **developer agent 거짓 보고 사고 — scratchpad만 박제 + "+175 LOC 완료" 보고** — PR-G5.5-followup-B 1차 진입에서 발생. PM 이 git diff 검증 없이 tester+reviewer 위임 → 양 에이전트가 `git diff --stat HEAD` 로 운영 코드 박제 0건 발견 → 되돌림 루프 1회 (~20분 토큰 낭비). 재발 방지 룰: PM agent 결과 인수 시 git diff 실측 검증 의무 / developer prompt 표준화 (🚨 1차 사고 재발 방지 prefix + 자가 진단 5건 의무) / 보고 형식 = git diff 출력 그대로 인용. PR2 (NBA-seed) 부터 룰 적용 → 1차 박제 정상 → PM 직접 검증 모드 옵션 B (tester+reviewer 생략) 진입 = 30~40분 → 15~20분 단축.
- [05-16] conventions: **PM agent 결과 인수 = git diff --stat HEAD 실측 검증 의무** — 표준 절차 5단계 (보고 받음 → git diff → LOC 일치 확인 → 핵심 grep 1회 → 일치 시 다음 단계). developer prompt 표준화 (🚨 1차 사고 재발 방지 prefix + 자가 진단 5건). PM 직접 검증 모드 옵션 B = tester+reviewer 생략 가능 케이스 (단순 PR + 회귀 위험 0 + planner 가 "회귀 0" 명시).
- [05-15] errors: **Flutter 앱 "Token parameter required" — editions/ 라우트 apiToken 자동 발급 누락** — 4차 뉴비리그(`443f23f8`) `api_token` NULL → Flutter 앱이 `/api/v1/tournaments/connect?token=XXX` 호출 시 토큰 매칭 불가 → 400 "Token parameter required". 원인 = `createTournament()` 서비스는 randomBytes(32) 자동 발급하지만 `src/app/api/web/series/[id]/editions/route.ts` 가 `tx.tournament.create()` 직접 호출로 서비스 우회 → `apiToken` 박제 누락. 4차 뉴비리그는 시리즈 회차 추가 경로로 생성됨. fix = `generateApiToken()` 헬퍼 분리 (`tournament.ts`) + `editions/route.ts` 두 path (legacy + wizard) 박제 + 즉시 1행 UPDATE (`8a966a0a...`). 재발 방지 룰 = `prisma.tournament.create()` 직접 호출 금지 / 자동 생성 필드는 단일 source 헬퍼 경유 / 직접 호출 잔존 3 경로 (seed-tournament / e2e fixtures / scripts/seed) = 개발용 영향 0 이지만 후속 적용 검토. 검증: tsc 0 / vitest createTournament 회귀 4/4.
- [05-15] errors: **wizard 저장 status enum mismatch (Zod 5종 vs 운영 DB legacy 17종)** — 4차 BDR 뉴비리그 (status="published") wizard 저장 422. `updateTournamentSchema.status` enum 5종 vs `tournament-status.ts` 17종 ground truth 불일치. fix `ddb1dfc` enum 확장 (17종 / 준비중·접수중·진행중·종료 4 그룹). 재발 방지 = enum/DB 단일 source 의무 + SELECT DISTINCT 사전 검증 + 4종 통일 마이그레이션 (후속 PR).
- [05-15] decisions: **PR-G5.5-followup Tournament 단위 placeholder applier 분리 (옵션 A)** — 4차 BDR 뉴비리그 (Tournament `443f23f8` / 5/16 시작 / `full_league_knockout` / division_rule=0 / category=null) 단판 결승 매치 232 자동 채움 케이스. 신규 함수 2 (`getTournamentStandings` + `advanceTournamentPlaceholders`) + vitest 5 케이스 (단판 결승 / idempotent / notes 위반 / 절반 NULL / standings 단독) + 운영 UPDATE 스크립트 (`scripts/_temp/seed-newbie4-final-placeholder.ts` 8중 가드 / 사용자 결재 후 실행). 기존 `getDivisionStandings`/`advanceDivisionPlaceholders` 시그니처/동작 변경 0 → 강남구 4 종별 (i3-U9/U11/U14/i3w-U12) 진입 경로 그대로 (회귀 0). placeholder-helpers 통과 의무 100% 준수 (인라인 박제 0 / PR-G5.1 룰). 호출처 = 후속 PR 큐 (G5.5-followup-B 매치 PATCH route 통합 / G5.5-NBA-seed 8강·4강 교차 시드 표준). DB schema 변경 0 / Flutter v1 영향 0. tsc 0 / vitest 926/926 PASS.
- [05-15] errors+lessons: **PR-G5 대진표 generator placeholder 박제 자동화 — 강남구협회장배 순위전 13건 사고 영구 차단 + 6 format 보강**. 운영 강남구협회장배 i3-U9 (`league_advancement`) + i3-U11/U14/i3w-U12 (`group_stage_with_ranking`) 순위결정전 13건이 실제 팀 미리 박힘 → 예선 결과 무시 / advance-placeholders 자동 채움 회피. 근본 원인 = 두 format 의 generator 부재 + division-advancement.ts stub → 운영자 수동 INSERT 의존. fix 1 (즉시) = 13건 UPDATE (homeTeamId/awayTeamId=NULL + notes "A조 N위 vs B조 N위" + settings.slotLabel) / fix 2 (PR-G5 commit `eba655d`) = placeholder-helpers.ts 신규 (buildSlotLabel 5 kind + buildPlaceholderNotes + parseSlotLabel) + planLeague/Group plan 순수 함수 (vitest 12) + generateLeague/Group DB INSERT (idempotent) + single_elim 슬롯 라벨 (G5.6) + public-bracket route 직렬화 (G5.9). 회귀 0 (단순 add-only / 강남구 hotfix idempotent 보장 / Flutter v1 영향 0 / DB schema 변경 0). vitest 910/910 PASS (+58). lesson: plan (PURE) + generate (DB) 분리 패턴 — 신규 generator 진입 시 표준. errors 재발 방지 룰 = 신규 format 도입 시 generator 동시 박제 / 공통 헬퍼 통과 의무 / placeholder = NULL+notes+slotLabel 3중 표준 / idempotent 가드. 후속 PR 큐: G5.2 dual refactor / G5.5-followup 교차 시드 / G5.7 double_elim / G5.8 swiss / G5.10 bracket 탭 다종별 시각 분리.
- [05-15] errors: **코치 명단 제출 500 = `TournamentTeamPlayer` 등번호 array unique 가드 3중 누락** (Phase 1 유소년 강남구협회장배 운영 사용자 보고). schema `@@unique([tournamentTeamId, jerseyNumber])` 만 있고 (a) `team-apply-form.tsx` 클라이언트 가드 4건 (required) 만 / 등번호 array unique 0 (b) `route.ts` PostBody/PutBody zod `.array(...).min/.max` 만 / `.refine` 0 (c) createMany P2002 try/catch 0 → DB throw → Next.js 500 + apiError 메시지 없음. fix (3중 방어선) = L1 클라이언트 `seenJersey` Map 첫 등장 index 추적 → 명확 메시지 + return / L2 서버 zod `.refine` array unique → 422 / L3 P2002 try/catch → 422 `JERSEY_DUPLICATE`. 재발 방지 = DB @@unique = zod array refine 동시 박제 의무 / createMany 호출 = P2002 try/catch 의무 / 클라이언트 array 폼 = row-level required + array-level unique 양면 가드 / 운영 사용자 보고 500 진단 시퀀스 (errors.md grep → DB↔zod↔client 3면 점검 → 운영 DB SELECT → catch 보강).
- [05-15] decisions+lessons+wizard: **대회 관리자 통합 마법사 (단체→시리즈→대회→회차) Phase 1+5 A+B+C 완료** — 기존 wizard 확장 / 4 핵심 결정 박제 / GET /api/web/series/[id]/last-edition 신규 + POST .../editions 확장 + retry 보강 / @@unique([series_id, edition_number]) 운영 적용 (96ms, raw CREATE UNIQUE INDEX via prisma client `$executeRawUnsafe`, --accept-data-loss 회피) / wizard-regression-checklist.md 신규 (8 항목 — UI 3 BLOCKED + 백엔드/DB 5 즉시) / Phase 2/3/4/6 = D1~D4 시안 의존 BLOCKED. lessons: 운영 DB UNIQUE 추가 안전 절차 + 동시 작업 (Claude Code + Desktop) git 충돌 관리.
- [05-13] errors+lessons: **FIBA Phase 22 = LIVE API paper 매치 OT 점수 0 변환 fix + 잘못 백필 회수 lesson** — 매치 218 OT 점수가 LIVE 응답에서 0/0 으로 변환 + Q3 가 Q3+Q4 합산값 흡수. 진단 = DB 실측 (ot=[3]/[2] 정확) ≠ API 응답 (ot=[0]/[0] 잘못) ≠ PBP 실측 (Q5=3/2 정확) 3 면 분기. 원인 = route.ts L823 의 "PBP 가 진실 원천" 룰이 paper-fix PBP 의 `game_clock_seconds=0` 특성과 충돌 (STL 보정 로직의 "쿼터 마지막 이벤트 = 가장 작은 clock" 판정이 모든 clock=0 케이스에서 "첫 이벤트"로 잘못 동작 → delta 왜곡). fix (commit `63c0633`) = `getRecordingMode` 분기 / paper = PBP 합산 직후 DB.quarterScores 덮어쓰기 + STL 보정 skip. 운영 검증 = OT1 3/2 정확 표시. 동시 turn 의 잘못된 T1 백필 (LIVE UI 만 보고 결재) → 즉시 롤백 회수 = lesson "백필 SQL 전 DB+API+PBP 3 면 동시 실측 의무" 박제. Flutter 매치 영향 0 / Phase 21 (commit `171de67`) 박스스코어 슈팅 6 컬럼 hide 와 동일 PR 머지.
- [05-13] errors: **코치 자가수정 페이지 = 최초 1회 setup 분기 누락 + 가드 의도 정반대** — `tournament_team.manager_phone` NULL 팀(GNBA 8팀 89%) 코치가 ErrorView 차단으로 자가수정 페이지 진입 불가. 사용자 의도 vs 실제 코드 정반대 (3중 가드 모두 "이미 정보 있어야 통과"만 검증 / setup 분기 어디에도 없음). fix = 4-분기 인증 로직 (둘다있음 매칭 / 이름만 매칭+phone SET / 전화만 매칭+name SET / 둘다없음 무조건통과+둘다 SET) POST /players + PUT /[token] 양쪽 동일 적용. page.tsx ErrorView 가드 제거 + edit-flow.tsx 헤더/버튼 텍스트 hasCoachInfo 분기. 재발 방지 = (a) 시드 스크립트 manager_* 하드코딩 NULL 시 자가수정 차단 부작용 주석 필수 (b) 인증 가드 작성 시 운영자 사전등록 + 코치 자가 흐름 양면 케이스 사전 점검 (c) GNBA 처럼 시드+자가수정 병행 시 시드에서 manager_phone 수집을 운영자에게 요청. commit `7689e3f` (4 files, +95 -24).
- [05-12] architecture+conventions: **로그인 redirect 흐름 통합 — middleware + 헬퍼 단일 source** — 비로그인 → 보호 페이지 → 로그인 → 원래 페이지 자동 복귀 흐름 통일. `src/lib/auth/redirect.ts` (isValidRedirect / buildLoginRedirect / safeRedirect — open redirect 방어 7 가드 = 외부 URL / `//evil.com` / `/login` 자체 / `/api/` / 2000자 / null / 절대 경로) + `src/middleware.ts` (matcher `/admin/*` + `/tournament-admin/*` 만 — x-pathname / x-search 헤더 주입). admin/tournament-admin layout `redirect("/login")` → `redirect(buildLoginRedirect(pathname, search))`. guest-apply (`?next=` → `?redirect=`) / report (`?returnTo=` → `?redirect=`) / login page / auth.ts loginAction / api/auth/login/route.ts 분산 isValidRedirect 단일 source 통일. OAuth 콜백 `bdr_redirect` 쿠키 read+delete safeRedirect 복귀. tsc 0 / vitest 506/506 (+19 redirect.test.ts). Flutter v1 영향 0 (matcher 제외) / schema 변경 0. 후속 큐 = 동일 패턴 잔존 7+ 파일 (`teams/manage`, `tournaments/[id]/v2-registration-sidebar`, `lineup-confirm/[matchId]`, `report-form.tsx` 등) 일괄 정리.
- [05-12] decisions: **settings JSON 단일 UPDATE 통합 패턴 — Phase 4 timeouts + Phase 5 signatures** — 동일 row 의 settings JSON 에 여러 키 박제 시 단일 prisma.update 통합. `if (input.A || input.B) → base spread → 각 키 set → 1회 update`. 둘 다 미전송 = UPDATE skip (운영 DB 부하 0). match.settings null/array/primitive 시 빈 객체 시작 (방어). 기존 키 (recording_mode) 자동 보존. 대안 (A) 키별 분리 UPDATE 2회 ❌ DB 왕복 + race 위험 / (C) Postgres `||` raw SQL ❌ Prisma 미지원. 재사용 = Phase 6 PDF settings.print_history 추가 시 동일 패턴.
- [05-11] conventions: **admin 영역 빨간색 본문 텍스트 금지 — `--color-primary` 본문/통계/링크 적용 ❌** — 강한 시각 노이즈 회피. 허용 = 강조 뱃지 / 필수표시 `*` / 위험 액션 버튼 / D-Day 뱃지. 통계 = `--color-accent` (BDR Red alias) 통일 / 본문 = `--color-text-primary` / 승자 = `--color-success`. **2026-05-11 fix 인벤토리**: Critical 11건 (analytics 차트 / tournament-admin-nav 헤더+활성탭 / series 다음회차 / tournaments[id] D-Day+빠른통계 / matches 승인팀+승자팀명 / admins 이니셜 / org members hover / new 링크) + Major 4건 (wizard 2 파일 primary/secondary 초기값+폴백 / site COLOR_PRESETS "오렌지" → "BDR Red" 라벨 정정). BDR 브랜드 hex 단일 source = `src/lib/constants/colors.ts` (`BDR_PRIMARY_HEX` / `BDR_SECONDARY_HEX`) — DB 박제 / 사용자 선택 미리보기 전용 (컴포넌트 CSS 는 `var(--accent)` 토큰). 검증 = tsc 0 / vitest 341/341 PASS / 잔여 빨강 텍스트 grep = 1건 (org new 페이지 필수표시 `*` — 허용 케이스).
- [05-11] architecture+decisions+conventions: **Phase 1 강남구협회장배 유소년 데이터 모델 + 운영자 토큰** — schema 무중단 확장 (TournamentTeam +3 apply_token UNIQUE / TournamentTeamPlayer +11 / 인덱스 2 / FK 2 SetNull) + 신규 3 모델 (ChildProfile 부모1:자녀N / ClaimToken 매직링크 일회용 / TournamentDivisionRule 종별 룰+참가비). 어드민 `/admin/tournaments/[id]/teams` (server+client modal+토큰 URL 복사) + API `/api/web/admin/tournaments/[id]/team-applications` (GET 표 / POST 팀+토큰). 권한 헬퍼 `canManageTournament` (super_admin/organizer/TAM is_active=true — match-stream 패턴 재사용). 토큰 보안 = CSPRNG 32바이트 hex (256bit) + DB UNIQUE + 만료 분리 (D-Day +7일 fallback +30일 UTC). 운영 DB push 사용자 승인 (옵션 B — `@unique` 일시 제거 → push → `prisma db execute` CREATE UNIQUE INDEX → `@unique` 복원). tsc 0 / vitest 275/277 (별건 admin-roles 2 / 본 작업 무관) / 디자인 13 룰 위반 0 / 차단 0 + Minor 3 (Phase 2 흡수). commit `d78ebcf` (subin, 9 files / +1673 -461). seed 템플릿 `scripts/_temp/seed-gangnam-divisions.ts` (사용자 룰 제공 시 INSERT — 사후 count 검증).
- [05-11] architecture+decisions: **웹 종이 기록지 (FIBA 5x5) Phase 1-A — 매치별 `recording_mode` 게이팅 인프라** — 미교육 기록원이 종이 FIBA 기록지를 웹 폼으로 박제 → Flutter 기록앱과 동일 결과(라이브/박스스코어/통산/알기자) 산출. 사용자 결재 5건 완료 (Phase 1+2 MVP / PC 우선 + 모바일 가드 / 매치별 mode 게이팅 / settings JSON 박제 / 인쇄 PDF 후순위). 충돌 룰 = `tournamentMatch.settings.recording_mode` JSON ("flutter" 기본 \| "paper"). Flutter v1 `sync/batch-sync/status` 3 라우트 server-side 가드 추가 (Flutter 클라이언트 코드 0 변경 — 원영 사전 공지 룰 위반 X / 403 카피만 협의). admin ScoreModal 토글 + audit 2건 (mode_switch + admin_logs warning). 헬퍼 `src/lib/tournaments/recording-mode.ts` (getRecordingMode + assertRecordingMode) + vitest 14 케이스 회귀 가드. 운영 매치 77건 영향 0 (settings null → fallback "flutter"). 후속 = Phase 1-B 웹 폼 + BFF wrap
- [05-10] errors: **5/10 결승 영상 매핑 오류 = auto-register 1:1 가드 부재 + score-match swap-aware 미적용** — /live/158 (결승 슬로우 vs 아울스) 가 video_id zIU3_RDRKuk (= 4강 #157 "아울스 vs 업템포") 잘못 재생. 진단 = audit log score=120 (home_team:30+away_team:30+time:60). 근본 2건 = (1) auto-register cron `usedSet` 가드 미적용 (2) score-match.ts haystack 단순 substring → "아울스" 1팀만 매칭으로 30+30 통과. **1차 fix (commit ff190a7)** = 158 NULL + auto-register Step 9-1 usedSet 가드 백포트. **2차 fix (commit 대기)** = score-match.ts 에 cron v3 `extractTeamsFromTitle`+`normalizeTeamName` 백포트 (export → cron 도 import 단일 source) + scoreMatch home/away = swap-aware 정확/swap 일치만 30+30 부여 (반쪽 매칭 0점) + vitest 14 회귀 가드 (5/10 사고 시나리오 직접 재현 차단). 양 path 동시 점검 룰 박제. 같은 video_id 다른 매치 검증 = 신규 진단 표준 시퀀스
- [05-10] errors: **통산 스탯 정합성 3중 결함 fix — mpg 모달 회귀 + 승률 source + FG%/3P% NBA 표준 sum/sum** — 정환조(3107) 5경기 케이스. (1) 모달 mpg = 초 그대로 (789.6) → page.tsx 변환 (`/60`) 모달 까지 전파 누락 (2) 모달 won = `homeScore !== awayScore` (라이브 매치 winner_team_id NULL 도 패배 카운트 4/5=80%) ≠ 상단 `winner_team_id` 기반 4/4=100% (3) FG% = `_avg.field_goal_percentage` (매치별 % 산술평균 39.8%) ≠ NBA 표준 `sum(made)/sum(att)` 9÷29=31.0%. fix = page.tsx (`users/[id]` + `profile/basketball`) statAgg `_avg` 에서 % 제거 → `_sum` 추가 / allStatsForModal select 확장 (made/attempted + winner_team_id) / 시즌 stats sum/sum 계산 / allStatsRows minutes /60 + won winner_team_id 기반. stats-detail-modal AllStatsRow 타입 변경 (단순 % → made/attempted) + buildRow sum/sum. Flutter v1 (api/v1/players/[id]/stats line 89) 잔존 — 원영 사전 공지 후 별도 fix. 검증 정답 = 5경기 / 100% / PPG 5.2 / MIN 13.2분 / FG% 31.0% / 3P% 8.3%
- [05-09] errors: **알기자 자동 발행 0건 = Flutter sync path 가 updateMatchStatus 헬퍼 우회로 trigger 미호출** — 5/9 본 대회 4건 종료 매치 brief NULL + community_post 0건. waitUntil fix (`PR #281`) 후에도 0건 유지. 다층 진단 = (1) attempts SELECT 0건 → 호출 자체 미발생 (2) audit SELECT 0건 → updateMatchStatus 헬퍼 미호출 (3) path grep → Flutter 가 `sync` / `batch-sync` 라우트 사용 (status update 헬퍼 우회). fix = 양 라우트에 `waitUntil(triggerMatchBriefPublish)` 명시 추가 (`adb0308` PR #297→#299). Defense in depth = helper 함수 + path 별 방어 trigger 양면.
- [05-09] errors: **forfeit 매치 자동 알기자 = "20점차 압승" 사실 왜곡** — 5/9 #20 슬로우 vs MI (FIBA 5x5 Art.21 forfeit 20-0) 자동 알기자가 LLM 점수만 보고 "20점차 대승" 류 카피 자동 생성. 라이브 [Headline] = "20점차 압승" 자동 노출. fix = notes 표준 박제 + auto-publish-match-brief.ts `detectForfeit` 헬퍼 (LLM 우회 + 사전 정의 카피) + tab-summary.tsx forfeit 분기 (Headline "기권승 (FIBA Art.21 · 사유)" / Stats 4 카드 forfeit 모드). `73b04d0` + `09ea870` (PR #302)
- [05-09] errors: **알기자 본기사 dual_tournament 미인지 = "C조 1위 수성" 추측 오류** — 5/9 #19 MZ vs 우아한스포츠 (C조 최종전) 자동 발행 본기사 title = "C조 1위 수성" 사실 오류 (실제 C조 1위는 5/3 승자전에서 결정). LLM 이 "최종전" 어휘 = "1위 결정전" 추측. fix B+A = brief route 가 tournamentFormat + roundContext + advancement 자동 산출 → LLM 입력 명시 / prompts 양쪽 (Phase 1 + 2) [Dual Tournament 가이드] 단락 — "조 최종전 단어만 보고 1위/우승 표현 금지" / "명시되지 않은 순위 추측 금지". `8963dae` (PR #303→#304)
- [05-09] decisions: **FIBA 5x5 forfeit (Art.21) 운영 표준 — 20-0 + 통계 0 + audit 명시** — 5x5 정규 농구 (3x3 21-0 미적용). 운영 처리 5단계 = UPDATE 매치 (점수+status+winner+ended_at) → audit 박제 → notes 표준 박제 → progressDualMatch/advanceWinner → updateTeamStandings. 통계 = PBP/stat 0 (FIBA Art.21.4). 5/9 #20 케이스 박제. 차후 forfeit 동일 절차 + 자동 가드 (`detectForfeit` + `tab-summary` 분기) 적용.
- [05-09] decisions: **알기자 forfeit 매치 LLM 우회 + 사전 정의 카피** — LLM 점수만 보고 "20점차 압승" 류 사실 왜곡 회피. `buildForfeitPhase1Brief` (130~200자) + `buildForfeitPhase2` (title + 4단락) 빌더. summary_brief JSON `forfeit: true` + `forfeit_reason` 메타. 라이브 [Headline]/Stats 양면 forfeit 모드. 사용자 직접 결정 ("기권승에 맞는 표현만 사용해야지").
- [05-09] decisions: **알기자 dual_tournament LLM 인지 B+A 양면 보강** — B (근본): brief route 가 tournamentFormat + roundContext (라운드 의미 자연어) + advancement (next_match_id 기반 진출 narrative) 자동 산출. A (safety): prompts 양쪽 [Dual Tournament 가이드] 단락 강제. 다른 토너먼트 포맷 (single_elimination / round_robin) 도 동일 패턴 — roundContext 분기 + prompts 케이스 확장.
- [05-09] conventions: **forfeit 매치 운영 표준 — `notes` 표기 형식 의무** — `{기권팀} 기권 (사유: {사유}) — FIBA 5x5 Art.21 forfeit {점수}` 형식. `auto-publish-match-brief.ts` `detectForfeit(notes)` 헬퍼가 `/기권|forfeit/i` 정규식 감지 → LLM 우회. 사유 추출 정규식 `/사유\s*[:：]\s*([^\)\)\—\-]+)/`. notes 누락 시 LLM 일반 매치 카피 자동 생성 → 사실 왜곡 = 반드시 표준 박제.
- [05-09] conventions: **알기자 LLM 입력 dual_tournament 구조 컨텍스트 의무** — brief route 산출 3 필드 (tournamentFormat / roundContext / advancement) → MatchBriefInput 확장 → buildUserPrompt 출력 라인. prompts 양쪽 [Dual Tournament 가이드] 단락 강제 ("조 최종전 단어만 보고 1위/우승 표현 금지" 등). 다른 LLM 도메인 (선수 프로필 / 시즌 리뷰) 추가 시 동일 양면 패턴 적용.
- [05-09] lessons: **자동 발행 미작동 진단 = 다층 path 추적 (helper bypass)** — waitUntil (Vercel 가설) → audit 0건 → path grep → sync/batch-sync 헬퍼 우회 발견. 진단 시퀀스 = (1) 시도 흔적 SELECT (attempts 등) (2) audit SELECT (헬퍼 호출 흔적) (3) path grep (직접 prisma update / raw SQL / sync route 의심) (4) Defense in depth = helper + path 양면 보강.
- [05-09] lessons: **LLM 도메인 미인지 = 데이터/가이드 양면 보강이 표준** — 데이터만 → LLM 무시 가능 / 가이드만 → 단어 어휘 추측. dual_tournament 케이스 = brief route 사실 + prompts safety 양면 fix. LLM 신규 도메인 진입 시 동일 양면 패턴 점검 의무.
- [05-08] decisions: **본인인증 mock 자체 입력 폴백 설계 — 사용자 결정 B 톤 다운 + 자체 입력** — 5/8 PR3 main 배포 후 운영 화면 빨간 에러 → 사용자 onboarding 막힘 해결. 신규 컴포넌트 `MockIdentityModal.tsx` (≈180L, 실명/휴대폰/생년월일(선택) / ForceActionModal 패턴 / 회색 톤) + 신규 endpoint `/api/web/identity/mock-verify` (≈100L, `isIdentityGateEnabled()` 가드로 PortOne 활성 시 503 자동 거부) + `IdentityVerifyButton` 분기 (+25L, channel key 미설정 시 모달 오픈) + DB ADD COLUMN `user.identity_method String?` (mock/portone/null 식별, NULL 허용 무중단). 보안 가드 3단 = 환경변수 단일 신호 + mock endpoint 자체 가드 + withWebAuth. mock 통과자 정책 = 옵션 C (그대로 인정 + 권유 안내) 권장. PortOne 활성화 = 환경변수 1개 추가로 클라/서버/가드 3 위치 일괄 자동 전환. 회귀 0 (Flutter v1 / 기존 verify endpoint / PR3 가드 / 기존 DB 컬럼 변경 X). Q1~Q5 결재 후 developer 진입. 보고서 `Dev/identity-mock-fallback-design-2026-05-08.md`
- [05-08] decisions: **PR3 layout 가드 mock flag 결정 — 옵션 a (channel key 환경변수 존재 시 자동 ON)** — `isIdentityGateEnabled() = !!process.env.NEXT_PUBLIC_PORTONE_IDENTITY_CHANNEL_KEY`. 가드 위치 = 페이지 server component 직접 분기. 가드 대상 = 3 페이지. 롤백 = 환경변수 제거. 보고서 `Dev/pr3-layout-guard-design-2026-05-08.md`
- [05-08] lessons: **BDR-current sync 운영 → 시안 역박제 갭 4건** — 5/8 BDR v2 zip 도착 → BDR-current sync v2.4 (Phase A.6 운영 정합) → spot check 결과 5/4~5/6 도메인 변경 4건 시안 미반영 (1) Login.jsx PasswordInput 보기 토글 0 / (2) TeamCreate.jsx jersey 입력 0 / (3) MyPage.jsx 단일 팀 카드 (다중 row 미반영) / (4) Team/TeamManage.jsx member-pending-badge 4종 0. 원인 = 시안 zip 작업이 5/1 v2.3 baseline 위에서 진행 / 5/4~5/8 도메인 commit 동시 누락. 재발 방지 = 새 zip README 베이스 vX.Y + 변경 범위 사전 확인 / spot check 매트릭스 + 갭 발견 시 (b) 역박제 후속 큐 즉시 등록. 사용자 결정 (c) 후속 큐 → scratchpad 3순위
- [05-05] conventions: **토큰 효율 5 룰 (응답 품질 영향 0)** — agent prompt = 산출물 형식 + 분량 + 시간 명시 / 보고서 long-form 1회 → 후속 diff-only / 결정 핵심 3건 + 옵션 분리 / 스크립트 작성 전 schema 1회 grep / scratchpad 즉시 압축. PM 직접 vs agent 위임 기준 강화 (SELECT 1~2회 = PM 직접). system-reminder TaskCreate 무시 룰
- [05-05] lessons: **토큰 비효율 5건 회고** — planner timeout (광범위 prompt) / 보고서 prefix 반복 / 결정 7~10건 묶음 / schema 미확인 재시도 / scratchpad 122줄 방치. 새 큰 작업 시작 시 5 룰 사전 점검 1회
- [05-05] errors: **마이페이지 등번호 입력 = `user.default_jersey_number` 만 저장 — team/대회 미동기화 단방향 함정** — `PATCH /api/web/profile` 가 갱신하는 것 = `user.default_jersey_number` (개인 선호값) 단 1곳. `team_members.jersey_number` / `tournament_team_players.jerseyNumber` 와 자동 sync 0건. 사용자 시점 = "등번호 등록 완료" 인지 / 시스템 시점 = 메모만 저장. errors.md `[2026-05-02]` 와 동일 패턴 (장기 큐 미해결). 이도균 사례 (userId=3352) — 마이페이지에서 70 입력 → 출전 명단 누락 → 운영자 수동 INSERT 로 fix. 단기 fix = 라벨 안내 추가 / 장기 fix = sync hook
- [05-05] lessons: **"등록했는데 반영 안 됨" 신고 = 도메인 모델 단방향 함정 1순위 의심** — jersey 도메인 단방향 단절: `user.default_jersey_number` (선호) → `team_members.jersey_number` (팀) → `tournament_team_players.jerseyNumber` (대회) **3개 테이블 각각 독립**. 진단 빠르게 = 사용자가 등록한 페이지 묻기 + 해당 페이지 갱신 컬럼 SELECT 검증 + cross-table mismatch 확인. 유사 잠재 함정 = position / is_elite / 활동지역 / 사진
- [05-05] architecture: **인증 흐름 단일 진입점 — `getAuthUser()` 헬퍼 + 4 layout 위임 + 쿠키 자동 cleanup** — `src/lib/auth/get-auth-user.ts` 신규 (JWT verify + DB user.status SELECT + status 분기 + cookies.delete 자동). 4 케이스 (`anonymous`/`active`/`withdrawn`/`missing`) 단일 응답. React.cache dedup → 4 layout 동시 호출해도 DB 1회. withdrawn/missing 시 cookies.delete 자동 → 사용자가 1회 진입으로 잘못된 쿠키 영구 제거 (5/5 사용자 신고 본질 해결). 4 layout (web/login/signup/profile) 모두 `auth.state` 분기 위임. me API 응답 401→200+state 통일 (옵션 A-3). commit `d8bba4a` + `61e9ab1` + `5fd1716` + `60e8468`
- [05-05] conventions: **인증 가드 추가 규칙 — `getAuthUser()` 단일 진입점 위임 (직접 `getWebSession`+`findUnique` 패턴 금지)** — 신규 보호 페이지 추가 시 직접 패턴 ❌ → `auth.state` 분기 ✅. 5 보장 (캡슐화/dedup/쿠키 cleanup/DB fail 안전/silent fail). 사용 위치 = server component layout / server action 만, route handler 는 기존 `withWebAuth` 유지. errors.md 회귀 패턴 영구 차단
- [05-05] errors: **layout 가드 status 검증 누락 — 탈퇴 회원 쿠키 잔존 시 /login 진입 차단** — `(web)/login/layout.tsx` 의 `if (session) redirect("/")` 가 JWT 만 검증 → 탈퇴 회원 쿠키 7일 잔존 시 /login 진입 즉시 / 로 보내져 시도 자체 불가. (web)/layout.tsx status 검증 추가했지만 login/layout 누락 = 회귀 패턴. fix: DB user.status 검증 추가. 회귀 방지 룰: 인증 가드 5개소 일괄 점검 + JWT 살아있음 ≠ 사용자 정상. commit `fa5bd90`. **B-PR1 후속 (`d8bba4a`) 으로 영구 차단**
- [05-05] decisions: **첫 로그인 onboarding 10단계 — 옵션 B 합의 (선호값 6종, DB 영향 0)** — 1 본인인증 / 2 활동환경 (17시도+게임유형 6종) / 3 출전정보 / 4 팀 / 5 사진 / 6 자기소개+SNS / 7 스타일 / 8 테마/표시 / 9 맞춤보기 (settings/feed PreferenceForm 흡수) / 10 알림. 6종 = `street-ball`/`pickup`/`guest`/`practice`/`regular`/`tournament`. 분기 룰 = `selected===["street-ball"]` 단독 → 3,4 선택 / 그 외 → 필수. PR1~5 분해 (~5.5d). game.game_type DB 마이그레이션 (옵션 A) 보류. 본 turn 합의만, 코드 변경 0
- [05-05] decisions: **admin/users 관리자 권한 12→3필드 축소 — PIPA 본인정정권** — 5/5 사용자 결정. 변경 가능 = nickname (부적절 신고) / bio (부적절 소개) / is_elite (대회 자격). 회수 9필드 = name/phone/birth_date(신원)/city/district(거주지)/height/weight(신체)/position/default_jersey_number(본인 선호). 사유 5자 이상 필수 + admin_logs warning + 자동 닉네임 초기화 버튼. commit `06d1376` + `8c95565` (auth fix 묶임)
- [05-05] decisions: **선수 배번 필수 정책 — role 분기** — `player` (default) → 배번 필수 (대회 신청 차단) / `coach`·`captain` → 선택. 차단 위치 (web): join API + admin players API. Flutter v1 = 변경 0 (zod 이미 required). admin/users 모달 = role 분기 표시 (player+누락 빨간 ⚠ / coach+누락 회색 —). commit `ef7e78e`
- [05-05] conventions: **관리자 강제 변경 액션 패턴 — 사유 필수 + admin_logs warning + 권한 최소** — PIPA 본인정정권 + 감사 추적. 사유 textarea minLength 5 (5자 미만 422). admin_logs severity warning + description 사유 박제. UI 톤 = warning border + "⚠ 긴급 변경" 라벨. 편의 액션 = 닉네임 자동 초기화 / 소개글 전체 삭제. is_elite_present hidden marker (체크 해제도 false 전송)
- [05-05] conventions: **role 별 배번 정책 — player 필수 / coach·captain 선택 (대회 신청 분기)** — 신청 차단 위치 (web) = join API + admin players API. UI 표시 (admin/users 모달) = player+누락 → "⚠ 배번 누락" 빨간 / coach·captain+누락 → "—" 회색 + 대회명 우측 role 라벨
<!-- 5/5 신규 2건 prepend (errors 마이페이지 등번호 / lessons 단방향 진단) → 5/5 lessons "다중 동시 commit" + "settings JSON" 절단 (본문 보존). 5/4 9건 + 5/3~3월 누적 30+건 절단 — 복원: git log -- .claude/knowledge/index.md / 본문 architecture.md / conventions.md / lessons.md / errors.md / decisions.md 모두 보존됨 -->
