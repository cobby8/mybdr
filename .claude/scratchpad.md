# 작업 스크래치패드

## 현재 작업
- **요청**: Auto Chain Master — 4단계 chain (v2.22 sync + Phase 2C/3C/4C = 25 PR)
- **상태**: ✅ **3단계 Phase 3C 완료 6/6** (3C-1~6 push) / 다음 = **4단계 Phase 4C 단체 8 PR (PR-4C-1 OU1)** — 사용자 결재 대기 (2C·3C 완료)
- **현재 담당**: pm
- **의뢰서**: `Dev/design/prompts/auto-chain-master-cli-prompt-2026-05-29.md`

## 🔑 Auto Chain 정책 (사용자 결재 2026-05-29)
- **데이터 통합 허용**: server 조회 + 새 web API route(`/api/web/*`) ✅. **금지(stop)**: `/api/v1`·DB schema(migrate/db push)·LOC>+2000·tsc실패·회귀6위반·디자인13룰위반. mock 금지(hide)
- **결재 default 자동**: sync A / 2C 결제 B(disabled) / 3C 모달 A / 4C Q1~Q4 lock
- **공유 컴포넌트(재사용)**: `src/components/bdr-v2/live-chip-row.tsx` (LiveChipRow) / `src/lib/services/live-chips.ts` (getLiveChips) — 홈·games 재사용 중, UA5도 재사용 가능

## Chain 진행 현황 (25 PR)
| 단계 | 내용 | 상태 |
|------|------|------|
| 1 | v2.22 sync (Phase 3 팀 + 4 단체) | ✅ `dee2445` |
| 2 | Phase 2C 경기 (10 PR) | ✅ **10/10 완료** |
| 3 | Phase 3C 팀 (6 PR) | ✅ **6/6 완료** |
| 4 | Phase 4C 단체 (8 PR) | 🔜 다음 (결재 대기) |

## 🔜 다음 재개점 — 4단계 Phase 4C (단체 8 PR / 의뢰서 §7)
| PR | 시안 | 운영 경로 | 핵심 |
|----|------|----------|------|
| **4C-1** | OU1 Organizations list 보강 | `/organizations` | (135줄) |
| 4C-2 | OU2 OrganizationDetail 보강 | `/organizations/[slug]` | (332줄) |
| 4C-3 | OU3 OrganizationApply 신규 | `/organizations/apply` | 5-step (291줄·Q4 lock) |
| 4C-4 | OU4 Series | `/series/*` | 위계 (269줄) |
| 4C-5 | OA1 AdminOrganizations 보강 | `/admin/organizations` | Site Operator (300줄) |
| 4C-6 | OO1 OrgAdmin | `/tournament-admin/organizations` (+/new) | (176줄) |
| 4C-7 | OO3 SeriesAdmin | `/tournament-admin/series/*` | 5 page (376줄·Q3 3-step) |
| 4C-8 | OO2 OrgAdminDetail | `/tournament-admin/organizations/[orgId]` | 6 sub-tab (443줄·Q2 순서) |

→ **Q1~Q4 lock 보존**: Q1 Series Operator badge=navy+silver / Q2 OO2 6탭 순서=basic→members→series→editions→officers→activity / Q3 OO3 3-step / Q4 OU3 5-step. 의존: BO1 OU3 form=OA1 모달 organizations 컬럼 / BO3 organization_members.role(owner/admin/member) / BO5 organizations.status(pending/approved/rejected/archived).

## 구현 기록 (developer) — Phase 2C
- **2C-1 UC2 Home** (`13feb36`): LiveChipRow 공유 컴포넌트 신규 + page.tsx 진행중 tournamentMatch 조회. Hero·본문·AppNav 무변경. 0건 hide
- **2C-2 UA1 Games** (`70118ea`): LIVE 띠 재사용 + 종료 MVP 라인(BG4) + game.ts final_mvp select / getLiveChips 공유 추출(홈 동작 보존)
- **2C-3 UC1 MyActivity** (`b796834`): 내 경기 상태정렬 + 내 매너 카드(평균+flag종류만, 개별건수 0) + route type=manner 분기
- **2C-4 UA5 Live** (`f4d8a2f`): `src/app/live/[id]/page.tsx` 헤더 라벨 강화만 박제(+약25줄). 시안 BG7 2요소 중 (1)Hero 라벨=박제: 트로피 아이콘(emoji_events)+round_name 결합("대회명 · 라운드", round null시 대회명만)·색 var(--color-info) (2)다음경기 영역=신규 추가 X — LiveMatchCardRail(same_day_matches)이 이미 동일 역할 구현됨 + mock 금지. 시안 픽업분기=운영 데이터에 없음(라이브=대회 매치 전용 tournament_id NOT NULL)→미적용. 데이터: 기존 조회값만 사용(새 쿼리 0). tsc0 회귀0(헤더 1곳 보강, 거대파일 미교체)
- **2C-5 UA3 CreateGame** (`d0385a2`): `/games/new` v2 단일폼 3번카드(conditions-section.tsx)에 신청정책(BG5)+게스트옵션(BG3) 박제. 변경파일 3: `_v2/conditions-section.tsx`(주 박제 +227) / `_v2/game-form.tsx`(ConditionsSection에 gameType·allowGuests·onAllowGuestsChange props 전달) / `_v2/advanced-section.tsx`(중복 게스트 토글 제거 -51, allowGuests state·allow_guests키 유지). **데이터 처리**: BG3 게스트허용 토글=**실연결**(games.allow_guests 컬럼 존재, 기존 advanced에 숨겨진 토글을 카드 표면으로 끌어올림, FormData 무변경) / BG5 자동승인 토글=**disabled 시각박제**(games 모델에 자동승인 컬럼 0, schema변경금지→ON 고정+'준비중' 배지+not-allowed) / BG3보조 최소경력·약관=**disabled 시각박제**(game_applications.experience_years/accepted_terms는 신청자 신청시점 입력값, 경기생성 시점 저장컬럼 0). 새 API·쿼리·schema 0. tsc0. LOC +242/-56
- **2C-6 UA4 GuestApply** (`cd7e9af`): `/games/[id]/guest-apply` 폼에 본인 프로필 prefill(BG3) 박제. 변경파일 2: `page.tsx`(본인 user 조회 server findUnique 1건 추가, **host 조회 보존** / skill_level·position·level·manner_score·total_games_participated select / skill→exp인덱스·position→GFC·skill→한글라벨 매핑 함수 + me prop 전달 +76) / `_components/guest-apply-form.tsx`(me prop 인터페이스 추가 / exp·pos 초기값 하드코딩"2"·"G"→prefill / 구력 select에 auto_awesome "내 프로필에서 자동" 힌트 + restart_alt "자동값으로 되돌리기" 버튼 / 미리보기 박스 mock 4값 실데이터 교체 +100/-8). **데이터 처리**: ⚠️시안은 skill 숫자(1~5)→년수 가정이나 운영 user.skill_level=**문자열**(beginner/intermediate/advanced/all/null)·운영폼은 이미 구력 select 5종 → skill_level→select인덱스 **실값 매핑**(beginner→0/intermediate→2/advanced→3, all·null→prefill생략 기본"2"유지+힌트숨김 mock금지) / position→G/F/C 한영표기 폭넓게 매핑(미매핑시 기본"G") / 미리보기박스: nickname·level·manner_score(null시"평가 전")·total_games_participated(0/null시 미표시) 전부 **실데이터**. 본인 user 조회=server component findUnique 1건(새 route 아님, 정책 허용). 새 API·schema 0. tsc0 EXIT0. LOC +168/-8
- **2C-7 UA2 GameDetail** (`390c22b`): `/games/[id]` 상세에 내 신청 진행 step indicator(BG1) 박제. 변경파일 2: `_v2/apply-step.tsx`(신규 249줄 — 3단계 [신청 완료→호스트 승인→참가 확정] 인디케이터 컴포넌트 + status별 분기 + KST 타임스탬프 포맷) / `page.tsx`(import 1줄 + ApplyPanel 위에 "내 신청 현황" 카드 +95줄, 기존 로직 0 변경). **status 매핑 일치 검증**: `game_applications.status`=**Int**(문자열 enum 아님, schema `@default(0)`) **0=대기/1=승인/2=거절** — `my-games/page.tsx`(L31~34)·`api/web/me/activity/route.ts`(2C-3 UC1, L97 주석 "0=대기/1=승인/2=거부")·`apply-panel.tsx`(L51)에서 **전부 동일 0/1/2** 확인 → 새 매핑 임의 생성 0, 단일 진실 그대로 사용. step 분기: status=0→신청완료done·호스트승인current(승인대기중)·참가확정pending / status=1→3단계 all done / status=2→신청완료done·신청거절(BDR Red)·참가확정pending. **본인 application 조회**: 기존 `myApplication`(page.tsx L140 `applications.find(a.user_id===session.sub)`, IDOR 안전) **그대로 재사용 — 새 조회 0**. listGameApplications는 findMany(select없음)라 created_at·approved_at·rejected_at 전체 컬럼 반환 → ApplyStep에 타임스탬프 전달 가능(추가 쿼리 0). **미신청 처리**: `myApplication`이 null이면 카드 자체 미렌더(mock 금지 준수, "아직 신청 안함" 가짜표시 0). 운영=단일칼럼(시안 sidebar 없음)이라 ApplyPanel 위 카드로 배치. 디자인13룰: var(--*)토큰만(ok/accent/bdr-red/ink-* + info폴백)·Material Symbols만·AppNav무변경·원형은 50%(정사각 예외룰). 새 API·route·schema·쿼리 0. tsc0 EXIT0. LOC +344(신규249+page95). 거대파일(이제 798줄)=카드1개 추가만, 기존 신청/상세/MVP/카페댓글 로직 회귀 0
- **2C-9 UD1 AdminGames** (`1985fde`): `/admin/games` 관리자 경기관리에 BG1 신청현황 박제 + BG5 출처컬럼 hide. 변경파일 **2**: `page.tsx`(games select에 `game_applications` 조회 추가 — id·status·is_guest·created_at·approved_at·rejected_at·신청자 users.nickname/email / serialized에 `applications[]`·`pendingCount` 추가 +26) / `admin-games-content.tsx`(BG1 박제 +144). **BG1 status 매핑 일치 검증**: `game_applications.status`=**Int 0=대기/1=승인/2=거절** — 2C-7 UA2 apply-step·2C-3 UC1 activity route·apply-panel·my-games·`api/web/games/[id]/applications/[appId]/route.ts`(approve→1/reject→2) **전부 동일 단일 진실** → 새 매핑 0. 박제 3요소: (1)상단 신청대기 배너(totalPending>0일 때만, 시안 atm-notify-bar→var(--color-primary-soft) 카드) (2)테이블 "신청 대기" 컬럼 신규(pendingCount>0 person_add 뱃지/0건 "—") (3)상세 모달 "신청 현황" 섹션(신청자 닉네임·게스트여부·신청/승인/거절 KST시간·status 0/1/2 admin-stat-pill[data-tone] warn/ok/err). **데이터 출처**: 기존 page.tsx는 game_applications 미조회 → **server findMany 조회 추가**(games select 내 nested, 새 route 아님, 정책 허용). **DB 실측**: game_applications 전체 **0건**(운영DB 신청 데이터 없음) — 구조는 실데이터 조회(0건이면 배너 숨김·컬럼 "—"·모달 "신청 내역 없음"), **mock 0**(가짜 신청자 생성 안 함, 실신청 생기면 자동 표시). **승인/거절 액션 미구현**: 승인/거절은 **호스트 권한**(applications PATCH route가 `game.organizer_id !== userId` 검증) → admin 화면은 **조회 전용** + "호스트가 처리" 안내문(권한충돌 회피, 가짜 액션버튼 0). **BG5 출처/최근변경자 컬럼=hide**: games 모델에 host/admin/system 출처 분류 컬럼 **없음**(cloned_from_id·template_id·is_recurring=생성단서지만 시안 "최근 변경자" 개념과 다름) / admin_logs game.status_change=**1건만**(super_admin 액션만, host/system 출처 추적 안 됨) → mock 금지 원칙상 **컬럼 자체 미추가**. 새 route·schema·`/api/v1` 0. tsc0 EXIT0. LOC +170(stop>+2000 미해당). 디자인13룰: var(--color-*)토큰만(primary/primary-soft/text-*/border/border-subtle/elevated)·Material Symbols(notifications_active·person_add·person)만·AppNav무변경·admin-stat-pill 기존패턴 재사용. 회귀: 기존 상태변경 모달폼·페이지네이션·탭·검색 무변경(섹션 추가만)
- **2C-10 UD2 AdminGameReports** (`9292fe6`): `/admin/game-reports` **기존 신고 큐 페이지를 3탭 구조로 확장** — 매너 통계(BG2)+30일 추세 추가. **운영 라우트=기존 활용**(신규 라우트 생성 0, 시안 박제 대상 `/admin/game-reports`와 정확히 일치). 변경파일 **2**: `src/app/api/web/admin/game-reports/stats/route.ts`(신규 +216, 통계 집계 route) / `src/app/(admin)/admin/game-reports/page.tsx`(3탭 구조 +570/-68). **탭 구성**: (1)매너 통계(기본탭,신규) (2)신고 큐(**기존 로직 100% 보존**, ReportQueueTab 컴포넌트로 분리만) (3)30일 추세(신규). **BG2 집계 = 2C-3 UC1 동일 원칙 검증**: 신규 stats route가 UC1 manner 집계(`api/web/me/activity` type=manner L114~148)의 **평균=rating합/건수 + flagKinds=Set distinct(건수 미집계)** 원칙을 그대로 따름 → **개별 건수(예 no_show 3회) 절대 노출 0**. top_flag는 "가장 흔한 종류" 판별용 내부 카운트만(건수 응답 제외) / report_rate=flags있는 평가 "비율"만 / 사용자별 flagKinds=Set 종류만(flagTotal은 하위판정 임계값용, 응답 매핑 제외) / distribution=평점 구간별 비율(%)만(개별 평가자·본문 0). **통계 종류**: 요약4(전체건수·평균·신고율·top_flag종류) / 평점분포 / 상위매너(평균4.5+&평가10+, 평균만) / 하위매너(평균3.0- or flags5+, 평균+flag종류만) / 30일추세(일별 평균·건수, KST날짜). **DB 실측**: game_player_ratings **0건** + game_reports **0건**(운영 매너평가 데이터 없음) → stats route가 0건이면 빈 통계 객체 반환 + 프론트 EmptyStats 빈상태("아직 매너 평가 데이터가 없습니다") **mock 0**(실데이터 쌓이면 자동 표시). **권한 가드**: 신규 stats route = `getWebSession()+isSuperAdmin()` 재검증(기존 game-reports route 동일 패턴) + layout.tsx admin 가드. **하위매너 경고/정지 버튼=disabled 시각박제**(동작은 후속작업, 가짜 액션 0). **status매핑**: game_reports.status=String(submitted/reviewed/dismissed) 기존 statusBadge 그대로(game_applications Int와 무관, 별 테이블). 디자인13룰: **운영 토큰 교정** — 시안 `--color-danger`→운영 정의 **`--color-error`**(11곳 일괄)·시안 `--color-surface-bright`(운영 미정의)→**`--color-card`**·var(--color-success/warning/info/primary/border/text-*)만·Material Symbols(insights·flag·trending_up·bar_chart·workspace_premium·report·verified_user·timeline·warning)만·AppNav무변경·정사각 50%(rounded-full 게이지바=시안 dist 바 동일). 새 `/api/v1`·schema·db push 0. tsc0 EXIT0. LOC +786/-68(stop>+2000 미해당). 신고 큐 기존 기능(필터탭·신고카드·플래그칩·코멘트·페이지네이션) 렌더 회귀 0(컴포넌트 분리+탭래핑만)
- **2C-8 UB1 GameResult** (`4681e51`): `/games/[id]` **종료(status===3) variant**에 결과 hero(BG4) 박제. 변경파일 **1**: `page.tsx`(기존 status===3 한 줄 MVP 띠 L328~419 → 시안 gr-hero 구조 결과 hero 카드로 격상, 신규 라우트·컴포넌트 0). **종료 status 값**: `status===3`(Int) — page.tsx·game.ts listGames(L142 날짜 지난 모집1/확정2도 3 override)·game-card·UA5 전부 동일. **MVP 소스 일치 검증**(BG4 양측): `finalMvp` = `game.final_mvp_user_id` (page.tsx L241~257 status===3일 때만 findUnique, **기존 로직 그대로**) = **2C-2(`70118ea`) game.ts listGames에 추가한 `final_mvp` 와 동일 schema 컬럼** = UA1 종료카드 MVP 라인·UA5 동일 소스 → **새 MVP 조회 로직 0**. getGame()은 findUnique(select없음)라 ended_at 등 games 전체 컬럼 반환(추가 쿼리 0). **mock 금지 — hide 처리**: ❌스코어(home:away)=games 점수 컬럼 0 → 미표시 / ❌우승·준우승 팀=픽업 games 팀/우승 개념 0(2C-2 동일 결론) → 미표시 / ❌Best 3 개인스탯=개인 득점 데이터 0 → 미표시 / ❌호스트 한마디=games 호스트 종료메시지 컬럼 0 → 미표시. **박제한 것**: 🏆"결과 발표" 태그 + ended_at 종료일시(있을 때만, KST `ko-KR` long date, null이면 라인 hide) + **MVP 큰 강조**(트로피 원형 메달 48px 정사각→50%라운딩 예외룰·var(--accent)·이름 22px 900·getDisplayName 실명우선·PlayerLink 유지 / finalMvp null이면 military_tech 아이콘+"아직 확정 전" fallback) + **평가 진행 상태**(reportCount/participantCount, 기존값 재사용). 카드1 참가자=기존 ParticipantsSlotBoard 유지 / 카드3 매너평가 진입=기존 /report 링크+진행상태 유지(중복 박제 0). 디자인13룰: var(--accent/surface-2/border/ink/ink-dim/ink-mute)토큰만·🏆 유니코드 이모지(game-card BG4 동일, 색상룰 무관)·military_tech Material Symbols·AppNav무변경·정사각 50% 예외. status!==3 분기(모집/확정/취소) **렌더 회귀 0**(띠 영역만 교체, hero·ribbon·summary·about·slotboard·applystep·applypanel·카페댓글·하단CTA 무변경). 새 API·route·schema·쿼리 0. tsc0 EXIT0. LOC +208/-66(stop>+2000 미해당). 거대파일(798줄)=status===3 띠 1곳 in-place 교체

### Phase 3C (팀)
- **3C-6 TA1+TA2 AdminTeams** (`0b61922`): `/admin/teams` 관리자 팀관리에 TA1 Hero status 통계 띠 + TA2 전적 조건부 hide 박제. **운영 = 시안 이미 충족 확인**: 운영이 이미 **모달 결재(시안 옵션 A) + v2 디자인** 완비 → 시안 5요소 중 **DB 뒷받침되는 2건만** 비파괴 박제(매너탭·활동도필터·멤버/이력탭·pending/suspend CTA = PM 승인대로 mock금지·회귀회피 미박제). 변경파일 **2**: `page.tsx`(TA1 — groupBy status 집계 1건 추가 + Hero 통계 띠 +약45) / `admin-teams-content.tsx`(TA2 — 전적 ModalInfoSection 조건부 hide +약7). **TA2 전적 hide**: 시안 BG6/BT6 = `Team.wins/losses/draws` — 상세 모달 "전적" 섹션을 `wins+losses+draws > 0`일 때만 렌더(전부 0이면 "0W0L0D" 노이즈 회피, 미래값 자동표시 mock금지). **TA1 status 통계 = 새 집계**: AdminPageHeader **그대로 두고 아래** admin-stat-pill 띠 추가. **데이터 소스 = 새 groupBy 1건**(`prisma.team.groupBy by:[status]`) — 목록은 `take:50`+검색`where`라 부정확 → 띠는 **where 없이 전체 96팀 기준** 분포(새 route 아님, server 조회 정책 허용). **DB 실측**(SELECT only, 운영영향0): active **84** / inactive **8** / merged **2** / dissolved **2** = 96팀 (4개 status 전부 실값 → mock0). status=null은 "active"로 합산(직렬화 기본값 일치). 톤: active=ok / inactive=err / merged=info / dissolved=mute. **0건 status는 hide**(현재 4개 다 ≥1이라 전부 표시, 0되면 자동 숨김). **미박제 4건(PM 승인)**: ❌매너 탭(팀 단위 매너 컬럼 없음) ❌활동도 필터(팀 last_activity_at 컬럼 없음) ❌멤버/이력 탭(mock) ❌pending/suspend CTA(운영 status active/inactive 워크플로우 유지, **server action 변경 0** — updateTeamStatusAction 무변경). **권한 가드**: (admin) layout.tsx super_admin 가드 기존 유지 — 새 route·조회권한 추가 0(server component 내 prisma 조회만). 디자인13룰: var(--color-text-primary/muted/success)+admin-stat-pill[data-tone] 기존패턴 재사용·Material Symbols만(신규 아이콘 추가 0)·AppNav무변경·정사각 미사용. 새 `/api/v1`·schema·db push 0. **tsc0 EXIT0**. LOC 약 +52(stop>+2000 미해당). **회귀 0**: AdminPageHeader·검색·상태탭·테이블·모달 결재폼(updateStatusAction)·STATUS_LABEL/TONE 전부 무변경(띠 1블록+전적 조건부 래핑+groupBy 조회 추가만). **결론: 운영이 이미 모달+v2라 DB 뒷받침 2건만 최소 박제**.
- **3C-5 TU4 TeamManageDetail** (`204f78e`): `/teams/[id]/manage` 헤더 배너 메타 라인에 **지역(city/district) 박제 1건**. 변경파일 **1**: `src/app/(web)/teams/[id]/manage/page.tsx`(LOC +13/-2). **운영 = 시안 초과 구현 확인**: 운영 2292줄 거대 페이지가 시안 6 sub-tab(멤버/가입신청BT1/변경신청BT2/권한위임BT4/유령BT3/매치BT5)을 **이미 전부 기능 구현 + 초과**(invite/settings/withdrawn 탭 추가). status·권한 소스 3C-3·3C-4와 일치: BT1 `team_join_requests.status`(String approved/rejected/pending) / BT2 `team_member_requests`(jersey_change/dormant/withdraw) / BT3 `last_activity_at`(DateTime?) / BT4 `TeamOfficerPermissions` → **기존 기능 재구성 0(회귀 0)**. **박제 = 시안 TeamMiniHero 4메타(멤버수·매너·승패·지역) 중 지역만**: 멤버수=운영 이미 있음(`members.length`) / 매너 평균=팀단위 매너 컬럼 **없음** hide / 승패(wins/losses)=96팀 전부 0(3C-4 실측) hide / **지역=teamData.city/district 실값 추가**(place Material Symbol + `[city,district].filter(Boolean).join(" ")`, 둘 다 없으면 라인 미표시 mock금지). **데이터 소스**: `teamData`(TeamEditData, settings/matches 탭 진입 시 lazy load — page L600~606)의 **기존 city/district 필드 재사용, 새 쿼리·route·schema 0**(roster 첫 진입엔 teamData null이라 팀명도 "팀 관리"로 보이는 기존 한계 그대로, 회귀 0). **토큰 교정 불필요**: 운영 헤더 배너·탭(border-bottom 라인+카운트 뱃지) 모두 정상 var(--color-*) 사용, 13룰 위반 0 → 시안 active 채움 배경 ↔ 운영 강조선 차이는 v2 확립 패턴이라 재구성 안 함. 시안 BT1~BT5 mono 디버그 배지=노이즈 회피 미박제. 디자인13룰: var(--color-*)토큰만(기존 메타 라인 opacity-85·font-mono 동일 스타일)·Material Symbols(place)만·AppNav무변경. 새 `/api/v1`·schema·db push 0. **tsc0 EXIT0**. LOC +13/-2(stop>+2000 미해당, 거대파일 메타 라인 1곳 in-place 교체). **운영이 시안 초과 구현이라 최소 박제** — 헤더 지역 메타 1건 외 추가 박제 불필요(무리한 재구성 ❌).
- **3C-4 TU2 TeamDetail 보강** (`42e2cc6`): `/teams/[id]` 사이드에 BT4 내권한 + BT7 운영액션 카드 박제. **운영 현황**: 페이지 이미 v2 박제 상당(Hero·4탭·사이드카드2·가입버튼·매치모달) → 시안 5요소(TU2-A~E) 중 **운영에 없던 BT4/BT7 2카드만** 추가. 변경파일 **3**: `_components_v2/team-ops-card-v2.tsx`(**신규 271줄** — TeamOpsCardV2, BT7 운영액션 + BT4 내권한 2카드) / `_components_v2/team-side-card-v2.tsx`(import + props 4개 추가 + 최근폼카드 다음에 TeamOpsCardV2 배치 +22) / `page.tsx`(myRole/isCaptain/grantedPermissions SSR 사전계산 +32). **BT6 전적 = hide(운영 기존 집계 유지)**: 의뢰서 지시대로 `Team.wins/losses/draws` **미사용**(DB실측 96팀 전부 0) — 운영은 이미 `tournamentMatch officialMatchWhere`로 wins/losses **실집계**(Hero/Overview, page.tsx L116~134) → **기존 집계 회귀 0, 새 stats 카드 추가 안 함**(StatsTabV2 "준비중" 그대로). 시안 TU2-D(팀MVP·우승이력)=개인스탯/우승순위 컬럼 0 → **미박제 mock금지**. **BT4 권한 = TeamOfficerPermissions 실값**: `permissions.ts` 6키(jerseyChangeApprove/dormantApprove/withdrawApprove/transferApprove/ghostClassify/forceChange) 라벨매핑 / **captain=자동 전체권한**(룰#1, isCaptain이면 6키 전부 보유로 표시 — 가짜아님 실제권한) / 위임자=`teamOfficerPermissions.findFirst(revokedAt:null) permissions JSON` 실값(captain은 조회생략, 일반멤버/비멤버 조회생략 — 불필요SELECT회피) / **DB실측 위임 0건**이나 captain 본인은 실표시 / 일반멤버=권한6키 나열대신 "경기·대회 참가 가능/운영권한 없음" info 안내(노이즈 회피). **BT7 운영액션 = 기존 동작 라우트만**: (1)이 팀 멤버 경기 모으기→`/games?host_team_id=`(searchParams 자유통과 확인) (2)팀 관리→`/teams/[id]/manage`(존재) / ⚠시안 "/tournaments?team_id=" = tournaments page가 **searchParams 미사용**(page.tsx 확인) → 효과없는 가짜동선 **제외**(canManage일 때만 카드 노출). **데이터 소스**: BT4 myRole=본조회 teamMembers **재활용**(추가쿼리0, captainId 본인이면 'captain' 보정) / 위임권한=server findFirst 1건(captain아님&canManage일 때만, 새route아님 정책허용) / BT7=링크만(쿼리0). **TU2-A sticky LIVE 띠=hide**: 라이브=tournament_id NOT NULL 전용(2C-4 결론), 팀단위 진행중경기 개념 없음 mock금지. **IDOR 안전**: 권한조회 where에 본인 userId 필수, 기존 canManage/isMember 판정 패턴 그대로. **mock 금지 hide 처리**: 비멤버→TeamOpsCardV2 내부 null반환(가짜카드0) / 운영권한 없는 멤버→6키 block 나열 대신 안내. 디자인13룰: var(--accent/cafe-blue/ok/ink-*/bg-alt/radius-chip/ff-mono)토큰만(--err 미정의 확인→회피)·Material Symbols(tune·sports_basketball·settings·chevron_right·verified·check_circle·block·info)만·AppNav무변경·정사각 미사용(라운딩=radius-chip 사각). 새 `/api/v1`·schema·db push 0. **tsc0 EXIT0**. LOC 신규271+수정54=약+325(stop>+2000 미해당). **기존 회귀 0**: Hero·4탭·OverviewTab·RosterTab·RecentTab·StatsTab·최근폼·가입버튼·매치모달·연락카드·휴면복구·활동추적·팔로우 전부 무변경(사이드카드에 카드2장 추가 + page SSR 권한계산 추가만)
- **3C-3 TU5 MyActivity "내 팀"** (`4eeb260`): `/profile/activity` 에 시안 v2.21 "내 팀" 섹션 박제. 변경파일 **2**: `src/app/api/web/me/activity/route.ts`(**type=myteams 분기 신규** +124, 기존 분기 무변경) / `src/app/(web)/profile/activity/page.tsx`(MyTeamItem 타입+state+fetch+MyTeamsSection/MyTeamRow/PendingChip/TeamLogo 컴포넌트+헤더 "내 팀 K"카운트 +약340). **"내 팀" = 내가 active 멤버인 팀**(`team_members where userId=me, status=active`) — 기존 "teams" 탭(가입 신청 이력 team_join_requests)과 **다른 개념** → 탭/매너카드 위 **단독 섹션**으로 배치(2C-3 섹션 무변경). **BT1/BT2/BT3 실제 타입 확정**(TU4 3C-5 일치 필수): BT1 `team_join_requests.status`=**String** approved(214)/rejected(18)/pending(3) (game_applications Int 0/1/2와 무관!) / BT2 `team_member_requests.status`=**String** approved(3)/pending(3), request_type=jersey_change/dormant/withdraw / BT3 `team_members.last_activity_at`=**DateTime?**(NULL허용→hide, 활동시 갱신, 3개월기준). **운영진 판정**(teams/[id]/route.ts 동일): `team.captainId===userId` 1순위 + role∈[captain,vice,manager] fallback(role 'director' 등 비표준 섞임→captainId 우선, captainId 본인이면 role 'captain' 보정). **박제 요소**: 팀명·지역·멤버수(members_count)·내 role배지·마지막활동(NULL hide)=**실값** / pending 칩 3종(BT1가입·BT2변경·BT5매치=team_match_requests to_team) = **운영진 팀만 실 count**(groupBy 묶음조회 N+1회피, 멤버 팀=0) / 팀로고=logoUrl 없으면 색상 그라디언트+이니셜(정사각50% 예외룰) / 헤더 "내 대회 N·내 경기 M·내 팀 K·평균 매너". **mock 금지 hide**: ❌**팀 매너 평균**(시안 manner_avg) — 운영 DB 팀단위 매너 집계 컬럼 **없음** → 응답서 제외·미표시. **DB 실측 검증**(SELECT only, 운영영향0, 임시스크립트 정리완료): user1=BDR(member,활동5/8)+스티즈남양주(captain byId,운영진) / ATLAS captain(3366) active멤버&pendingJoin=2 → "가입 2" 칩 정상노출 확인 / pending 가입3·변경3 DB존재 / last_activity NULL·실값 둘다 정상. **빈상태**: 소속 0팀→"팀 둘러보기" CTA(mock 가짜팀0). 디자인13룰: var(--accent/cafe-blue/ink-*/border/bg-card/ok/warn/danger)토큰만(--bg-soft 미정의→color-mix 직접)·Material Symbols(groups·place·schedule·how_to_reg·edit_note·handshake·arrow_forward)만·AppNav무변경·정사각 50% 예외(팀로고 원형)·color-mix 칩. 새 `/api/v1`·schema·db push 0. **tsc0 EXIT0**. LOC 약 +464(stop>+2000 미해당). **2C-3 회귀 0**: 경기 상태정렬·내 매너 카드·탭(games/tournaments/teams)·필터칩·counters·EmptyState·GameCard/TeamCard/MannerCard 전부 무변경(신규 섹션·타입·state·fetch 추가만, 매너카드 위에 1줄 삽입)
- **3C-2 TU1 Teams 보강** (`2ab8a6e`): `/teams` 목록/탐색 페이지에 시안 BT2 일부 박제. 변경파일 **1**: `src/app/(web)/teams/_components/teams-content-v2.tsx`(LOC 약 +75). **박제 2요소(실값)**: (1)**헤더 메타 "모집 중 M팀"** — 기존 `teams[]` 응답의 `accepting_members` 플래그 client 집계(`recruitingCount`), var(--ok) 강조, `!loading && teams.length>0`일 때만 노출(0팀 깜빡임 방지) (2)**모집 상태 필터 칩 "전체/모집 중"** — 정렬 칩 아래 신규, `recruitOnly` client state로 `teams.filter(accepting_members)` 적용(URL param 아님 — 검색q/지역city/정렬sort는 서버 처리지만 모집상태는 응답에 플래그 이미 있어 client 즉시필터가 단순). 활성 톤=전체 var(--ink)/모집중 var(--ok). **데이터 소스**: 둘 다 기존 `/api/web/teams` 응답 필드만 사용 — **새 쿼리·route·server조회 0**. paginatedTeams·hasMore·remaining·빈상태를 `filteredTeams` 기준으로 교체(모집중 0건 시 빈상태 정확히 표시, `recruitOnly`도 "조건에 맞는 팀 없음" 분기에 포함). recruitOnly 토글 시 currentPage=1 리셋(기존 URL필터와 동일 정책). **mock 금지 → 미박제 3요소**: ❌**팀 전적 표시**(BT6) — `Team.wins/losses/draws`(Int? @default0) 컬럼 **존재하나 운영 45팀 전부 0**(DB 실측) → 카드·필터에 전적 미표시. **BT6 소스 = Team.wins/losses/draws 컬럼 확정**(TU2 3C-4·TA1 3C-6도 동일 컬럼 사용해야 함, 임의 집계 금지). ❌**매너 ★ 필터**(시안 BT2 "매너 ★4.5+/3.5+") — 팀 단위 매너점수 컬럼이 schema에 **없음** → 필터 칩 미박제(가짜 ★ 버튼 금지). ❌**추천 "내 지역 활성 팀" 섹션**(시안 BT2) — 시안 조건 `last_activity_at<30일` 컬럼 **없음** + `/teams`는 **공개 페이지(인증 불필요, client fetch)**라 로그인 사용자 city를 알 수 없음 → 추천 로직 성립 불가 → 섹션 자체 미박제(mock 금지). **DB 실측**: active+public 45팀 / 모집중 45 / 전적0아님 0 / 대회참가0아님 0 / 도시분포 서울3·경기1 등(대부분 city=null). 디자인13룰: var(--ok/ink/bg/ink-mute/ink-dim)토큰만(--ok=#1CA05E 운영 정의 확인)·Material Symbols(person_add)만·AppNav무변경·정사각룰 미사용·기존 .btn--sm 칩 패턴 재사용·page-hero__sub 미정의 className 제거(인라인스타일만). 새 `/api/v1`·schema·db push 0. tsc0 EXIT0. LOC 약 +75(stop>+2000 미해당). **회귀 0**: 기존 검색(q)·지역(city)·정렬(sort)·페이지네이션(더보기)·카드(TeamCardV2)·빈상태 CTA(/teams/new) 로직 무변경(메타1줄+상태칩블록 추가 + slice 소스를 teams→filteredTeams 치환만)
- **3C-1 TU3 TeamManage hub** (`50ee237`): `/teams/manage` 팀 관리 hub 시각 박제. 변경파일 **1**: `src/app/(web)/teams/manage/page.tsx`(LOC +169/-50). **운영 존재**: 페이지 이미 존재(357줄, 0/1/N 분기 로직 완비) → 시안은 그 위 시각 박제. **박제 3요소**: (1)N≥2 그리드 화면 상단 **tu3-hero 카드** 신규 — 큰 카운트 숫자(activeTeams.length 실값, var(--accent) 48px display)+제목 "운영 중인 팀 N개"+우측 CTA(팀 둘러보기 ghost / +팀 등록 accent, 44px). 기존 단순 header(eyebrow+h1+p)를 hero 카드로 격상 (2)**N=0 빈상태 강화** — 구형 --color-* 토큰→v2(--ink/--ink-mute) 교정+카피 "팀을 만들거나, 기존 팀에 가입해서…"(시안 카피)+CTA 2개 44px (3)**하단 "참여 중 (멤버 only)" 섹션 신규** — 관리 권한 없이 멤버로만 참여 중인 팀 안내(info 아이콘+팀명·지역·"팀 상세 →" 링크). **N=1 분기**: 시안은 안내카드지만 운영은 **즉시 redirect 유지**(시안 안내카드 logic상 도달 불가 → 박제 안 함, 운영 redirect 보존). **데이터 소스**: Hero/그리드=기존 `memberships`+`captainTeams` 조회 **그대로 재사용**(새 쿼리·route 0) / 멤버 only 섹션=**server findMany 1건 추가**(teamMember where status=active & role notIn[captain/vice/manager], team select id/name/city/district/status — 정책 허용 server 조회, 새 route 아님). **dedup**: 운영진 팀 id Set으로 멤버 only에서 중복 제외 + 해산팀(status=dissolved) 제외. **mock 금지**: 멤버 only 섹션은 실데이터 0건이면 섹션 자체 hide(가짜 "강남BC" 박제 안 함). **schema 무변경**: teamMember role/status/userId·team captainId/members_count/primaryColor 전부 기존 컬럼. 디자인13룰: 시안 토큰이 운영 globals.css에 **동일 존재**(--ink/--ink-mute/--ink-dim/--bg-elev/--border/--accent/--cafe-blue-deep/--ff-display/--ff-mono) → 교정 거의 없음(--r-sm→--radius-chip / .btn--touch→minHeight:44px 인라인)·Material Symbols(groups·info·chevron_right)만·AppNav무변경·정사각 50% 미사용(이니셜 박스=radius 8px 사각)·720px·44px 모바일룰. 새 `/api/v1`·schema·db push 0. tsc0 EXIT0. LOC +169/-50(stop>+2000 미해당). 기존 0/1/N 분기 로직·captain_id 보강·redirect 회귀 0(시각 레이어만 교체+멤버only 조회 추가)

## 진행 현황 (Phase 1C — 완료)
- ✅ Phase 1C 15/16 박제+머지 (PR #650~#653) / PA3 SKIP 보류 (decisions.md) / subin=dev=main 정합

## 수정 요청
| 요청자 | 대상 | 문제 | 상태 |
|--------|------|------|------|

## 작업 로그 (최근 10건)
| 날짜 | 작업 | 결과 |
|------|------|------|
| 2026-05-29 | **Phase 3C 완료 (6/6)** PR-3C-6 박제 (TA1+TA2 AdminTeams) | ✅ `0b61922` push / 운영 이미 모달(옵션A)+v2 → DB뒷받침 2건만 / TA1 status통계띠(groupBy 96팀 active84/inactive8/merged2/dissolved2) / TA2 전적 조건부hide(96팀0) / 매너탭·활동도·pendingCTA=mock·워크플로우 미박제 / tsc0 LOC+54/-9 |
| 2026-05-29 | Phase 3C PR-3C-5 박제 (TU4 TeamManageDetail) | ✅ `204f78e` push / 운영 2292줄=시안 6 sub-tab 이미 초과구현 → 헤더 지역(city/district) 메타 1건만 비파괴 박제 / status·권한(BT1/2/3/4) 3C-3·3C-4 일치·기능 재구성0 / 전적·매너 hide / tsc0 LOC+13/-2 |
| 2026-05-29 | Phase 3C PR-3C-4 박제 (TU2 TeamDetail) | ✅ `42e2cc6` push / 팀상세 사이드 BT4 내권한+BT7 운영액션 카드(신규 TeamOpsCardV2) / BT4=TeamOfficerPermissions 실값(captain자동전체·위임자JSON) / BT6 전적=96팀0 hide(기존 tournamentMatch집계유지) / BT7=동작라우트만(tournaments?team_id 제외) / IDOR안전 / 기존Hero·4탭·모달 회귀0 / tsc0 LOC+325 |
| 2026-05-29 | Phase 3C PR-3C-3 박제 (TU5 MyActivity 내팀) | ✅ `4eeb260` push / profile/activity "내 팀" 섹션(매너카드 위, 2C-3 회귀0) / route type=myteams 신규(세션본인 IDOR안전) / 팀명·지역·멤버수·role·활동 실값+pending칩(BT1가입·BT2변경·매치) 운영진팀 groupBy / BT1·BT2=String·BT3=DateTime? / 팀매너=컬럼없음 hide / tsc0 LOC+529 |
| 2026-05-29 | Phase 3C PR-3C-2 박제 (TU1 Teams 보강) | ✅ `2ab8a6e` push / /teams 헤더 "모집중 M팀"+모집상태 필터칩 / 기존 응답 client집계·필터(새쿼리0) / 미박제:전적(45팀0)·매너필터(컬럼없음)·추천(데이터없음) mock금지 / BT6=Team.wins/losses/draws 확정 / tsc0 LOC+90/-6 |
| 2026-05-29 | Phase 3C PR-3C-1 박제 (TU3 TeamManage hub) | ✅ `50ee237` push / /teams/manage hub 시각박제(Hero카운트+빈상태강화+멤버only섹션) / Hero·그리드=기존 memberships·captainTeams 재사용 / 멤버only=teamMember server조회1건(비운영진·dedup·해산제외) / 0/1/N분기·redirect 회귀0 / 멤버only0건 hide / 토큰교정 / tsc0 LOC+169/-50 |
| 2026-05-29 | **Phase 2C 완료 (10/10)** PR-2C-10 박제 (UD2 AdminGameReports) | ✅ `9292fe6` push / `/admin/game-reports` 3탭 확장(매너통계+신고큐보존+30일추세) / 신규 stats route(super_admin가드+403) / 집계=UC1 동일(평균+flag종류, 개별건수0) / DB 0건→빈상태 mock0 / 토큰교정 danger→error·surface-bright→card / tsc0 LOC+786/-68 |
| 2026-05-29 | Phase 2C PR-2C-9 박제 (UD1 AdminGames) | ✅ `1985fde` push / 관리자 경기관리 BG1 신청현황(배너+신청대기 컬럼+모달 섹션) / status=Int0/1/2 통일 / game_applications nested 조회 추가(새route0) / DB 0건→빈상태 mock0 / 승인거절=호스트권한이라 조회전용 / BG5 출처컬럼=컬럼없음 hide / tsc0 LOC+170 |
| 2026-05-29 | Phase 2C PR-2C-8 박제 (UB1 GameResult) | ✅ `4681e51` push / 종료(status3) MVP 띠→결과 hero(BG4) / MVP=final_mvp_user_id 재사용(2C-2·UA5 동일소스, 새조회0) / 박제:발표태그·종료일시·MVP강조·평가진행 / hide:스코어·우승팀·개인스탯·호스트한마디(데이터없음 mock금지) / status!==3 회귀0 새route·쿼리0 tsc0 LOC+208/-66 |
| 2026-05-29 | Phase 2C PR-2C-7 박제 (UA2 GameDetail) | ✅ `390c22b` push / 신청 step indicator(BG1) 3단계[신청완료→호스트승인→참가확정] / 신규 _v2/apply-step.tsx + page.tsx 카드 / status매핑=Int0/1/2(UC1·apply-panel 동일, 새매핑0) / myApplication 재사용(IDOR안전,새쿼리0) / 미신청 미렌더(mock금지) / tsc0 LOC+344 |
