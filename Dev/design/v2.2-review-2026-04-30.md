# BDR v2.2 디자인 작업물 검토 — 2026-04-30

> **상태**: active
> **갱신 주기**: 1회성 (v2.2 인수 검토)
> **상위 문서**: [README.md](./README.md)
> **작성 사유**: 사용자 — "v2.2 다운받음, 검토 요청"
> **검토 대상**: `Dev/design/BDR v2.2/`
> **의뢰서**: [claude-design-upgrade-prompt-2026-04-30.md](./claude-design-upgrade-prompt-2026-04-30.md)

---

## 0. 종합 평가

### 🎯 결과: **A- (전반적 우수, 2개 결정 위반 픽스 필요)**

**의뢰 11 페이지 전부 작성 + 매트릭스 / 회귀 픽스 / 모바일 G-10 모두 우수**. 단 사용자 결정 §2 위반 2건 + 폴더 구조 중첩 1건이 박제 전 정정 필요.

### 한 줄 요약

| 영역 | 평가 |
|------|------|
| 의뢰 11 페이지 작성 | ✅ 11/11 모두 (PostEdit, GameEdit, LiveResult, ProfileComplete, ProfileCompletePreferences, ProfileGrowth, ProfileBookings, VenueDetail, ProfileWeeklyReport, HelpGlossary, RefereeInfo) |
| 검수 매트릭스 §8-1 | ✅ 모든 신규 시안 JSDoc 헤더에 첨부 |
| 진입·복귀 명세 §6-2 | ✅ 모든 신규 시안에 진입/복귀/Edge 명시 |
| 회귀 픽스 LiveResult §5-3 | ✅ 색·폰트만 v2 + 옛 레이아웃 복원 정확 |
| 모바일 G-10 §5-4 | ✅ 가로 스크롤 + G-9 보호 (`.scoreboard / [data-keep-cols]`) 완벽 |
| README.md §9 | ✅ 변경 요약 + 폴더 구조 + 회귀 매트릭스 가이드 |
| MyBDR.html 라우트 등록 | ✅ 11/11 모두 |
| _mobile_audit.html 그룹 | ✅ 3 그룹 신규 (P0·필수 / P1·내 프로필 / P2·P3) |
| **사용자 결정 §2-2 가짜링크 4건** | 🚨 **위반 — 가짜링크 4건 다시 추가** |
| **사용자 결정 §2-6 글로벌 카피** | 🚨 **위반 — "서울 3x3" 표현 5 파일** |
| 폴더 구조 | ⚠️ 이중 중첩 `BDR v2.2/Dev/design/BDR v2.2/` |
| 보너스 — 모바일 오딧 M1~M9 | ✨ 의뢰 외 추가 픽스 |

---

## 1. ✅ 우수한 점

### 1-1. 의뢰 11 페이지 전부 작성 (D등급 + 회귀 픽스)

| # | 시안 파일 | 의뢰 우선도 | 작성 |
|---|---------|-----------|-----|
| 1 | `PostEdit.jsx` (`/community/[id]/edit`) | P0 | ✅ |
| 2 | `GameEdit.jsx` (`/games/[id]/edit`) | P0 | ✅ |
| 3 | `LiveResult.jsx` (`/live/[id]` finished 회귀) | P0 | ✅ |
| 4 | `ProfileComplete.jsx` (`/profile/complete`) | P0 | ✅ |
| 5 | `ProfileGrowth.jsx` (`/profile/growth`) | P1 | ✅ |
| 6 | `ProfileBookings.jsx` (`/profile/bookings`) | P1 | ✅ |
| 7 | `VenueDetail.jsx` (`/venues/[slug]`) | P1 | ✅ |
| 8 | `ProfileWeeklyReport.jsx` (`/profile/weekly-report`) | P1 | ✅ |
| 9 | `ProfileCompletePreferences.jsx` (`/profile/complete/preferences`) | P2 | ✅ |
| 10 | `HelpGlossary.jsx` (`/help/glossary` redirect 박제) | P2 | ✅ |
| 11 | `RefereeInfo.jsx` (`/referee` 옵션 B SEO 안내) | P3 | ✅ |

**총 11/11 (100%)**. RefereeInfo 는 의뢰서 §4-2 옵션 B (SEO 페이지) 채택. 정책 결정 함께 처리.

### 1-2. 검수 매트릭스 + 진입·복귀 모범 사례 (PostEdit.jsx)

```jsx
/**
 * PostEdit — /community/[id]/edit (D등급 P0-1 신규)
 *
 * Why: 본인 게시글 수정 (제목 / 본문 / 카테고리 / 첨부 사진)
 * Pattern: PostWrite.jsx 와 동일 + 기존 데이터 prefill + "취소"·"수정 완료"
 *
 * 진입: /community/[id] 본인 글 점-3개 dropdown "수정"
 * 복귀: 저장 → /community/[id] / 취소 → /community/[id]
 * 에러: 권한 없음 (작성자 != 본인) → 권한 안내 + 상세 복귀
 *
 * 회귀 검수 매트릭스:
 *   기능              | 옛 페이지 | 시안 v2.2          | 진입점     | 모바일
 *   본문 수정         | -        | ✅ textarea prefill | dropdown | 1열
 *   사진 추가/삭제    | -        | ✅ 첨부 리스트       | dropdown | 가로 hscroll
 *   비밀글 토글       | -        | ✅ checkbox         | dropdown | OK
 *   권한 체크         | -        | ✅ noPermission view | -        | OK
 */
```

→ 의뢰서 §6-2 + §8-1 정확히 따름. **GameEdit, LiveResult, ProfileComplete, ProfileGrowth, ProfileBookings, VenueDetail, ProfileWeeklyReport, ProfileCompletePreferences 모두 같은 구조**.

### 1-3. Edge case + 권한 처리 표준 (GameEdit.jsx)

```jsx
function GameEdit({ setRoute, isHost = true, gameStatus = 'open', applicantCount = 0 }) {
  const lockedByApplicants = applicantCount > 0;
  const finishedView = gameStatus === 'finished';
  
  if (!isHost) {
    return /* §7-5 표준 권한 없음 view */
  }
```

→ 의뢰서 §5-2-2 의 "신청자 0 = 모든 필드, 신청자 ≥ 1 = 인원·회비·날짜 잠금, 종료 = noEdit" Edge case 정확히 반영.

### 1-4. 회귀 픽스 LiveResult.jsx 의뢰 룰 정확

JSDoc 헤더:
```
시안 룰: 색/폰트만 v2 유지 + 레이아웃·기능 옛 디자인 복원
옛 레이아웃 핵심: 큰 스코어보드 / 쿼터별 점수 / MVP / 평가 진입점 / 기록 보기
진입: /live/[id] (경기 종료 자동) / 알림 "경기 결과 보기" 클릭
복귀: /games/[id] / /games/my-games / /live (다른 경기 보기)
```

→ 의뢰서 §5-3 그대로.

### 1-5. 모바일 G-10 + G-9 보호 모범 사례

`responsive.css` 마지막 블록:

```css
/* G-9 보호: scoreboard / data-keep-cols 는 모바일에서도 다열 유지
   (사용자 의도가 stack이 아닌 가로 비교) */
.page .scoreboard,
.page [data-keep-cols] {
  grid-template-columns: 1fr auto 1fr !important;
  text-align: initial !important;
  gap: 12px !important;
}

/* G-10: 큰 N열(7~14) repeat() grid → 가로 스크롤 보존 */
.page div[style*="grid-template-columns: repeat(7"]:not([data-keep-cols]),
.page div[style*="grid-template-columns: repeat(8"]:not([data-keep-cols]),
.page div[style*="grid-template-columns: repeat(9"]:not([data-keep-cols]),
.page div[style*="grid-template-columns: repeat(10"]:not([data-keep-cols]),
.page div[style*="grid-template-columns: repeat(11"]:not([data-keep-cols]),
.page div[style*="grid-template-columns: repeat(12"]:not([data-keep-cols]),
.page div[style*="grid-template-columns: repeat(13"]:not([data-keep-cols]),
.page div[style*="grid-template-columns: repeat(14"]:not([data-keep-cols]) {
  overflow-x: auto !important;
  -webkit-overflow-scrolling: touch;
}

/* G-10b: signup positions (5열) → 3열 분기 */
.page div[style*="grid-template-columns: repeat(5, 1fr)"]:not([data-keep-cols])... {
  grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
}

/* G-10c: 6열 ranking 표 → 가로 스크롤 */
.page div[style*="grid-template-columns: repeat(6, 1fr)"]... {
  overflow-x: auto !important;
}

/* G-10d: 비교 표 (safety/pricing) — 첫 열 고정 + 가로 스크롤 */
.page .compare-table[data-cols] {
  overflow-x: auto !important;
}
.page .compare-table[data-cols] > div { min-width: 720px; }
```

→ 의뢰서 §5-4 + §6 보호 가이드 정확. **`:not([data-keep-cols])` 보호 셀렉터** 까지 빠짐없이 적용.

### 1-6. 보너스 — 모바일 오딧 M1~M9 (의뢰 외 추가)

README 에 명시된 9건 추가 픽스:

| ID | 영역 | 픽스 |
|---|---|---|
| M1 | gameResult | 가로 스크롤 페이드 + 스와이프 hint |
| M2 | live | 비디오 aspect-ratio 16/9 + 스코어버그 클램프 |
| M3 | gallery | 라이트박스 모달 모바일 stack |
| M4 | stats | 슈팅 차트 aspect-ratio |
| M5 | calendar | 셀 탭 → 일간 모달 |
| M6 | postWrite | 툴바 그룹/스크롤 |
| M7 | profile | 모바일 hero 변환 |
| M8 | gameResult | 쿼터별 스코어 컬럼 축소 |
| M9 | global | 가로 스크롤 우측 페이드 마스크 (G-9, G-10 가드) |

→ 의뢰 외에 사이트 모바일 회귀 9건 추가 픽스. **이건 사용자 임팩트 큼**.

---

## 2. 🚨 사용자 결정 위반 (박제 전 정정 필수)

### 2-1. 🚨 위반 1 — `components.jsx` moreGroups 가짜링크 4건 다시 추가

> 사용자 결정 §2-2: "DB/API 미지원 가짜 링크 영구 제거 — guestApps / gameResult / gameReport / referee"

**`Dev/design/BDR v2.2/Dev/design/BDR v2.2/components.jsx`**:

| 라인 | 항목 | 문제 |
|------|------|------|
| 120 | `{ id:'guestApps', label:'게스트 지원', icon:'🎟️' }` | DB 미존재. 사용자가 영구 제거한 항목 다시 추가 |
| 130 | `{ id:'gameResult', label:'경기 결과', icon:'📊' }` | 가짜 링크 (실제로는 /games/my-games 로 fallback). 사용자가 제거한 항목 |
| 131 | `{ id:'gameReport', label:'경기 신고·평가', icon:'🚩' }` | 같음. 종료된 경기 카드의 CTA 정책으로 전환됨 |
| 145 | `{ id:'referee', label:'심판 센터', icon:'🦓' }` | RefereeInfo 옵션 B 채택의 결과 — **조건부 정당화 가능** |

**판정**:
- guestApps / gameResult / gameReport (3건): **위반 — 박제 시 제외 필수**
- referee: 옵션 B 의 RefereeInfo (`/referee-info` 또는 별도 라우트) 와 매핑 시 정당. 단 사이트의 `/referee` 가 **심판 플랫폼**이므로 충돌 — **별도 라우트명 사용 권장** (예: `refereeInfo`)

**박제 시 처리**:
```typescript
// src/components/bdr-v2/more-groups.ts (사이트 코드)
// 가짜링크 4건은 영구 제거 (user-design-decisions §2-2)
// v2.2 시안의 components.jsx 변경분 중 다음만 수용:
// - 신규 8 진입점 (이미 등록됨)
// - 더보기 패널 자체의 그룹 구조
// 가짜링크 4건은 components.jsx 라인 120/130/131/145 를 **무시**하고 박제.
```

### 2-2. 🚨 위반 2 — 글로벌 카피 "서울 3x3" 5 파일

> 사용자 결정 §2-6: "전국 농구 매칭 플랫폼 (서울 3x3 한정 표현 금지)" + commit `46e5d1b`

`BDR v2.2/Dev/design/BDR v2.2/screens/` 안의 **v2.1 그대로 복사된 옛 카피**:

| 파일 | 라인 | 카피 |
|------|------|------|
| `About.jsx` | 영역 | "서울을 중심으로 한 3x3 농구 커뮤니티의 공식 허브" |
| `Checkout.jsx` | subtitle | "서울 3x3 오픈 챔피언십 · 팀 참가비" |
| `Login.jsx` | subtitle | "서울 3x3 농구 커뮤니티" |
| `Match.jsx` | desc | "서울 3x3 농구의 정수를 겨루는 오픈 챔피언십" |
| `More.jsx` | desc | "20년을 이어온 서울 3x3 농구 커뮤니티" |

**판정**: v2.1 시안에 있던 옛 카피가 v2.2 에도 그대로. 사용자 사이트 코드 (`src/`) 는 commit `46e5d1b` 으로 이미 픽스됐지만 **시안에는 회귀**.

**박제 시 처리**: 위 5 파일을 박제할 때 카피만 사용자 사이트 버전 ("전국 농구 매칭 플랫폼" 등) 으로 유지. 시안의 텍스트 그대로 박제 금지.

---

## 3. ⚠️ 구조 문제 — 폴더 이중 중첩

### 3-1. 발견

```
Dev/design/BDR v2.2/                    ← 사용자가 만든 폴더 (v2.1 카피)
├── (v2.1 동일 파일들)
└── Dev/                                ← ⚠️ zip 압축 풀 때 nested 됨
    └── design/
        └── BDR v2.2/                   ← 실제 신규 작업물 위치
            ├── README.md               ← 의뢰 답변
            ├── components.jsx          ← v2.2 변경 (가짜링크 위반 포함)
            ├── responsive.css          ← G-10 추가 (859줄)
            ├── MyBDR.html              ← 라우트 11 신규
            ├── _mobile_audit.html      ← 그룹 3 신규
            ├── extras-data.jsx
            └── screens/
                ├── (v2.1 72 그대로)
                └── (v2.2 신규 11)
```

### 3-2. 원인

클로드 디자인이 zip 파일에 `Dev/design/BDR v2.2/` 풀 패스를 포함해서 산출함. 사용자가 압축 푼 폴더가 이미 `BDR v2.2/` 라서 nested 됨.

### 3-3. 해결 (3 옵션)

#### A. nested 만 사용 (가장 간단, 권장)
```bash
cd "C:\0. Programing\mybdr\Dev\design"

# 외부 BDR v2.2/ (v2.1 카피만) 삭제
rm -rf "BDR v2.2"

# nested 안의 진짜 작업물을 BDR v2.2/ 로 이동
mv "BDR v2.2/Dev/design/BDR v2.2" "BDR v2.2-temp"
rm -rf "BDR v2.2"
mv "BDR v2.2-temp" "BDR v2.2"
```

→ 단, nested 버전은 v2.1 의 일부 파일 (data.jsx, community-data.jsx, tokens.css 등 변경 없음) 만 가짐. v2.1 카피본의 변경 없는 파일들은 별도로 복사 필요.

#### B. 두 폴더 병합 (안전)
```bash
cd "C:\0. Programing\mybdr\Dev\design"

# nested 의 신규/변경 파일을 외부 BDR v2.2/ 로 덮어쓰기
cp -rv "BDR v2.2/Dev/design/BDR v2.2/"* "BDR v2.2/"

# 검증 후 nested 삭제
rm -rf "BDR v2.2/Dev"
```

→ 외부 폴더가 v2.1 카피본 + 신규 작업물의 합본이 됨.

#### C. 그대로 두고 박제 시 정확한 경로 사용 (변경 없음)
박제 시 항상 `BDR v2.2/Dev/design/BDR v2.2/` 경로 사용. 단, 향후 혼동 위험.

**권장**: 옵션 B (병합).

---

## 4. ✅ 모든 의뢰 항목 점검 (산출물 체크리스트 §9)

| 의뢰 항목 | 상태 | 근거 |
|---------|------|------|
| `Dev/design/BDR v2.2/` 폴더 생성 | ⚠️ 이중 중첩 | §3 |
| `tokens.css` (변경 시 명시) | ✅ 변경 0 (v2.1 동일, 26456 byte) | — |
| `components.jsx` AppNav 결정 그대로 | ✅ utility bar / 메인 탭 9개 / 더보기 5그룹 모두 보존 | grep 검증 |
| `components.jsx` 신규 8 진입점 등록 | ✅ courtAdd / teamManage / searchResults / editProfile / notificationSettings / safety / passwordReset / onboardingV2 | grep 검증 |
| `components.jsx` 가짜링크 4건 영구 제거 | 🚨 위반 — 4건 다시 추가됨 | §2-1 |
| `extras-data.jsx` 신규 D등급 더미 데이터 | ✅ 11430 byte (v2.1 동일) | — |
| `responsive.css` v2.1 + G-10 + G-9 보호 | ✅ 859줄 (v2.1 의 756줄 + 100줄+) | §1-5 |
| `MyBDR.html` 라우트 11+ 추가 | ✅ 11/11 등록 | grep 11 |
| `_mobile_audit.html` 그룹 추가 | ✅ 3 그룹 (P0·필수 / P1·내 프로필 / P2·P3) | grep |
| `README.md` v2.1 → v2.2 변경 요약 | ✅ M1~M9 + D등급 12건 + 폴더 구조 + 회귀 매트릭스 가이드 | 직접 확인 |
| `screens/` D등급 11 신규 + LiveResult 회귀 | ✅ 11/11 (RefereeInfo + HelpGlossary 포함) | ls |
| 검수 매트릭스 §8-1 시안별 첨부 | ✅ JSDoc 헤더에 모두 첨부 | grep |
| 연결성 매트릭스 §6-2 시안별 첨부 | ✅ JSDoc Why/Pattern/진입/복귀/Edge 모두 명시 | grep |

**총 13 항목 중 11 통과 / 1 부분 통과 (구조) / 1 위반 (가짜링크)**.

---

## 5. 박제 전 정정 작업 (CLI 에 줄 후속 프롬프트)

```text
"오늘 작업 시작하자" 체크리스트 통과 후 다음 작업 시작.

# 목표
BDR v2.2 시안을 박제하기 전에 사용자 결정 위반 2건 정정 + 폴더 구조 정리.

# 근거 문서
Dev/design/v2.2-review-2026-04-30.md §2, §3

# 작업 1 — 폴더 구조 정리 (5분)

```bash
cd "C:\0. Programing\mybdr\Dev\design"

# nested 안의 신규/변경 파일을 외부 BDR v2.2 로 병합
cp -rv "BDR v2.2/Dev/design/BDR v2.2/"* "BDR v2.2/"

# 검증
ls "BDR v2.2/screens/" | wc -l  # 83 (v2.1 의 72 + 신규 11)
ls "BDR v2.2/README.md"          # 존재 확인

# nested 폴더 삭제
rm -rf "BDR v2.2/Dev"

# 최종 검증
ls "BDR v2.2/" | grep -v Dev | sort
```

# 작업 2 — components.jsx 가짜링크 3건 제거 (10분)

파일: BDR v2.2/components.jsx (병합 후 외부)

다음 라인 삭제:
- L120: { id:'guestApps',  label:'게스트 지원', icon:'🎟️' }
- L130: { id:'gameResult', label:'경기 결과', icon:'📊' }
- L131: { id:'gameReport', label:'경기 신고·평가', icon:'🚩' }

referee (L145) 는 RefereeInfo 옵션 B 채택의 결과로 정당. 단 라우트명을 'refereeInfo' 로 변경:
- L145: { id:'referee', ... } → { id:'refereeInfo', label:'심판 센터 안내', icon:'🦓' }

# 작업 3 — 글로벌 카피 5 파일 정정 (15분)

다음 5 파일에서 "서울 3x3" 표현을 사용자 결정 §2-6 기준으로 정정:

| 파일 | AS-IS | TO-BE |
|------|-------|-------|
| screens/About.jsx | "서울을 중심으로 한 3x3 농구 커뮤니티의 공식 허브" | "전국 농구 매칭 플랫폼" |
| screens/Login.jsx | "서울 3x3 농구 커뮤니티" | "전국 농구 매칭 플랫폼" |
| screens/More.jsx | "20년을 이어온 서울 3x3 농구 커뮤니티" | "20년을 이어온 농구 커뮤니티" |
| screens/Match.jsx | "서울 3x3 농구의 정수를 겨루는" | "전국 농구 대회" — 또는 toponym 보존 시 "서울" 만 유지 |
| screens/Checkout.jsx | "서울 3x3 오픈 챔피언십" | toponym (특정 대회 이름 — PM 결정) |

→ Match.jsx / Checkout.jsx 의 카피는 **특정 대회 이름** 일 가능성. PM 확인 후 결정.
→ 다른 3 파일 (About / Login / More) 은 글로벌 슬로건이므로 무조건 정정.

# 작업 4 — 박제 작업 우선순위 결정 (PM)

이후 시안 박제 작업은 다음 순서 권장:

P0 (사용자 일상 사용):
1. PostEdit.jsx → /community/[id]/edit 박제
2. GameEdit.jsx → /games/[id]/edit 박제
3. LiveResult.jsx → /live/[id] finished 분기 회귀 픽스
4. ProfileComplete.jsx → /profile/complete 박제 + /verify 직후 자동 redirect

P1 (사용자 경험):
5. ProfileGrowth.jsx → /profile/growth
6. ProfileBookings.jsx → /profile/bookings
7. VenueDetail.jsx → /venues/[slug]
8. ProfileWeeklyReport.jsx → /profile/weekly-report

P2:
9. ProfileCompletePreferences.jsx → /profile/complete/preferences
10. HelpGlossary.jsx → /help/glossary 박제 또는 /help redirect

P3:
11. RefereeInfo.jsx → /referee-info 신규 라우트 (사이트의 /referee 와 분리)

# 작업 5 — responsive.css G-10 사이트 globals.css 이식 (30분)

병합 후 BDR v2.2/responsive.css 의 마지막 G-10 블록을 src/app/globals.css 의 @media (max-width: 720px) 끝에 추가.

검증: npm run check:design + 모바일 viewport 시각.

# 검증 (작업 후)

- Dev/design/BDR v2.2/ 깊이 1 폴더 구조
- components.jsx 의 moreGroups 에 guestApps / gameResult / gameReport 0건
- About / Login / More 의 "서울 3x3" 표현 0건
- src/app/globals.css 에 G-10 룰 추가됨

총 견적: 약 1시간 (정정만). 박제 작업은 별도 11 페이지 × 1-2h.
```

---

## 6. 박제 작업 순서 권장 (정정 후)

```
순서 1 — 정정 작업 (위 §5, 1h)
  ├─ 폴더 구조 병합
  ├─ components.jsx 가짜링크 3건 제거 + referee → refereeInfo
  └─ 글로벌 카피 3 파일 정정

순서 2 — responsive.css G-10 사이트 이식 (30m)

순서 3 — P0 박제 4건 (4-6h)
  ├─ PostEdit (1.5h)
  ├─ GameEdit (1.5h)
  ├─ LiveResult 회귀 픽스 (1.5h)
  └─ ProfileComplete + /verify redirect (1.5h)

순서 4 — P1 박제 4건 (5-7h)
  ├─ ProfileGrowth (1.5h)
  ├─ ProfileBookings (1.5h)
  ├─ VenueDetail (2h)
  └─ ProfileWeeklyReport (1.5h)

순서 5 — P2/P3 박제 3건 (3-4h)
  ├─ ProfileCompletePreferences (1.5h)
  ├─ HelpGlossary (30m)
  └─ RefereeInfo (/referee-info 신규 라우트, 1.5h)
```

**총 견적**: 약 14-17시간 / 3 작업일.

---

## 7. KPI — v2.2 인수 후 박제율 목표

| 지표 | 현재 (v2.1) | v2.2 박제 후 |
|------|------------|------------|
| A등급 (시안 출처 명시) | 32 | 43+ (D 11 → A) |
| D등급 (박제 안됨) | 11 | 0 |
| F등급 (라우트 누락) | 1 | 0 (RefereeInfo 옵션 B 처리) |
| 박제율 | 27% (32/117) | 37%+ (43/117) |
| 모바일 G-10 룰 | 미적용 | 적용 |
| 시안 사이트 동기화 | 부분 | 거의 완료 |

---

## 부록 A — 검증된 사실

| 주장 | 근거 |
|------|------|
| 신규 시안 11/11 작성 | `find . -iname "*Edit.jsx" -o -iname "*Profile*.jsx" ...` 11 매칭 |
| 모든 시안 JSDoc 매트릭스 첨부 | `head -15` 직접 확인 8 파일 모두 매트릭스 표 |
| MyBDR.html 라우트 11 등록 | `grep -c "route === '..."` = 11 |
| _mobile_audit.html 3 그룹 추가 | `grep "v2.2 P0·필수\|v2.2 P1\|v2.2 P2"` 3 매칭 |
| components.jsx 가짜링크 4건 추가 | `grep "id:'guestApps'\|id:'gameResult'\|id:'gameReport'\|id:'referee'"` L120/130/131/145 |
| 글로벌 카피 위반 5 파일 | `grep -rE "서울 3x3"` 5 매칭 |
| responsive.css 859줄 (v2.1 의 756줄 + 100+) | `wc -l responsive.css` |
| 사용자 결정 §2-2 명시 | `Dev/design/user-design-decisions-2026-04-30.md` L48~52 |
| 사용자 결정 §2-6 명시 | `Dev/design/user-design-decisions-2026-04-30.md` L150~160 |

## 부록 B — 외부 참조

- 의뢰서: [claude-design-upgrade-prompt-2026-04-30.md](./claude-design-upgrade-prompt-2026-04-30.md)
- 사용자 결정: [user-design-decisions-2026-04-30.md](./user-design-decisions-2026-04-30.md)
- v2.1 베이스라인: `Dev/design/BDR v2.1/`
- v2.2 작업물 (현재 nested): `Dev/design/BDR v2.2/Dev/design/BDR v2.2/`
- v2.2 README: 위 폴더의 `README.md`
