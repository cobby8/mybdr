# v2.4 시안 작성 Batch Prompt — 시안 누락 페이지 일괄 작성 (2026-05-01)

> **목적**: v2.4 시안에 누락된 페이지 12+건 일괄 작성 (디자인 터미널용)
> **작업 환경**: Claude CLI / Windows + PowerShell / `subin` 브랜치 (또는 `subin-design`)
> **사전 조건**: 박제 터미널의 Phase A+B 완료 + commit 후 시작 (충돌 회피)
> **시안 베이스**: `Dev/design/BDR v2.4/`

---

## 0. 시작 전 체크 (필수 — 디자인 터미널)

```powershell
# 1. 박제 터미널의 commit 다 받기
git checkout subin
git pull origin subin

# 2. working tree 깨끗한지
git status

# 3. 박제 터미널의 Phase A+B 결과 검증 (Phase 19 적용 확인)
Select-String -Path 'Dev/design/BDR v2.4/components.jsx' -Pattern "Icon\.mail|app-nav__icon-btn"
# → 1+ 매칭 기대

# 4. 작업 영역 확인 — 디자인 터미널은 screens/ 만 수정
ls "Dev/design/BDR v2.4/screens"
```

**보고 형식**:
```
디자인 터미널 시작 점검:
- 박제 commit 받음 (subin: <hash>)
- working tree 깨끗
- Phase 19 components.jsx ✓ (Icon.mail 발견)
- 작업 영역: Dev/design/BDR v2.4/screens/ 만
이대로 시안 작성 시작합니다.
```

---

## 0-A. 룰 자동 적용 — 매 시안 작성 전 인지

```
[프롬프트 첫 줄에 항상 포함]

다음 시안 작성 시작.

룰 인지 (시안 작성 전 13 룰 자동 적용):
- uploads/00-master-guide.md (13 룰 마스터)
- uploads/02-design-system-tokens.md (var(--*) 토큰만)
- uploads/03-appnav-frozen-component.md (AppNav frozen — 새 시안엔 헤더 그리지 X)
- uploads/06-self-checklist.md (작성 후 자체 검수)

자체 검수 형식 (작성 후 보고):
✅ <시안명> 작성 완료
- 13 룰 통과: A1~D13 모두 ✓
- 분량: <N줄>
- 데이터 placeholder: const SAMPLE_<NAME> = {...}
- 작성 후 grep 자동 검수: 0 위반
```

---

## 0-B. 박제 터미널 충돌 회피 룰 (디자인 터미널)

| 영역 | 박제 터미널 | 디자인 터미널 |
|------|------|------|
| `Dev/design/BDR v2.4/components.jsx` | ⭕ 수정 가능 | ❌ 수정 금지 |
| `Dev/design/BDR v2.4/MyBDR.html` | ⭕ 수정 가능 | ❌ 수정 금지 |
| `Dev/design/BDR v2.4/screens/<기존>.jsx` | ⭕ 수정 가능 (Phase A 정리) | ❌ 수정 금지 |
| `Dev/design/BDR v2.4/screens/<신규>.jsx` | ❌ | ⭕ **신규 추가** |
| `Dev/design/BDR v2.4/screens/_pending-routes.md` | ❌ | ⭕ 라우트 매칭 추가 |
| `src/app/`, `src/components/` | ⭕ Phase C/D 박제 | ❌ |

**라우트 매칭은 `_pending-routes.md` 에만 추가 → 박제 터미널이 다음 commit 시 MyBDR.html 에 통합** (충돌 회피).

---

## Phase F-1 — P0 마이페이지 hub 깊은 페이지 (3건, 확정)

> **출처**: v2.3 README — "Profile.jsx hub 흡수 + 별도 깊은 페이지 유지" 명시
> **시간**: 각 30~45분 / 총 1.5~2시간

### F-1-1. ProfileBookings.jsx — `/profile/bookings`

```
@design — Dev/design/BDR v2.4/screens/ProfileBookings.jsx 신규 작성.

[룰 인지] §0-A 자동 적용.

[페이지 의도]
- 마이페이지 hub (/profile) 의 "다가오는 일정" 카드 클릭 시 진입하는 깊은 페이지
- 내 예약 / 신청 전체 리스트 통합 (코트 예약 + 게임 참가 + 게스트 신청 + 대회 참가)

[레이아웃]
- 상단: 페이지 헤더 ("내 예약 · BOOKINGS") + 타입 필터 칩 (전체/코트/게임/게스트/대회) + 정렬 드롭다운 (최신/임박/완료)
- 좌측 (PC ≥1024px): 예약 카드 리스트 1열 (각 카드 = 코트명 + 일시 + 상태 배지 + CTA)
- 우측 sticky (PC): 통계 카드 (이번 달 N건 / 누적 N건 / 다음 일정 D-N)
- 모바일 (<720px): 통계는 상단 가로 스크롤 / 필터 칩 가로 스크롤 / 카드 1열

[데이터 placeholder]
const SAMPLE_BOOKINGS = [
  { id, type: 'court_booking'|'game'|'guest'|'tournament', title, court, datetime, status: 'upcoming'|'ongoing'|'done'|'canceled', cta_route },
  ...
];  // 8~12건

[상태별 시각]
- upcoming  → var(--accent) 액센트 + D-N 표시
- ongoing   → var(--ok) 그린 점멸 (CSS animation)
- done      → var(--ink-mute) 회색
- canceled  → var(--ink-dim) + 취소선

[빈 상태] 예약 0건 → 일러스트 + "코트 예약하러 가기" CTA → /courts

[모바일 룰 13]
- 720px 분기 / iOS input 16px / 버튼 44px
- 인라인 grid repeat(3, 1fr) 사용 시 720px 분기 추가

[컴포넌트] export: function ProfileBookings({ setRoute })

[작성 후]
- _pending-routes.md 에 추가:
  else if (route === 'profileBookings') screen = <ProfileBookings setRoute={setRoute}/>;
- §0-A 자체 검수 형식으로 보고

[분량] 200~280줄
```

### F-1-2. ProfileGrowth.jsx — `/profile/growth`

```
@design — ProfileGrowth.jsx 신규 작성. (룰 §0-A + 충돌 룰 §0-B 자동)

[페이지 의도]
- 마이페이지 hub 의 "시즌 스탯" 카드 클릭 시 진입
- 시즌별 성장 그래프 + 마일스톤 + 추세 분석

[레이아웃]
- 상단: 시즌 선택 드롭다운 (2024-S1 / 2024-S2 / 2025-S1 / 2026-S1) + 핵심 KPI 4건 카드 grid
- 중간: 성장 그래프 (가로축 = 시간, 세로축 = 메트릭) — SVG + 3가지 메트릭 toggle (참가/평점/평균득점)
- 하단: 마일스톤 timeline (달성 시점 + 배지 + 한줄 설명)

[KPI 4건]
- 참가 게임 수 + 전 시즌 대비 ±N
- 평점 + 추세 화살표
- 활동 일수
- 누적 업적

[데이터 placeholder]
const SAMPLE_GROWTH = {
  current_season: '2026-S1',
  seasons: [{ id, label }, ...],
  kpis: { games: { value, delta }, rating: { value, delta }, ... },
  graph: [{ date, games, rating, points }, ...],  // 시즌 기간만큼 (예 12주)
  milestones: [{ date, type, title, description, badge_icon }, ...],
};

[그래프] 차트 라이브러리 X — SVG path + line / bar 직접 그리기 (시안 단계)
- 다크/라이트 토큰 자동 적용 (var(--accent), var(--cafe-blue))

[모바일] 그래프 가로 스크롤 / KPI grid 2열로 / timeline 세로 1열

[컴포넌트] function ProfileGrowth({ setRoute })

[_pending-routes 추가]
else if (route === 'profileGrowth') screen = <ProfileGrowth setRoute={setRoute}/>;

[분량] 250~320줄
```

### F-1-3. ProfileWeeklyReport.jsx — `/profile/weekly-report`

```
@design — ProfileWeeklyReport.jsx 신규 작성. (룰 §0-A + 충돌 룰 §0-B 자동)

[페이지 의도]
- 매주 월요일 발송되는 주간 활동 리포트 페이지
- 마이페이지 hub 의 "최근 활동" 카드 또는 알림 클릭 시 진입
- 이번 주 vs 지난 주 비교 → 동기 부여

[레이아웃]
- 상단: 주차 네비 (◀ 2026-W17 ▶) + 발송 일자 + "이메일 다시 받기" 링크
- 중간 1: 비교 카드 4건 grid (이번 주 vs 지난 주: 참가 / 평점 / 거리 / 활동 시간)
- 중간 2: 주간 하이라이트 (3~5건 — 베스트 게임 / 새 친구 / 마일스톤)
- 하단: "다음 주 추천" 섹션 (코트 / 게임 / 대회 추천 3건)

[비교 카드 시각]
- 증가 → var(--ok) + ↑ + delta 표시
- 감소 → var(--ink-mute) + ↓
- 무변화 → var(--ink-mute) + 「-」

[데이터 placeholder]
const SAMPLE_WEEKLY = {
  current_week: '2026-W17',
  week_start: '2026-04-27',
  week_end: '2026-05-03',
  this_week: { games, rating, distance, hours },
  last_week: { games, rating, distance, hours },
  highlights: [{ type, title, value, link_route }, ...],
  recommendations: { courts: [...], games: [...], tournaments: [...] },
};

[빈 상태] 활동 0건 주 → "이번 주는 잠시 쉬어가는 주네요. 다음 주를 함께 시작해보세요." + CTA

[모바일] 비교 카드 2열 grid / 하이라이트 1열 / 추천 가로 스크롤

[컴포넌트] function ProfileWeeklyReport({ setRoute })

[_pending-routes 추가]
else if (route === 'profileWeeklyReport') screen = <ProfileWeeklyReport setRoute={setRoute}/>;

[분량] 220~280줄
```

---

## Phase F-2 — 결제 / 체크인 흐름 (2건, 확정)

### F-2-1. PaymentFail.jsx — `/courts/[id]/booking/payment-fail`

```
@design — PaymentFail.jsx 신규 작성. (룰 §0-A + 충돌 룰 §0-B 자동)

[페이지 의도]
- 코트 예약 결제 실패 시 도착 분기 페이지
- 실패 사유 명확히 + 재시도 / 다른 결제 / 환불 안내 CTA 3개

[레이아웃]
- 중앙 정렬 / max-width 480px
- 큰 일러스트 (X 아이콘 SVG, var(--err) #dc2626)
- 페이지 타이틀: "결제를 완료하지 못했어요"
- 실패 사유 카드 (이유 + 코드)
- CTA 3개 stack: [다시 시도] (primary) / [다른 결제 수단] (secondary) / [도움말 보기] (ghost)
- 하단: 예약 정보 요약 (코트명 / 일시 / 금액)

[에러 코드 분기]
- PAYMENT_DECLINED → "카드사가 거절했어요"
- INSUFFICIENT_BALANCE → "잔액이 부족해요"
- TIMEOUT → "시간이 초과됐어요"
- NETWORK_ERROR → "네트워크 오류가 발생했어요"
- DEFAULT → "결제 중 문제가 발생했어요"

[데이터 placeholder]
const SAMPLE_PAYMENT_FAIL = {
  error_code: 'PAYMENT_DECLINED',
  error_message: '카드사가 결제를 거절했어요',
  court: { name, address, datetime },
  amount: 25000,
  retry_url: '/courts/.../booking',
  payment_methods: ['card', 'transfer', 'point'],
};

[모바일] 일러스트 축소 (60% 크기) / CTA 버튼 44px 풀 너비
[룰 12 카피] placeholder 5단어 이내, "예) " X
[룰 11] "전국 농구 매칭 플랫폼" 일관

[컴포넌트] function PaymentFail({ setRoute })

[_pending-routes 추가]
else if (route === 'paymentFail') screen = <PaymentFail setRoute={setRoute}/>;

[분량] 150~200줄
```

### F-2-2. Checkin.jsx — `/courts/[id]/checkin`

```
@design — Checkin.jsx 신규 작성. (룰 §0-A + 충돌 룰 §0-B 자동)

[페이지 의도]
- 예약한 코트 도착 시 QR 또는 GPS 기반 체크인
- 체크인 후 게임 진입 (라이브 스코어 / 팀원 / 다음 단계)

[상태 분기 — 4 상태 모두 시안 표시 (탭으로 전환)]
1. before_checkin (예약 시간 30분 전): 카운트다운 + "곧 체크인 가능"
2. during_window (체크인 가능): QR 스캔 + "현재 위치로 체크인" 버튼
3. after_checkin (체크인 완료): 게임 카드 + 팀원 리스트 + 다음 액션
4. expired (체크인 시간 지남 — 예약 취소 위험): 경고 + "운영자에게 문의" CTA

[레이아웃]
- 상단: 코트 정보 (이름 + 주소 + 예약 시간)
- 중간: 상태별 메인 영역 (위 4 분기)
- 하단: 도움말 / 취소 / 문의 링크

[QR 스캔 영역]
- 큰 카메라 viewfinder (320px x 320px) — placeholder div + 스캔 가이드 라인
- 또는 "QR 없이 체크인" 버튼 (GPS 기반)

[체크인 후 게임 카드]
- 코트 + 일시 + 팀원 4명 (Avatar) + "게임 시작" CTA → /games/[id] 또는 /live/[id]

[데이터 placeholder]
const SAMPLE_CHECKIN = {
  status: 'during_window',  // 시안에서 4 상태 전환 가능 (개발자 toggle 버튼)
  court: { id, name, address, datetime },
  reservation: { team_id, members: [...] },
  checkin_window: { start, end },
  current_time: '...',
};

[시안 디버그] 우측 상단 작은 toggle: [Before] [Window] [After] [Expired]

[모바일] viewfinder 320 → 280 / 버튼 44px / 카운트다운 큰 글씨

[컴포넌트] function Checkin({ setRoute })

[_pending-routes 추가]
else if (route === 'checkin') screen = <Checkin setRoute={setRoute}/>;

[분량] 250~320줄 (4 상태 분기로 인해 풍부)
```

---

## Phase F-3 — 가짜링크 정합성 (1건, 확정)

### F-3-1. RefereeInfo.jsx — `/referee-info`

```
@design — RefereeInfo.jsx 신규 작성. Referee.jsx 가짜링크 대체. (룰 §0-A + §0-B 자동)

[페이지 의도]
- /referee 사이트는 별도 셸 (심판 플랫폼) 점유 중
- /referee-info 는 일반 사용자 대상 SEO 페이지 — 심판 자격 / 시스템 / 절차 설명
- 심판 지망자를 /referee/signup 으로 유도

[레이아웃]
- Hero 섹션: 큰 이미지 + 타이틀 ("BDR 심판이 되어보세요") + 부제 ("전국 농구 매칭 플랫폼의 공정한 경기를 만들어가는 심판들") + CTA "심판 등록하기"
- 섹션 1 — 심판 시스템: 4 카드 grid (자격 / 교육 / 등급 / 활동)
- 섹션 2 — 절차: 4 step (신청 → 교육 → 인증 → 활동) — 가로 stepper
- 섹션 3 — 등급 시스템: D / C / B / A / S 등급 표 (자격 + 시급 + 권한)
- 섹션 4 — FAQ: 5~7건 아코디언
- 섹션 5 — 후기: 3건 카드 (실제 심판의 한줄 + 등급)
- 하단 CTA: "지금 심판 등록하기" → /referee/signup (외부 링크)

[데이터 placeholder]
const REFEREE_INFO = {
  hero: { title, subtitle, image_url, cta_url: '/referee/signup' },
  system_cards: [
    { title: '자격', description, icon },
    { title: '교육', ... },
    { title: '등급', ... },
    { title: '활동', ... },
  ],
  steps: [
    { num: 1, title: '신청', description },
    ...
  ],
  grades: [
    { grade: 'D', requirement, hourly_rate, permissions },
    ...
  ],
  faqs: [{ question, answer }, ...],
  testimonials: [{ name_initial, grade, comment }, ...],
};

[모바일]
- Hero 이미지 hidden 또는 작게 / 타이틀 폰트 축소
- 4 step stepper → 1열 vertical
- 등급 표 → 가로 스크롤
- FAQ 아코디언 동일

[룰 11] "전국 농구 매칭 플랫폼" / "서울 3x3" X
[룰 9 — About 운영진 실명 박제 X] testimonials 는 "김OO 심판" 같이 일반 라벨

[컴포넌트] function RefereeInfo({ setRoute })

[_pending-routes 추가]
else if (route === 'refereeInfo') screen = <RefereeInfo setRoute={setRoute}/>;

[분량] 350~450줄 (SEO 풍부한 콘텐츠)
```

---

## Phase F-4 — PM 결정 후 작성 (6건)

> 다음 6건은 **PM (수빈) 답변 받기 전 작성 시작 X**. 1줄 답변으로 통합 vs 별도 결정.

### F-4-PM-Q. PM 결정 질문 (6건 한꺼번에)

```
[디자인 터미널 → PM (수빈) 1회 보고]

PM 결정 필요 6건:

Q1. /courts/[id]/manage = 사용자 영역인가 admin 영역인가?
  A. 코트 운영자(협력업체) 전용 → E등급 (admin), 시안 X
  B. 일반 사용자 권한 페이지 → 시안 작성 필요

Q2. /teams/manage 와 /teams/[id]/manage 의 차이?
  A. 다중 팀 관리 hub (가입 팀 전환) → 시안 작성
  B. 유령 라우트 → 삭제

Q3. /tournaments/[id]/schedule, /teams = 별도 시안인가 Match.jsx 탭 통합인가?
  A. 별도 라우트 살림 → 시안 2건 추가
  B. Match.jsx 의 탭으로 통합 → 시안 X + 라우트 통합 (코드 변경 필요)

Q4. /profile/basketball — 진입로 5곳 활발히 사용 중. 시안은?
  A. 별도 페이지로 ProfileBasketball.jsx 시안 작성 (권장)
  B. EditProfile.jsx 의 농구 탭으로 통합

Q5. /profile/payments, /preferences, /subscription = 유령 3건. 어떻게?
  A. Billing.jsx + Settings.jsx 의 탭으로 통합 → 라우트는 redirect, 시안 X (권장)
  B. 별도 페이지 3건 시안 작성

Q6. /community/[id]/edit, /games/[id]/edit, /help/glossary, /venues/[slug]
   = v2.3 README 통합 의도. 그대로 진행?
  A. hub 시안 안에 ?mode=edit / 탭 / 분기로 처리, 시안 X (권장)
  B. 별도 시안 4건 작성

→ 답변 형식: "Q1=A, Q2=B, Q3=A, Q4=A, Q5=A, Q6=A" 1줄로.
```

PM 답변 받은 후 진행할 시안:

### F-4-1. (Q1=B 시) CourtManage.jsx — `/courts/[id]/manage`

```
@design — CourtManage.jsx 신규. (PM 답변 Q1=B 시)

[페이지 의도] 코트 등록자가 자기 코트 관리 (예약 현황 / 가격 변경 / 휴무일 설정 / 사진 변경)
[레이아웃] 좌 sticky 탭 nav (예약 현황 / 가격 / 휴무 / 사진 / 운영자) + 우 본문
[분량] 280~350줄
```

### F-4-2. TeamsManage.jsx — `/teams/manage` (Q2=A 시)

```
@design — TeamsManage.jsx 신규. (PM 답변 Q2=A 시)

[페이지 의도] 사용자가 가입한 팀 전체 관리 hub (다중 팀 전환)
[레이아웃] 상단 가입 팀 카드 grid (로고 + 이름 + 역할) + 클릭 시 /teams/[id]/manage 로 이동
[분량] 150~200줄
```

### F-4-3, F-4-4. TournamentSchedule.jsx + TournamentTeams.jsx (Q3=A 시)

```
@design — TournamentSchedule.jsx + TournamentTeams.jsx 신규. (PM 답변 Q3=A 시)

[페이지 의도] 대회 hero 페이지의 일정 / 참가팀 별도 라우트 (/tournaments/[id]/schedule, /teams)
[일정] 상단 헤더 + 라운드별 일정 timeline + 경기 카드 리스트
[참가팀] 헤더 + 팀 카드 grid + 시드별 정렬
[Match.jsx 와 톤 일관]
[각 분량] 200~280줄
```

### F-4-5. ProfileBasketball.jsx — `/profile/basketball` (Q4=A 시)

```
@design — ProfileBasketball.jsx 신규. (PM 답변 Q4=A 시)

[페이지 의도] 농구 프로필 종합 hub — 포지션 / 스타일 / 참가 대회 / 베스트 경기 / 농구 비고
[레이아웃] 상단 프로필 카드 + 4 섹션 (참가 대회 #my-tournaments / 베스트 게임 / 스킬 레이더 / 농구 메모)
[#my-tournaments 앵커 필수] (현재 페이지 5곳에서 사용 중)
[분량] 280~350줄
```

### F-4-6. (Q5=B 시) ProfilePayments / Preferences / Subscription 3건

```
@design — 3건 신규. (PM 답변 Q5=B 시 — 권장 X)

권장은 Q5=A (Billing/Settings 탭 통합). PM 가 명시적으로 B 선택해야만 작성.
```

### Q6=B 시 — 4건 추가 (권장 X)

```
v2.3 README 통합 의도 따르는 게 정석. PM 가 명시적으로 B 선택해야 작성.
- PostEdit.jsx / GameEdit.jsx / HelpGlossary.jsx / VenueDetail.jsx
```

---

## Phase F-5 — _pending-routes.md 작성 (모든 시안 작성 후)

```
@design — Dev/design/BDR v2.4/screens/_pending-routes.md 신규 또는 갱신.

[목적] 디자인 터미널이 추가한 신규 라우트를 박제 터미널이 다음 commit 시 MyBDR.html 에 일괄 통합하기 위한 핸드오프 파일

[형식]
# v2.4 신규 시안 라우트 매칭 (디자인 터미널 → 박제 터미널 핸드오프)

다음 라인을 MyBDR.html 의 route handler 체인 안에 추가:

```jsx
// Phase F-1 — 마이페이지 깊은
else if (route === 'profileBookings')      screen = <ProfileBookings setRoute={setRoute}/>;
else if (route === 'profileGrowth')        screen = <ProfileGrowth setRoute={setRoute}/>;
else if (route === 'profileWeeklyReport')  screen = <ProfileWeeklyReport setRoute={setRoute}/>;

// Phase F-2 — 결제·체크인
else if (route === 'paymentFail')          screen = <PaymentFail setRoute={setRoute}/>;
else if (route === 'checkin')              screen = <Checkin setRoute={setRoute}/>;

// Phase F-3 — 가짜링크 대체
else if (route === 'refereeInfo')          screen = <RefereeInfo setRoute={setRoute}/>;

// Phase F-4 — PM 결정 후
// (조건부 — PM 답변에 따라 추가)
```

또한 MyBDR.html 의 <script type="text/babel" src="screens/..."> import 영역에 다음 추가:

```html
<script type="text/babel" src="screens/ProfileBookings.jsx"></script>
<script type="text/babel" src="screens/ProfileGrowth.jsx"></script>
<script type="text/babel" src="screens/ProfileWeeklyReport.jsx"></script>
<script type="text/babel" src="screens/PaymentFail.jsx"></script>
<script type="text/babel" src="screens/Checkin.jsx"></script>
<script type="text/babel" src="screens/RefereeInfo.jsx"></script>
```

→ 박제 터미널 다음 commit 시 위 라인을 MyBDR.html 에 통합 + _pending-routes.md 삭제.
```

---

## Phase F-6 — 자동 검수 (모든 시안 작성 후)

```powershell
$base = "Dev/design/BDR v2.4"

Write-Host "=== 카피 룰 11 — 글로벌 카피 위반 ==="
Get-ChildItem -Path "$base/screens" -Include *.jsx | Select-String -Pattern "서울 3x3 농구 커뮤니티|다음카페"

Write-Host "=== 카피 룰 12 — placeholder 5단어 / '예) ' 시작 ==="
Get-ChildItem -Path "$base/screens" -Include *.jsx | Select-String -Pattern 'placeholder="예[\) :]'

Write-Host "=== 토큰 룰 12 — pill 9999/999px ==="
Get-ChildItem -Path "$base/screens" -Include *.jsx | Select-String -Pattern "border-radius:\s*(9999|999)px"

Write-Host "=== 토큰 룰 10 — 핑크/살몬/코랄 hex ==="
Get-ChildItem -Path "$base/screens" -Include *.jsx | Select-String -Pattern "#FF[68][0-9A-F]{4}|#F0[68][0-9A-F]{4}|pink|salmon|coral"

Write-Host "=== 룰 11 — lucide-react ==="
Get-ChildItem -Path "$base/screens" -Include *.jsx | Select-String -Pattern "lucide-react"

Write-Host "=== AppNav 새로 그리기 위반 (시안에서 헤더 그렸으면 룰 위반) ==="
Get-ChildItem -Path "$base/screens" -Include *.jsx | Select-String -Pattern "function AppNav|class.*app-nav__main"
```

→ 모두 0 매칭이면 Phase F 통과.

---

## Phase F-7 — commit + push (디자인 터미널 마감)

```powershell
cd C:\0. Programing\mybdr
git status

# 신규 시안 + _pending-routes 만 stage (박제 터미널 변경분 절대 stage X)
git add "Dev/design/BDR v2.4/screens/ProfileBookings.jsx"
git add "Dev/design/BDR v2.4/screens/ProfileGrowth.jsx"
git add "Dev/design/BDR v2.4/screens/ProfileWeeklyReport.jsx"
git add "Dev/design/BDR v2.4/screens/PaymentFail.jsx"
git add "Dev/design/BDR v2.4/screens/Checkin.jsx"
git add "Dev/design/BDR v2.4/screens/RefereeInfo.jsx"
git add "Dev/design/BDR v2.4/screens/_pending-routes.md"

# PM 답변 후 추가 작성한 것도 같이 stage

git commit -m "design(v2.4): 시안 누락 N건 신규 작성

- ProfileBookings / Growth / WeeklyReport (마이페이지 깊은 페이지 hub 흡수)
- PaymentFail / Checkin (결제·체크인 분기)
- RefereeInfo (Referee.jsx 가짜링크 대체 SEO)
- _pending-routes.md 핸드오프 파일 (박제 터미널 통합용)

13 룰 자동 검수 통과 (자동 grep 0 위반)."

git push origin subin
```

→ 박제 터미널이 다음 작업 (Phase C/D 박제) 진행 시 _pending-routes.md 라인을 MyBDR.html 에 통합.

---

## 부록 A — 신규 시안 작성 체크리스트 (각 시안마다 자체 검수)

```
□ 룰 1 — AppNav 새로 그리기 X (시안에서 헤더 그리면 위반)
□ 룰 2~7 — AppNav frozen 코드는 components.jsx 에서 처리 (시안 영역 외)
□ 룰 8 — 가짜링크 4건 라우트 안 만들기 (gameResult/gameReport/guestApps/referee)
□ 룰 9 — "전국 농구 매칭 플랫폼" / "서울 3x3" X / 운영진 실명 박제 X
□ 룰 10 — var(--*) 토큰만 / 핑크·살몬·코랄·hex 하드코딩 X
□ 룰 11 — Material Symbols Outlined 또는 검증된 이모지 / lucide-react X
□ 룰 12 — 버튼·카드 4px 또는 8px / pill 9999px·999px X / placeholder 5단어 / "예) " X
□ 룰 13 — 720px 분기 / iOS input 16px / 버튼 44px / 인라인 grid 분기

추가 BDR v2.4 표준:
□ 데이터 placeholder: const SAMPLE_<NAME> = {...} 분리
□ 컴포넌트 export: function <Name>({ setRoute })
□ _pending-routes.md 라인 추가
□ 분량 100~400줄 (단순 페이지 100~150 / 복합 250~400)
□ 다크 모드 기본 동작 (var(--bg) 등 토큰)
□ 빈 상태 / 에러 상태 시안에 포함
```

---

## 부록 B — 작성 순서 권장 (디자인 터미널)

```
1. §0 시작 전 체크 → 사용자 (PM) 승인
2. §0-A 룰 인지 + §0-B 충돌 룰 인지
3. F-4-PM-Q 6건 PM 결정 질문 → 사용자 답변 대기 (1줄)
4. PM 답변 받는 동안 확정 6건 먼저 진행:
   a. F-1-1 ProfileBookings (30분)
   b. F-1-2 ProfileGrowth (40분)
   c. F-1-3 ProfileWeeklyReport (40분)
   d. F-2-1 PaymentFail (20분)
   e. F-2-2 Checkin (40분)
   f. F-3-1 RefereeInfo (50분)
5. PM 답변 받으면 F-4 의 해당 항목만 추가 작성
6. §F-5 _pending-routes.md 작성
7. §F-6 자동 검수 PowerShell
8. §F-7 commit + push
9. 박제 터미널에 알림: "디자인 터미널 시안 N건 push 완료. _pending-routes.md 통합 부탁."

총 시간: 확정 6건 = 3.5~4시간 / PM 답변 후 추가 = +1~3시간
```

---

## 출처

- 시안 누락 분석: 대화 (2026-05-01) — 양방향 매핑 표 A·B
- 룰 13개: `Dev/design/BDR v2.4/uploads/00-master-guide.md`
- AppNav frozen: `Dev/design/BDR v2.4/uploads/03-appnav-frozen-component.md`
- 토큰: `Dev/design/BDR v2.4/uploads/02-design-system-tokens.md`
- 자체 검수: `Dev/design/BDR v2.4/uploads/06-self-checklist.md`
- v2.3 README 통합 의도: `Dev/design/BDR v2.3/README.md` §1
- 박제 터미널 충돌 회피: 대화 (2026-05-01) "병렬 진행 5룰"
