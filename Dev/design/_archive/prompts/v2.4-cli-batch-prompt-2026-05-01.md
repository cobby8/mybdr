# v2.4 CLI 박제 Batch Prompt — Phase 19 적용 (2026-05-01)

> **목적**: BDR v2.4 시안 → 운영 코드 박제 (P0 마이페이지 hub + P1 4건). Phase 19 (AppNav 쪽지 추가) 13 룰 일괄 적용.
> **작업 환경**: Claude CLI / Windows + PowerShell / `subin` 브랜치
> **사전 동기화 완료**: claude-project-knowledge/03·07, mybdr/CLAUDE.md, 메모리 `design_appnav_frozen_violations.md` 모두 Phase 19 반영
> **시안 베이스**: `Dev/design/BDR v2.4/` (다운로드 BDR v2.3 카피 + Phase 19 룰 자료 동봉)

---

## 0. 작업 시작 전 체크리스트 (필수 — 매 세션 첫 응답)

> CLAUDE.md "오늘 작업 시작하자" 체크리스트 따름. 본 작업 전 반드시 점검 + 사용자 승인.

```powershell
# 1. 원격 / 브랜치 상태
git remote -v
git fetch origin --prune
git branch -vv

# 2. 현재 브랜치가 subin 인지
git branch --show-current   # subin 기대

# 3. .env 의 DATABASE_URL 이 개발 DB 인지 (값 노출 금지 — 호스트만 확인)
node -e "const u=new URL(process.env.DATABASE_URL||''); console.log(u.host || 'no-env')"

# 4. .env.local 에 localhost:3001 오버라이드 있는지
findstr /i "localhost:3001" .env.local

# 5. main / dev / subin 차이
git log --oneline origin/main..origin/dev | head -5
git log --oneline origin/dev..subin | head -5
```

**보고 형식**:
```
오늘 작업 시작 점검:
- 원격: github.com/bdr-tech/mybdr ✓
- 브랜치: subin (origin/dev에서 N커밋 앞)
- DB: 개발 DB (호스트 *.supabase.co) ✓
- 로컬 포트: 3001 ✓
이대로 작업 시작해도 될까요?
```

→ 사용자 승인 후 Phase A 진행. 승인 전 파일 수정 / 브랜치 전환 / 머지 / 커밋 금지.

---

## Phase A — v2.4 시안 자체 Phase 19 룰 적용

> **목표**: `Dev/design/BDR v2.4/` 가 13 룰 통과. 자동 검수 6 케이스 0 위반.
> **예상**: 30분
> **에이전트**: developer 1 + tester 1 (병렬 가능)

### A-1. AppNav frozen 코드 카피 (components.jsx)

**입력**: `Dev/design/BDR v2.4/uploads/03-appnav-frozen-component.md` §1 코드
**출력**: `Dev/design/BDR v2.4/components.jsx` 의 AppNav 함수 교체

```
[프롬프트]
Dev/design/BDR v2.4/uploads/03-appnav-frozen-component.md 의 §1 JSX 코드를
Dev/design/BDR v2.4/components.jsx 의 `function AppNav(...)` 부터 닫는 } 까지
그대로 카피해서 교체해줘. 기존 코드의 utility bar / main bar / drawer 모두 03 §1 코드로 대체.

체크 포인트:
- 9 메인 탭 배열에 'more' 추가 (마지막 9번째)
- main bar 우측 = 검색/쪽지/알림/다크/햄버거 5개 (Phase 19 — 쪽지 Icon.mail)
- 검색·쪽지·알림 모두 .app-nav__icon-btn 클래스 (.btn .btn--sm 사용 X)
- "더보기 ▼" dropdown trigger 버튼 제거 (drawer 안 5그룹 패널만)
- "RDM rdm_captain" 아바타 + 닉네임 main bar 우측에서 제거
- utility bar 우측 (계정/설정/로그아웃) 모바일에서도 표시 (좌측만 .util-left)

작업 완료 후 다음 grep 자체 검수 통과 보고:

powershell -Command "Select-String -Path 'Dev/design/BDR v2.4/components.jsx' -Pattern '더보기 ▼|RDM rdm_captain|btn btn--sm.*title=.(검색|쪽지|알림)'"
→ 0 매칭 기대
```

### A-2. ThemeSwitch 카피 (components.jsx)

```
[프롬프트]
Dev/design/BDR v2.4/uploads/03-appnav-frozen-component.md §2 의 ThemeSwitch 함수와
§3 CSS 클래스를 적용해줘.

- ThemeSwitch 는 viewport 분기 (PC 듀얼 라벨 / 모바일 단일 아이콘)
- .theme-switch--desktop / .theme-switch--mobile 미디어 쿼리
- 모바일 (≤768px) 에서 듀얼 라벨 hidden, 단일 아이콘 inline-flex
```

### A-3. moreGroups IA 카피 (components.jsx)

```
[프롬프트]
Dev/design/BDR v2.4/uploads/03-appnav-frozen-component.md §4 의 moreGroups 배열을
components.jsx 의 AppNav 안에 그대로 적용. 가짜링크 4건 (gameResult / gameReport / guestApps / referee) 자동 제거.

추가로 다음 항목 필요:
- 내 활동: communityNew (글 작성) — 03 §4 에 명시되어 있는데 v2.3 components.jsx 에서 빠져있었음 (write 라우트 참조)
- 둘러보기: refereeInfo (심판 센터 안내) — 가짜링크 referee 대체

03 §4 에 없는 신규 항목 (v2.3 에서 추가됐던 것들):
- bracket / tournamentEnroll / guestApply / refereeRequest
→ 이건 v2.3 사용자 결정과 충돌 가능. PM (수빈) 에게 1줄 질문 후 진행 여부 결정.
```

### A-4. 라우트 매칭 정리 (MyBDR.html)

```
[프롬프트]
Dev/design/BDR v2.4/MyBDR.html 의 route handler (else if 체인) 정리:

가짜링크 4건 — 라우트 매칭 제거:
- route === 'gameResult'  →  제거
- route === 'gameReport'  →  제거
- route === 'guestApps'   →  제거
- route === 'referee'     →  제거 (단, refereeInfo 신규 라우트 추가)

신규 라우트 매칭 추가:
- route === 'mypage'      →  <Profile setRoute={setRoute}/>  (mypage = Profile 별칭)
- route === 'refereeInfo' →  <RefereeInfo setRoute={setRoute}/>  (신규 컴포넌트)
- route === 'communityNew' →  <PostWrite ... />  (write 라우트와 동일)

작업 후 검증:
powershell -Command "Select-String -Path 'Dev/design/BDR v2.4/MyBDR.html' -Pattern 'route === ..(gameResult|gameReport|guestApps|referee).'"
→ 0 매칭 기대
powershell -Command "Select-String -Path 'Dev/design/BDR v2.4/MyBDR.html' -Pattern 'route === ..(mypage|refereeInfo).'"
→ 2 매칭 기대
```

### A-5. screens/ 정리 — 가짜링크 컴포넌트 격리 + RefereeInfo 신규

```
[프롬프트]
Dev/design/BDR v2.4/screens/ 의 가짜링크 4건 컴포넌트를 _archived/ 하위로 이동:
- screens/GameResult.jsx → screens/_archived/GameResult.jsx
- screens/GameReport.jsx → screens/_archived/GameReport.jsx
- screens/GuestApps.jsx  → screens/_archived/GuestApps.jsx
- screens/Referee.jsx    → screens/_archived/Referee.jsx

신규 screens/RefereeInfo.jsx 생성:
- 심판 센터 안내 SEO 페이지 (옵션 B — uploads/01 §2-2 참조)
- /referee 사이트 점유와 분리된 별도 라우트

신규 screens/More.jsx 정리:
- 현재 NotFound + About 함수만 들어있는 잘못된 머지본
- More 더보기 페이지 자체가 별도 라우트인지, drawer 안 5그룹만인지 결정 (PM 1줄 질문)
- 기본 가정: drawer 안 5그룹만 — More.jsx 파일 자체 삭제 또는 빈 stub 으로 정리
```

### A-6. Phase A 완료 검수 (필수 통과)

```powershell
# 6 케이스 자동 검수 — 모두 0 매칭 기대
$base = "Dev/design/BDR v2.4"

# 1. 더보기 ▼ dropdown / RDM 아바타
Select-String -Path "$base/components.jsx" -Pattern "더보기 ▼|RDM rdm_captain"

# 2. 모바일 듀얼 라벨 다크 (theme-switch--mobile 분기 있어야 함)
Select-String -Path "$base/components.jsx" -Pattern "theme-switch--mobile"   # 1+ 매칭 기대

# 3. 검색·쪽지·알림 박스
Select-String -Path "$base/components.jsx" -Pattern 'btn btn--sm" title="(검색|쪽지|알림)"'

# 4. utility 우측 모바일 누락 (.util-left 좌측에만)
Select-String -Path "$base/components.jsx" -Pattern '\.util-left|util-left'    # 1+ 매칭 기대

# 5. 아이콘 순서 [다크, 검색, 쪽지, 알림, 햄버거] 검증 — Icon.bell 앞에 Icon.mail 있는지
Select-String -Path "$base/components.jsx" -Pattern "Icon\.mail|쪽지·채팅|쪽지"   # 1+ 매칭 기대

# 6. 가짜링크 라우트 매칭
Select-String -Path "$base/MyBDR.html" -Pattern "route === ..(gameResult|gameReport|guestApps|referee)."
# → 위 1, 3, 6 = 0 매칭, 2, 4, 5 = 1+ 매칭이면 통과
```

→ Phase A 완료 보고: 6 케이스 ✓✓✓ ✓✓✓ + 변경 파일 목록.

---

## Phase B — 카피·토큰 룰 위반 일괄 수정

> **목표**: 룰 11 (전국 농구 매칭 플랫폼) + 룰 12 (placeholder) + 룰 12 (pill 9999px) 위반 0건.
> **예상**: 20분
> **에이전트**: developer 1

### B-1. 카피 룰 11 — "서울 3x3" 위반 8건

```
[프롬프트]
Dev/design/BDR v2.4/ 안의 "서울 3x3" 표현 8건 일괄 수정.

[유지 OK — 대회 명칭]
- data.jsx:23 "서울 3x3 오픈 챔피언십"  ← 대회명. 그대로 유지.
- screens/Series.jsx:5, screens/SeriesDetail.jsx:7, 144 "서울 3x3 오픈 챔피언십"  ← 대회 tagline. 유지.
- screens/Checkout.jsx:17 "서울 3x3 오픈 챔피언십 · 팀 참가비"  ← 대회명 일부. 유지.

[수정 필수 — 글로벌 카피]
- MyBDR.html:301 "서울 3x3 농구 커뮤니티 · 2026"  →  "전국 농구 매칭 플랫폼 · 2026"
- screens/Login.jsx:11 "서울 3x3 농구 커뮤니티"  →  "전국 농구 매칭 플랫폼"
- screens/Match.jsx:372 "서울 3x3 농구의 정수를 겨루는 오픈 챔피언십..."  →  "전국 농구 매칭 플랫폼이 매분기 개최하는 오픈 챔피언십..."

검증:
Select-String -Path 'Dev/design/BDR v2.4/MyBDR.html','Dev/design/BDR v2.4/screens/Login.jsx','Dev/design/BDR v2.4/screens/Match.jsx' -Pattern "서울 3x3 농구 커뮤니티|서울 3x3 농구의"
→ 0 매칭 기대 (대회 tagline 외 글로벌 카피 0)
```

### B-2. 카피 룰 11 — "다음카페" 표현 3건

```
[프롬프트]
Dev/design/BDR v2.4/ 안의 "다음카페" 표현 3건 수정 (운영진 / 사용자 출신 박제 금지).

- components.jsx:280 "다음카페에서 이관하신 분들을 위해..."  →  "익숙한 라이트 모드와 새로운 다크 모드 두 가지를 준비했어요..."
- components.jsx:285 "추천 · 기존 다음카페 분위기"  →  "추천 · 익숙한 게시판 톤"
- screens/More.jsx:41 "다음카페 시절부터 20년을 이어온 서울 3x3 농구 커뮤니티"  →  "20년의 농구 커뮤니티 경험을 이어가는 전국 농구 매칭 플랫폼"
- community-data.jsx:23 게시판 공지 제목에 "다음카페"  →  "이관하신 회원분들" 같이 일반화

검증:
Select-String -Path 'Dev/design/BDR v2.4' -Pattern "다음카페" -Recurse
→ 0 매칭 기대
```

### B-3. 카피 룰 12 — placeholder "예) " 시작

```
[프롬프트]
Dev/design/BDR v2.4/screens/SeriesCreate.jsx:41 의 placeholder 수정:

기존: placeholder="예) 서울 3x3 오픈 챔피언십"
수정: placeholder="대회 슬로건"

다른 screens/ 안 placeholder도 일괄 검색 후 "예) ", "예: ", 5단어 초과 위반 정리:
Select-String -Path 'Dev/design/BDR v2.4/screens/*.jsx' -Pattern 'placeholder="예[\) :]' -Recurse
```

### B-4. 토큰 룰 12 — pill 9999px 금지

```
[프롬프트]
Dev/design/BDR v2.4/tokens.css:544, 555 의 border-radius: 999px 수정.

- 두 줄 모두 4px (버튼 표준) 으로 변경
- 또는 8px (카드 표준) 으로 — 어떤 컴포넌트에 적용된 건지 확인 후 결정
- pill 9999px / 999px / 50% 모두 룰 12 위반

검증:
Select-String -Path 'Dev/design/BDR v2.4/tokens.css','Dev/design/BDR v2.4/responsive.css' -Pattern "border-radius:\s*(9999|999)px"
→ 0 매칭 기대
```

### B-5. Phase B 완료 검수

```powershell
$base = "Dev/design/BDR v2.4"

# 카피 룰 11
Get-ChildItem -Path "$base" -Recurse -Include *.jsx,*.html,*.md | Select-String -Pattern "서울 3x3 농구 커뮤니티|다음카페" | Select-Object Path,LineNumber,Line

# 카피 룰 12
Get-ChildItem -Path "$base/screens" -Include *.jsx | Select-String -Pattern 'placeholder="예[\) :]'

# 토큰 룰 12
Select-String -Path "$base/tokens.css","$base/responsive.css" -Pattern "border-radius:\s*(9999|999)px"
```

→ 모두 0 매칭이면 Phase B 통과. v2.4 시안 자체 13 룰 모두 통과 상태.

---

## Phase C — P0 박제: /profile (마이페이지 hub)

> **목표**: `Dev/design/BDR v2.4/screens/Profile.jsx` 톤을 `src/app/(web)/profile/page.tsx` 에 박제.
> **예상**: 2시간
> **에이전트**: planner 1 → developer 1 → tester 1 (직렬)

### C-0. 사전 보존 — 절대 변경 X

```
[프롬프트]
다음은 박제 시 0 변경. UI 톤만 v2.4 적용.

- /profile 라우트 + 9 서브 라우트 (page.tsx 위치)
- getWebSession + Promise.all 7쿼리 병렬 fetch 패턴
- /api/web/me + /api/web/profile 응답 형식 (snake_case)
- 응답 키 자동 snake_case 변환 (apiSuccess() / apiError())

박제 전 확인:
- src/app/(web)/profile/page.tsx 의 데이터 fetch 부분 (서버 컴포넌트)
- src/app/api/web/me/route.ts / src/app/api/web/profile/route.ts 의 응답 키
- 프론트 인터페이스에서 snake_case 접근자 사용하는지 (errors.md 2026-04-17 — 5회 재발 사일런트 undefined 버그)
```

### C-1. Plan (planner 에이전트)

```
[프롬프트]
@planner — /profile (Profile.jsx) 박제 계획 수립.

입력:
- 시안: Dev/design/BDR v2.4/screens/Profile.jsx
- 운영: src/app/(web)/profile/page.tsx
- 시안 의도: README.md §1 — Profile.jsx 가 hub 역할로 재구성, 4 sub 시안 흡수 (Bookings/Complete/Growth/WeeklyReport)
- 보존: getWebSession + 7쿼리 + /api/web/me|profile

출력:
1. UI 변경 영역 매트릭스 (어느 섹션이 어떻게 바뀌는지)
2. 데이터 fetch 매핑 (시안의 각 섹션 → 운영의 어느 API)
3. 영향 분석 (hooks 변경 / 컴포넌트 분리 필요 / 새 _components 파일)
4. 위험 요소 (snake_case ↔ camelCase / null 처리 / 다이나믹 라우트)
5. 단계별 작업 리스트 (각 ≤30분 단위)

architecture.md / decisions.md 갱신 필요한 항목 식별.
```

### C-2. Implement (developer 에이전트)

```
[프롬프트]
@developer — Phase C-1 의 plan 대로 /profile 박제 구현.

작업 룰:
- 시안 v2.4/screens/Profile.jsx 의 JSX 구조를 src/app/(web)/profile/page.tsx 의 본문에 적용
- 시안의 데이터 placeholder (예: const profile = {...}) 는 운영의 props / API 응답으로 교체
- 시안 클래스명 → 토큰 변수 (var(--*)) / Tailwind 클래스 또는 v2.4 tokens.css 와 일치하는 클래스
- AppNav 는 src/components/bdr-v2/app-nav.tsx 를 03 frozen 으로 동기화 (Phase 19 — 쪽지 추가)

⚠️ 신규 필드 추가 / API 변경 시 curl 1회로 raw 응답 확인 (errors.md 2026-04-17 룰):
  curl -s http://localhost:3001/api/web/me | jq .
  curl -s http://localhost:3001/api/web/profile | jq .

수정 후 npm run lint && npm run typecheck 통과 확인.
```

### C-3. Test (tester 에이전트)

```
[프롬프트]
@tester — /profile 박제 결과 검증.

체크 항목:
1. npm run dev (port 3001) 후 브라우저에서 /profile 진입 — 본인 데이터 표시
2. AppNav 6 케이스 자동 검수 통과 (위 Phase A-6 grep)
3. 마이페이지 hub 4 섹션 표시 확인 (다가오는 일정 / 시즌 스탯 / 최근 활동 / 프로필 편집 진입)
4. 모바일 720px 분기 정상
5. snake_case 응답 키 정상 표시 (필드 undefined 없음)
6. /profile/bookings, /profile/growth, /profile/weekly-report, /profile/complete 깊은 페이지 진입 동작
7. Vercel 미리보기 (https://mybdr-git-dev-mybdr.vercel.app/profile) 으로 dev 머지 후 검증

실패 시 → debugger 에이전트 호출 + errors.md 갱신.
```

### C-4. Phase C 완료 — 커밋 + PR

```bash
git add src/app/(web)/profile src/components/bdr-v2/app-nav.tsx
git commit -m "feat(profile): apply v2.4 mypage hub design (Phase 19 AppNav)

- /profile 본문 4섹션 hub 재구성 (Profile.jsx 박제)
- AppNav 03 frozen 코드 적용 (쪽지 아이콘 추가 — Phase 19)
- API/data fetch 0 변경 (getWebSession + 7쿼리 보존)
- 자동 검수 6 케이스 ✓"

git push origin subin
# GitHub 에서 subin → dev PR
```

knowledge 갱신:
- architecture.md — 마이페이지 hub 구조 변경
- decisions.md — Phase 19 채택 + v2.4 박제 결정
- conventions.md — AppNav frozen 카피 패턴 (재발 방지)

---

## Phase D — P1 박제: /profile/edit, /settings, /billing, /activity

> **목표**: 마이페이지 hub 의 4개 깊은 페이지 박제.
> **예상**: 4 페이지 × 1~1.5h = 4~6h
> **에이전트**: developer 4명 병렬 가능 (페이지간 독립적)

### D-1. /profile/edit (EditProfile.jsx)

```
[프롬프트]
@developer-1 (병렬) — /profile/edit 박제.

입력: Dev/design/BDR v2.4/screens/EditProfile.jsx
운영: src/app/(web)/profile/edit/page.tsx + src/app/api/web/profile/route.ts (PATCH)
보존: PATCH 응답 형식 / nickname 변경 시 JWT 재발급 패턴 (mypage-feasibility §E-5 참조)

체크:
- 폼 필드 1:1 매핑 (nickname / position / region / experience 등)
- snake_case 접근자
- nickname 변경 시 JWT 재발급 검증 (referee 계정 1명 + 비-referee 1명 — mypage-feasibility §E-4)
- 모바일 iOS 16px input 룰 (룰 13)
```

### D-2. /profile/settings (Settings.jsx)

```
[프롬프트]
@developer-2 (병렬) — /profile/settings 박제.

입력: Dev/design/BDR v2.4/screens/Settings.jsx (가입 위저드 흡수 — README §1)
운영: src/app/(web)/profile/settings/page.tsx
보존: 알림 설정 / 안전 설정 / 차단 목록 / 비밀번호 변경 API

체크:
- 4 섹션 (계정 / 알림 / 안전 / 비밀번호)
- ProfileCompletePreferences.jsx 흡수 (README 제거 시안 11건 중 1건)
- 폼 dirty 추적 + 저장 후 toast
```

### D-3. /profile/billing (Billing.jsx)

```
[프롬프트]
@developer-3 (병렬) — /profile/billing 박제.

입력: Dev/design/BDR v2.4/screens/Billing.jsx
운영: src/app/(web)/profile/billing/page.tsx
보존: 결제 내역 fetch (Toss API) / 구독 상태 / 환불 정책

체크:
- 결제 내역 / 구독 상태 / 환불 / 영수증 4 섹션
- ?tab=payments 쿼리로 결제 탭 진입 (PricingSuccess.jsx → /profile/billing?tab=payments 사용)
- snake_case 응답 키
```

### D-4. /profile/activity (MyActivity.jsx)

```
[프롬프트]
@developer-4 (병렬) — /profile/activity 박제.

입력: Dev/design/BDR v2.4/screens/MyActivity.jsx
운영: src/app/(web)/profile/activity/page.tsx
보존: 활동 로그 fetch (게임 참가 / 글 작성 / 코트 예약 / 팀 가입)

체크:
- 무한 스크롤 또는 페이지네이션
- 활동 타입별 필터 칩
- 빈 상태 (활동 0개) UI
```

### D-5. Phase D 완료

각 developer 마다:
```bash
git add src/app/(web)/profile/<영역>
git commit -m "feat(profile/<영역>): apply v2.4 design"
git push origin subin
```

GitHub 에서 subin → dev PR 4개 또는 1개 통합 PR.

---

## Phase E — 최종 자동 검수 + 작업 마감

### E-1. 13 룰 회귀 자동 검수 (전체)

```powershell
# 시안 측 (Dev/design/BDR v2.4)
$design = "Dev/design/BDR v2.4"
Write-Host "=== 시안 6 케이스 ==="
Select-String -Path "$design/components.jsx" -Pattern "더보기 ▼|RDM rdm_captain"
Select-String -Path "$design/components.jsx" -Pattern 'btn btn--sm" title="(검색|쪽지|알림)"'
Select-String -Path "$design/components.jsx" -Pattern "Icon\.mail"   # 1+ 기대

# 운영 측 (src/components/bdr-v2/app-nav.tsx)
$prod = "src/components/bdr-v2/app-nav.tsx"
Write-Host "=== 운영 헤더 검수 ==="
Select-String -Path "$prod" -Pattern "더보기 ▼|RDM rdm_captain"
Select-String -Path "$prod" -Pattern "Icon\.mail|/messages"   # 1+ 기대

# 카피 / 토큰
Write-Host "=== 카피 / 토큰 검수 ==="
Get-ChildItem -Path "$design" -Recurse -Include *.jsx,*.html,*.md | Select-String -Pattern "서울 3x3 농구 커뮤니티|다음카페"
Select-String -Path "$design/tokens.css","$design/responsive.css" -Pattern "border-radius:\s*(9999|999)px"
```

### E-2. 06-self-checklist.md 통과 확인

```
[프롬프트]
Dev/design/BDR v2.4/uploads/06-self-checklist.md 의 모든 항목 ✅ 검수.
- §1 AppNav 7 룰 (Phase 19 갱신)
- §2 더보기 5그룹 IA
- §3 카피 / 모바일
- §4 디자인 토큰
- §5 자동 검수 grep

검수 결과 보고 — 모든 항목 ✅ 통과 vs 위반 N건 (위치 + 사유).
```

### E-3. PM 작업 완료 체크리스트 (CLAUDE.md §)

```
□ 작업 로그 1줄 (.claude/scratchpad.md)
□ scratchpad 100줄 이내
□ 에러 → .claude/knowledge/errors.md
□ 30분+ 삽질 → lessons.md
□ 기술 결정 → decisions.md (Phase 19 채택 + v2.4 박제)
□ 새 패턴 → conventions.md (AppNav frozen 카피 의무)
□ 구조 변경 → architecture.md (마이페이지 hub)
□ index.md 갱신
□ 미푸시 커밋 알림
```

### E-4. 커뮤니케이션 — 사용자 (수빈) 보고

```
✅ v2.4 박제 완료 보고

Phase A: 시안 자체 13 룰 통과 (6 케이스 ✓)
Phase B: 카피 / 토큰 위반 0건
Phase C: /profile (마이페이지 hub) 박제 — Vercel 프리뷰 OK
Phase D: /profile/{edit,settings,billing,activity} 박제 — 4 PR 머지 대기
Phase E: 06-self-checklist 모두 ✅ + PM 체크리스트 완료

미푸시 커밋: N개
PR: github.com/bdr-tech/mybdr/pull/<번호>
다음 단계: dev → main 머지 (원영 작업)
```

---

## 부록 — 위반 / 막힘 발생 시 대응

### 시안 자체 룰 위반 (Phase A 검수 실패)

```
[보고 형식]
🚨 v2.4 시안 룰 위반 발견

위반 # | 룰 | 위치 | 내용 | 권장 수정
1     | A3 | components.jsx:206 | "더보기 ▼" dropdown trigger | 03 §1 코드 카피
...

PM 결정 필요 항목:
- N건 (사용자 결정 §X 충돌)

계속 진행 vs PM 답변 대기 결정 부탁.
```

### API / 데이터 미스매치 (Phase C/D 박제 중)

```
[보고 형식]
🚨 박제 블로커

영역: /profile
시안: Profile.jsx 의 "다가오는 일정" 섹션
운영: /api/web/profile 응답에 upcoming_bookings 키 없음

옵션:
A. /api/web/profile 응답에 키 추가 (route.ts + 프론트 인터페이스 동시 변경)
B. 시안 섹션 제거 (마이페이지 hub 의도와 충돌)
C. 별도 API /api/web/profile/upcoming 신규

권장: A (마이페이지 hub 의도 보존). 다만 errors.md 2026-04-17 룰 따라 curl 1회로 raw 검증 필수.
어느 옵션으로 진행?
```

### 30분+ 삽질 — lessons.md 기록

```
.claude/knowledge/lessons.md 추가:

## YYYY-MM-DD: <삽질 제목>

**Context**: 어떤 작업 중 어떤 문제 발생
**Hypothesis tried**: 시도한 가설들
**Root cause**: 진짜 원인
**Fix**: 어떻게 해결
**Why hard to find**: 왜 30분+ 걸렸는지
**Prevention**: 다음에 어떻게 피할지
```

---

## 참고 — 에이전트 호출 룰 (CLAUDE.md §⚡)

| 작업 | 에이전트 | 사유 |
|------|---------|------|
| 파일 1~2개 + grep 1회 | PM 직접 | conventions.md |
| 파일 3개+ 검색, 키워드 애매 | Explore | 검색 비용 |
| 구조적 결정, 영향 분석 | planner | architecture / decisions |
| 2파일+ 연계 or 비즈니스 로직 | developer | 구현 |
| 에러 추적 | debugger | errors.md |
| 통합 검증 | tester | E2E |

병렬 실행 가능 시 동시 호출 (예: Phase D 의 4 developer).

---

## 출처

- Phase 19 룰 변경: `Dev/design/BDR v2.4/uploads/03-appnav-frozen-component.md`
- 박제 우선순위: `Dev/design/BDR v2.3/README.md` §3 (P0/P1)
- 마이페이지 hub 의도: `Dev/design/mypage-feasibility-and-design-brief-2026-05-01.md`
- 메모리 회귀 방지: `memory/design_appnav_frozen_violations.md` (위반 6건 — Phase 19)
- 작업 룰: `mybdr/CLAUDE.md` §디자인 / §보안 / §⚡
- 에이전트 룰: `.claude/knowledge/conventions.md`
