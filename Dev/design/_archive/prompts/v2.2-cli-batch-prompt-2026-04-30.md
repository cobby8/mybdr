# BDR v2.2 박제 CLI 일괄 위임 프롬프트 — 2026-04-30

> **사용법**: 아래 § "프롬프트 (복사 시작)" 부터 § "프롬프트 (복사 끝)" 까지 통째로 복사해서 클로드 CLI 에 붙여넣기.
> **시안 위치**: `Dev/design/BDR v2.2/Dev/design/BDR v2.2/` (박제 작업 1단계에서 정리)
> **총 작업량**: 폴더 정리 + globals.css G-10 이식 + 11 페이지 박제 = 약 13~17시간 / 2-3 작업일
> **CLI 세션 권장**: 토큰 부족 시 P0 (Step 3 — 4건) 만 먼저 끝내고 P1/P2 는 다음 세션

---

## 분할 실행 옵션

| 옵션 | 단계 | 견적 | 비고 |
|------|------|------|------|
| A — 한 세션 일괄 | Step 1~6 전체 | 13~17h | Max Ultimate 권장 |
| **B — 배치 분할 (권장)** | 세션 1: Step 1~3 (P0) | 5~7h | Max Expanded OK |
| | 세션 2: Step 4 (P1) | 5~7h | |
| | 세션 3: Step 5 (P2/P3) | 3~4h | |
| C — 1 페이지씩 | Step 별로 1 페이지 | 1~2h/페이지 | Pro 플랜 |

---

## 프롬프트 (복사 시작)

# BDR v2.2 박제 일괄 위임 — Phase 11

너는 BDR(MyBDR — 농구 매칭 플랫폼) 의 시니어 풀스택 엔지니어다. 이번 세션에서는 BDR v2.2 시안을 사이트에 박제하는 일괄 작업을 진행한다. 11 페이지 + 인프라 작업이 포함되며, 사용자 결정 영구 보존 + 회귀 0 + 모바일 일관성을 모두 만족해야 한다.

---

## 0. 사전 준비 (필수)

### 0-1. "오늘 작업 시작하자" 체크리스트

```bash
cd "C:\0. Programing\mybdr"

git remote -v                    # github.com/bdr-tech/mybdr
git fetch origin --prune
git branch --show-current        # subin 인지 확인
git log --oneline -5
git status --short              # .claude/* 외에 src/ modified 없는지

# .env 확인 (값 노출 금지)
grep "DATABASE_URL" .env | grep -i "dev\|local"  # 개발 DB여야 함
ls .env.local                    # localhost:3001 오버라이드 확인
```

이 결과를 PM 에게 1줄 보고 후 진행 OK 신호 받기. **승인 전 파일 수정 / 브랜치 전환 / 머지 / 커밋 금지**.

### 0-2. 핵심 보호 룰 (절대 위반 금지)

이 작업 전체에 적용되는 사용자 결정 영구 보존 룰. 출처: `Dev/design/user-design-decisions-2026-04-30.md` + `Dev/design/v2.2-review-2026-04-30.md`.

**룰 1 — 헤더 (AppNav)**:
- AppNav 글로벌 헤더 단일화. 페이지 안에 별도 nav 추가 금지.
- utility bar 우측 (계정/설정/로그아웃) 모바일에서도 표시 유지.
- main bar 우측: 검색 + 알림 + 다크모드 토글 + 햄버거. 더보기 드롭다운 트리거 / 계정 아이콘+닉네임 추가 금지.
- 다크모드 — 데스크톱 두 라벨 / 모바일 단일 아이콘 (하이브리드 유지).

**룰 2 — 더보기 메뉴**:
- 5그룹 IA 보존 (내 활동 / 경기·대회 / 등록·예약 / 둘러보기 / 계정·도움)
- 가짜링크 추가 금지 — `gameResult / gameReport / guestApps` 영구 제외.
- `referee → refereeInfo` 라우트명 사용 (RefereeInfo 옵션 B).

**룰 3 — 팀 페이지**:
- 팀 카드 목록: 로고 + 팀명 + 창단년도 + 상세 보기만. 레이팅 / 승 / 패 / 매치신청 노출 금지.
- 팀 상세 hero: 레이팅 stat 노출 금지.
- 팀 생성: 4스텝 + 홈/어웨이 색상 분리 + 자유 컬러피커 + 로고 업로드 기본.

**룰 4 — 프로필**:
- ProfileSideNav 모바일 nav 제거 유지 (PC 사이드바만).
- /profile/settings 메뉴 — 이모지 아이콘 (👤🏀🔔🔒💳⚠️). Material Symbols 로 되돌리지 말 것.
- /settings 더미 페이지 → /profile/settings redirect 유지 (640줄 박제 복원 금지).

**룰 5 — 글로벌 카피**:
- "전국 농구 매칭 플랫폼" 슬로건. "서울 3x3 한정" 표현 금지.
- About 페이지 운영진 — 일반 라벨 (실명 박제 금지).

**룰 6 — 모바일**:
- 720px 통일 브레이크포인트.
- iOS 자동 줌 차단 (input/select/textarea font-size 16px).
- 버튼 44px 터치 타겟.
- 인라인 grid `repeat(N, 1fr)` 추가 시 720px 분기 필수.

**룰 7 — DB 미지원 기능**:
- UI 자리만 박제 + "준비 중" + scratchpad 추후 구현 큐.
- alert("준비 중") 신규 추가 시 라우트 존재 여부 점검 필수 (라우트 있으면 alert 금지, Link 사용).

**룰 8 — 디자인 토큰**:
- 하드코딩 hex 금지 → `var(--color-*)`.
- lucide-react 금지 → Material Symbols.
- 버튼 border-radius 4px (pill 9999px 금지).
- 핑크/살몬/코랄 금지.

각 단계 작업 시 이 룰 위반하지 않는지 자체 검수.

### 0-3. 작업 결과 보고 형식

각 단계 완료 시 PM 에게 1줄 보고:
```
✅ Step N — <작업명> 완료. 커밋 X건 (예: <hash> <subject>). 다음 Step 진행 OK?
```

PM 답변 받고 다음 Step 진행. 단 명시적 승인 없이 자체 진행 가능한 단계는 (1, 2, 6 — 단순 작업) 만 자동 진행.

---

## 1. Step 1 — 폴더 구조 정리 (5분)

### 1-1. 현황

```
Dev/design/BDR v2.2/                    ← 외부 (v2.1 카피만 — 옛 상태)
└── Dev/design/BDR v2.2/                ← 진짜 작업물 (정정 적용된 최신)
    ├── README.md
    ├── components.jsx                  (가짜링크 3건 제거 + refereeInfo)
    ├── responsive.css                  (G-10 + G-9 보호 859줄)
    ├── MyBDR.html                      (라우트 11 신규)
    ├── _mobile_audit.html
    ├── extras-data.jsx
    ├── tokens.css
    └── screens/                        (83개 = v2.1 72 + 신규 11)
```

### 1-2. 작업

```bash
cd "C:\0. Programing\mybdr\Dev\design"

# 1. 외부 BDR v2.2/ 의 옛 파일들을 nested 의 최신 파일로 덮어쓰기
cp -rv "BDR v2.2/Dev/design/BDR v2.2/"* "BDR v2.2/"

# 2. 검증
ls "BDR v2.2/screens/" | wc -l          # 83 기대
cat "BDR v2.2/README.md" | head -3      # README 노출 확인
grep -c "refereeInfo" "BDR v2.2/components.jsx"  # 1+ 기대 (라우트명 변경됨)
grep -cE "id:\s*'(guestApps|gameResult|gameReport)\b'" "BDR v2.2/components.jsx"  # 0 기대

# 3. nested Dev/ 폴더 삭제
rm -rf "BDR v2.2/Dev"

# 4. 최종 검증 — 단일 경로 구조
ls "BDR v2.2/" | head -20
```

### 1-3. 커밋

```bash
cd "C:\0. Programing\mybdr"
git add "Dev/design/BDR v2.2/"
git commit -m "chore(design): BDR v2.2 시안 폴더 구조 정리 (nested 병합)

zip 압축 풀 때 이중 중첩되었던 Dev/design/BDR v2.2/Dev/design/BDR v2.2/ 를
단일 경로 Dev/design/BDR v2.2/ 로 병합. 신규 작업물(README/components/responsive/
신규 11 시안)을 외부로 이동, nested 폴더 삭제.

근거: Dev/design/v2.2-review-2026-04-30.md §3"
```

### 1-4. 보고

```
✅ Step 1 — 폴더 정리 완료. Dev/design/BDR v2.2/ 단일 경로로 병합. 다음 Step 진행 OK?
```

---

## 2. Step 2 — responsive.css G-10 + G-9 보호 사이트 이식 (30분)

### 2-1. 작업

`Dev/design/BDR v2.2/responsive.css` 의 마지막 블록 (v2.2 — G-9 보호 + G-10 가로 스크롤) 을 `src/app/globals.css` 에 이식.

#### 추출 명령
```bash
cd "C:\0. Programing\mybdr"

# v2.2 신규 룰 추출 (G-10 시작 마커부터 끝까지)
sed -n '/v2.2 — G-9 보호 + G-10 가로 스크롤/,$p' "Dev/design/BDR v2.2/responsive.css" > /tmp/v2.2-additions.css
wc -l /tmp/v2.2-additions.css      # 약 60-80줄 기대
```

#### 사이트 globals.css 에 삽입

`src/app/globals.css` 의 라인 1450 근처 (기존 [v2.1 P0-1 G-1] data-table 룰 아래) 에 다음 블록 추가:

```css
/* ============================================================
   [v2.2 G-9 보호 + G-10] 큰 N열 표 + 사용자 의도 다열 보호
   v2.1 매처가 잡지 못한 패턴 + 사용자 의도 데이터 비교는 가로 유지.
   출처: Dev/design/BDR v2.2/responsive.css
   ============================================================ */

@media (max-width: 720px) {
  /* G-9 보호: scoreboard / data-keep-cols 모바일 다열 유지 */
  .page .scoreboard,
  .page [data-keep-cols] {
    grid-template-columns: 1fr auto 1fr !important;
    text-align: initial !important;
    gap: 12px !important;
  }
  .page .scoreboard > div,
  .page [data-keep-cols] > div {
    text-align: initial !important;
  }

  /* G-10: 큰 N열(7~14) repeat() grid → 가로 스크롤 보존 */
  .page div[style*="grid-template-columns: repeat(7"]:not([data-keep-cols]),
  .page div[style*="grid-template-columns: repeat(8"]:not([data-keep-cols]),
  .page div[style*="grid-template-columns: repeat(9"]:not([data-keep-cols]),
  .page div[style*="grid-template-columns: repeat(10"]:not([data-keep-cols]),
  .page div[style*="grid-template-columns: repeat(11"]:not([data-keep-cols]),
  .page div[style*="grid-template-columns: repeat(12"]:not([data-keep-cols]),
  .page div[style*="grid-template-columns: repeat(13"]:not([data-keep-cols]),
  .page div[style*="grid-template-columns: repeat(14"]:not([data-keep-cols]) {
    overflow-x: auto !important;
    -webkit-overflow-scrolling: touch;
  }

  /* G-10b: signup positions (5열) → 3열 */
  .page div[style*="grid-template-columns: repeat(5, 1fr)"]:not([data-keep-cols]):not(.onb__body):not(.drawer__body) {
    grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
    gap: 8px !important;
  }

  /* G-10c: 6열 ranking 표 → 가로 스크롤 */
  .page div[style*="grid-template-columns: repeat(6, 1fr)"]:not([data-keep-cols]):not(.onb__body) {
    overflow-x: auto !important;
    -webkit-overflow-scrolling: touch;
  }

  /* G-10d: 비교 표 (safety/pricing) — 첫 열 고정 + 가로 스크롤 */
  .page .compare-table[data-cols] {
    overflow-x: auto !important;
    -webkit-overflow-scrolling: touch;
    border: 1px solid var(--border);
    border-radius: 8px;
  }
  .page .compare-table[data-cols] > div {
    min-width: 720px;
  }
}
```

### 2-2. 검증

```bash
# 라인 수 변화
wc -l src/app/globals.css       # 1782 → 약 1830~1850 기대

# 회귀 방지 스크립트 통과
npm run check:design 2>&1 | tail -5

# 모바일 viewport 시각 확인 (Chrome DevTools 390x844)
# - /live/[id] tab-players: 14열 → 가로 스크롤 (글자 짜부러짐 X)
# - /stats: 8/11열 → 가로 스크롤
# - /rankings: 6/8열 → 가로 스크롤
```

### 2-3. 커밋

```bash
git add src/app/globals.css
git commit -m "fix(mobile): G-10 큰 N열 표 + G-9 보호 가드 (v2.2)

v2.1 매처가 잡지 못한 패턴 일괄 처리:
- repeat(7~14) → 가로 스크롤 (live tab-players, stats 8/11열)
- repeat(5, 1fr) signup positions → repeat(3) (모바일)
- repeat(6, 1fr) ranking → 가로 스크롤
- .compare-table[data-cols] safety/pricing 비교 표
- .scoreboard / [data-keep-cols] 사용자 의도 다열 유지 (G-9 보호)

근거: Dev/design/BDR v2.2/responsive.css 마지막 블록"
```

### 2-4. 보고

```
✅ Step 2 — responsive.css G-10 사이트 이식 완료. 모바일 깨짐 7+ 케이스 해소. 다음 Step 진행 OK?
```

---

## 3. Step 3 — 🔴 P0 박제 4건 (4-6시간)

### 3-1. P0-1: PostEdit (`/community/[id]/edit`) — 1.5h

#### 시안 출처
`Dev/design/BDR v2.2/screens/PostEdit.jsx`

#### 사이트 페이지
`src/app/(web)/community/[id]/edit/page.tsx`

#### 박제 룰
- 시안의 JSDoc 헤더 매트릭스 그대로 유지 (page.tsx 상단 코멘트로 이식)
- 시안 JSX 구조 그대로 박제하되 데이터 fetching 은 사이트 기존 방식 (server component + prisma + getWebSession)
- 권한 체크: 본인이 작성자가 아니면 시안의 noPermission view 표시
- 진입점: `/community/[id]` 본인 글의 점-3개 dropdown 에서 "수정" 항목 (이미 있으면 그대로, 없으면 추가)
- 복귀: 저장 → `/community/[id]` (router.push), 취소 → router.back()

#### 작업 단계
```bash
# 1. 시안 읽기
cat "Dev/design/BDR v2.2/screens/PostEdit.jsx"

# 2. 사이트 기존 페이지 읽기
cat src/app/\(web\)/community/\[id\]/edit/page.tsx

# 3. 박제 — 시안 JSX → page.tsx + 분할 컴포넌트 (필요 시 _components/)
# 4. data fetching — 기존 방식 유지 (server prisma 호출 등)
# 5. tsc --noEmit 통과 확인
# 6. 모바일 시각 확인 — /community/[id]/edit (테스트 글)
```

#### 검수 매트릭스 (page.tsx 헤더 코멘트로 이식 필수)
| 기능 | v1 | v2.2 | 진입점 | 모바일 |
|------|-----|------|--------|--------|
| 본문 수정 | ✅ | ✅ textarea prefill | dropdown | 1열 |
| 사진 추가/삭제 | ✅ | ✅ 첨부 리스트 | dropdown | 가로 hscroll |
| 비밀글 토글 | ✅ | ✅ checkbox | dropdown | OK |
| 권한 체크 | ✅ | ✅ noPermission view | - | OK |

#### 커밋
```bash
git commit -m "feat(design-v2): /community/[id]/edit Phase 11 박제 (PostEdit.jsx)

시안 출처: Dev/design/BDR v2.2/screens/PostEdit.jsx (D등급 P0-1)
박제 등급: A (시안 1:1 + 데이터 fetching 보존)
검수 매트릭스: 본문/사진/비밀글/권한 4 통과

진입: /community/[id] dropdown '수정'
복귀: 저장/취소 → /community/[id]"
```

### 3-2. P0-2: GameEdit (`/games/[id]/edit`) — 1.5h

#### 시안 출처
`Dev/design/BDR v2.2/screens/GameEdit.jsx`

#### 사이트 페이지
`src/app/(web)/games/[id]/edit/page.tsx`

#### 박제 룰 (시안 §5-2-2 Edge cases 정확히 반영)
- `applicantCount` 0 → 모든 필드 편집 가능
- `applicantCount ≥ 1` → 인원·회비·날짜 잠금 + 안내 배너
- 경기 시작 < 24시간 → "취소만 가능" + danger 카드
- `gameStatus === 'finished'` → noEdit view + /live/[id] 또는 /games/[id]/report 진입
- 호스트 아니면 noPermission view + /games/[id] 복귀

#### 검수 매트릭스
| 기능 | v1 | v2.2 | 진입점 | 모바일 |
|------|-----|------|--------|--------|
| 기본 정보 수정 | ✅ | ✅ prefill + 잠금 | host btn | 1열 |
| 신청 조건 수정 | ✅ | ✅ checkbox 그룹 | host btn | 2→1열 |
| 경기 취소 | ✅ | ✅ 위험 액션 카드 | host btn | OK |
| 호스트 권한 체크 | - | ✅ noPermission view | - | OK |

#### 커밋
```bash
git commit -m "feat(design-v2): /games/[id]/edit Phase 11 박제 (GameEdit.jsx)

시안 출처: Dev/design/BDR v2.2/screens/GameEdit.jsx (D등급 P0-2)
Edge cases: applicantCount 잠금 + finished noEdit + 24h 이내 취소만
검수 매트릭스: 기본/조건/취소/권한 4 통과

진입: /games/[id] '경기 수정' 버튼 (호스트 본인)
복귀: 저장/취소 → /games/[id]"
```

### 3-3. P0-3: LiveResult — `/live/[id]` finished 회귀 픽스 (1.5h)

#### 시안 출처
`Dev/design/BDR v2.2/screens/LiveResult.jsx`

#### 사이트 페이지
`src/app/live/[id]/page.tsx` 의 `gameStatus === 'finished'` 분기

#### 박제 룰 (의뢰서 §5-3 — 회귀 픽스)
**시안 룰 (절대 준수)**:
- 색/폰트만 v2 유지
- 레이아웃·기능은 옛 디자인 복원

옛 디자인 핵심 (시안 JSDoc 명시):
- 큰 스코어보드 (.scoreboard 클래스 사용 → G-9 보호 자동 적용)
- 쿼터별 점수 (qscore, 가로 스크롤)
- MVP 표시
- 평가 진입점 (/games/[id]/report 링크)
- "기록 보기" 액션 (/profile/activity 링크)

#### 회귀 검수 매트릭스
| 기능 | v1 (옛) | v2.2 | 진입점 | 모바일 |
|------|---------|------|--------|--------|
| FINAL 스코어보드 | ✅ | ✅ GameResult 활용 | 라이브 종료 | qscore 가로 |
| 쿼터별 점수 | ✅ | ✅ qscore 보존 | - | hscroll |
| MVP 표시 | ✅ | ✅ MVP 카드 | - | OK |
| 경기 평가 진입 | ✅ | ✅ /games/[id]/report Link | finished | OK |
| 기록 보기 | ✅ | ✅ /profile/activity Link | finished | OK |

#### 커밋
```bash
git commit -m "fix(live): /live/[id] finished 분기 회귀 픽스 (P0-5)

시안 출처: Dev/design/BDR v2.2/screens/LiveResult.jsx (D등급 P0-3 회귀)
시안 룰: 색/폰트만 v2 유지 + 레이아웃·기능 옛 디자인 복원
.scoreboard 클래스 사용 → G-9 보호 모바일 다열 유지

검수 매트릭스: FINAL/쿼터/MVP/평가/기록 5 통과
진입: /live/[id] 경기 종료 자동
복귀: /games/[id] / /games/my-games / /live"
```

### 3-4. P0-4: ProfileComplete (`/profile/complete`) + verify 자동 redirect (1.5h)

#### 시안 출처
`Dev/design/BDR v2.2/screens/ProfileComplete.jsx`

#### 사이트 페이지 (2개 작업)
1. `src/app/(web)/profile/complete/page.tsx` 박제
2. `src/app/(web)/verify/page.tsx` — verify 직후 `/profile/complete` 자동 redirect 추가

#### 박제 룰
- OnboardingV2 의 단계형 폼 패턴 (M5 압축 — 4 step)
- 4 step: 포지션 / 키 / 활동 지역 / 사진
- 진행바 + step 카드 + next/back/skip 버튼
- 스킵 가능 (skip → /home)
- 완료 시 → /onboarding/setup (전체 온보딩) 또는 / (이미 마쳤으면)

#### verify redirect 작업
`src/app/(web)/verify/page.tsx` 의 인증 성공 핸들러:
```typescript
// AS-IS:
router.push("/");

// TO-BE:
// 신규 가입자 (profile_completed = false) 면 /profile/complete 로
// 이미 완성된 사용자는 /onboarding/setup 또는 /
const session = await getWebSession();
const user = await prisma.user.findUnique({
  where: { id: BigInt(session.sub) },
  select: { profile_completed: true, onboarded_at: true }
});
if (!user.profile_completed) router.push("/profile/complete");
else if (!user.onboarded_at) router.push("/onboarding/setup");
else router.push("/");
```

> 단 prisma 의 `profile_completed`, `onboarded_at` 컬럼이 존재하는지 먼저 확인. 없으면 phase-9-future-features.md 큐 추가하고 단순 redirect 만 적용.

#### 검수 매트릭스
| 기능 | 옛 페이지 | v2.2 | 진입점 | 모바일 |
|------|----------|------|--------|--------|
| 4 step wizard | - | ✅ ProfileComplete | /verify auto | 1열 stack |
| 진행바 | - | ✅ progress bar | - | OK |
| skip 옵션 | - | ✅ "나중에" | - | OK |
| 완료 redirect | - | ✅ /onboarding/setup or / | - | - |

#### 커밋 (2개)
```bash
git commit -m "feat(design-v2): /profile/complete Phase 11 박제 (ProfileComplete.jsx)

시안 출처: Dev/design/BDR v2.2/screens/ProfileComplete.jsx (D등급 P0-4)
4 step wizard (포지션/키/지역/사진) + 진행바 + skip
완료 시 → /onboarding/setup or /home (DB 상태 분기)

진입: /verify auto / /profile 미완성 배너
복귀: 완료/스킵 → / or /onboarding/setup"

# verify redirect 별도 커밋
git commit -m "feat(verify): 신규 가입자 /profile/complete 자동 redirect

verify 직후 user.profile_completed 분기:
- false → /profile/complete (D등급 P0-4 박제)
- onboarded_at null → /onboarding/setup
- 모두 완료 → /

DB 컬럼 미존재 시 phase-9-future-features 큐로 처리"
```

### 3-5. Step 3 통합 검증

```bash
npm run typecheck 2>&1 | tail -5
npm run check:design 2>&1 | tail -10

# 4 페이지 모바일 시각 확인 (Chrome DevTools 390x844)
# - /community/[id]/edit (테스트 글로 진입)
# - /games/[id]/edit (호스트 계정으로 진입)
# - /live/[id] (finished 상태 테스트)
# - /verify → /profile/complete 자동 redirect 동작
```

### 3-6. Step 3 보고

```
✅ Step 3 — P0 박제 4건 완료. 5 커밋 (PostEdit, GameEdit, LiveResult, ProfileComplete, verify-redirect).
주요 임팩트: 글 수정/경기 수정/라이브 결과 회귀/신규 가입자 자동 동선.
P1 (Step 4) 진행 OK?
```

---

## 4. Step 4 — 🟠 P1 박제 4건 (5-7시간)

### 4-1. P1-1: ProfileGrowth (`/profile/growth`) — 1.5h

**시안**: `Dev/design/BDR v2.2/screens/ProfileGrowth.jsx`
**사이트**: `src/app/(web)/profile/growth/page.tsx`

**박제 룰**:
- 12주 spark line + 마일스톤 카드 + 레벨/XP hero
- DB 미구현 부분 (예: 12주 집계) 더미 + "준비 중" 배지
- profile-grid 1fr 모바일 (이미 globals.css 룰 적용됨)

**검수 매트릭스 (page.tsx 헤더 이식)**:
| 기능 | 옛 | v2.2 | 진입점 | 모바일 |
|------|----|------|--------|--------|
| 레벨·XP | Profile ✅ | ✅ hero | 본인 카드 | 1열 |
| 12주 추이 | - | ✅ spark | - | 가로 hscroll |
| 마일스톤 | - | ✅ 카드 grid | - | 1→2열 |

**커밋**:
```bash
git commit -m "feat(design-v2): /profile/growth Phase 11 박제 (ProfileGrowth.jsx)

시안 출처: Dev/design/BDR v2.2/screens/ProfileGrowth.jsx (D등급 P1-1)
12주 spark line + 마일스톤 카드 + 레벨/XP hero
DB 미구현 집계는 더미 + 준비 중 배지

진입: /profile '내 성장' 카드 / 더보기 '내 활동'
복귀: AppNav 뒤로 → /profile"
```

### 4-2. P1-2: ProfileBookings (`/profile/bookings`) — 1.5h

**시안**: `Dev/design/BDR v2.2/screens/ProfileBookings.jsx`
**사이트**: `src/app/(web)/profile/bookings/page.tsx`

**박제 룰**:
- 카테고리 탭 (전체/코트/토너먼트/게스트) + 상태 필터 칩 + 카드 리스트
- 진입점은 더보기 메뉴 "내 활동" 그룹 (사용자 결정 §1-3: 빌링 탭 X, 결제 영수증 혼동 방지)
- 항목 클릭 → 해당 상세 (`/courts/[id]`, `/tournaments/[id]`, `/games/[id]`)

**검수 매트릭스**:
| 기능 | 옛 | v2.2 | 진입점 | 모바일 |
|------|----|------|--------|--------|
| 카테고리 탭 | - | ✅ 4탭 | 더보기 | OK |
| 상태 필터 | - | ✅ 칩 | - | flex-wrap |
| 항목 클릭 | - | ✅ 상세 라우팅 | - | OK |

**커밋**:
```bash
git commit -m "feat(design-v2): /profile/bookings Phase 11 박제 (ProfileBookings.jsx)

시안 출처: Dev/design/BDR v2.2/screens/ProfileBookings.jsx (D등급 P1-2)
카테고리 탭 + 상태 필터 + 항목 라우팅

진입: 더보기 '내 활동' (빌링 탭 X)
복귀: 항목 → 상세 / 뒤로 → /profile"
```

### 4-3. P1-3: VenueDetail (`/venues/[slug]`) — 2h

**시안**: `Dev/design/BDR v2.2/screens/VenueDetail.jsx`
**사이트**: `src/app/(web)/venues/[slug]/page.tsx`

**박제 룰**:
- **공개 SEO 페이지** — 비로그인 열람 가능
- 메타데이터 보존 (Open Graph, JSON-LD `Place` / `SportsActivityLocation` 추후)
- CourtDetail.jsx 의 hero + info 카드 단순화 (예약/일정 hidden)
- 비로그인 → 예약/모집 클릭 시 `/login` 유도
- 로그인 → `/courts/[id]` 풀 기능 페이지로 자동 redirect 옵션 (또는 둘 다 노출)

**검수 매트릭스**:
| 기능 | 옛 | v2.2 | 진입점 | 모바일 |
|------|----|------|--------|--------|
| 코트 기본 정보 | CourtDetail ✅ | ✅ hero + info | SEO/공유 | 1열 |
| 비로그인 가입 유도 | - | ✅ /login CTA | - | OK |
| 로그인 풀페이지 redirect | - | ✅ /courts/[id] | - | - |

**커밋**:
```bash
git commit -m "feat(design-v2): /venues/[slug] Phase 11 박제 (VenueDetail.jsx)

시안 출처: Dev/design/BDR v2.2/screens/VenueDetail.jsx (D등급 P1-3)
공개 SEO 페이지 (비로그인 열람) + 가입 유도 CTA
CourtDetail 단순화 (예약/일정 hidden)
SEO metadata 보존

진입: 외부 검색엔진 / 공유 링크
복귀: 비로그인 → /login / 로그인 → /courts/[id]"
```

### 4-4. P1-4: ProfileWeeklyReport (`/profile/weekly-report`) — 1.5h

**시안**: `Dev/design/BDR v2.2/screens/ProfileWeeklyReport.jsx`
**사이트**: `src/app/(web)/profile/weekly-report/page.tsx`

**박제 룰**:
- 이메일 뉴스레터 톤 (좁은 칼럼 max-width 680, 카드 stack, 명확한 섹션 헤더)
- 주차 navigation (< W47 >)
- "이메일 구독 관리" → `/profile/notification-settings` 또는 `/profile/settings?section=notify`

**검수 매트릭스**:
| 기능 | 옛 | v2.2 | 진입점 | 모바일 |
|------|----|------|--------|--------|
| 주차 navigation | - | ✅ < W47 > | - | OK |
| 이번 주 vs 지난 주 | - | ✅ 비교 카드 | - | 1열 |
| 인사이트 | - | ✅ 섹션 카드 | - | OK |

**커밋**:
```bash
git commit -m "feat(design-v2): /profile/weekly-report Phase 11 박제 (ProfileWeeklyReport.jsx)

시안 출처: Dev/design/BDR v2.2/screens/ProfileWeeklyReport.jsx (D등급 P1-4)
이메일 뉴스레터 톤 + 주차 navigation + 섹션 카드

진입: /profile 주간 카드 / 알림 '주간 리포트 도착'
복귀: AppNav 뒤로 → /profile"
```

### 4-5. Step 4 통합 검증

```bash
npm run typecheck
npm run check:design
# 4 페이지 모바일 시각 확인
```

### 4-6. Step 4 보고

```
✅ Step 4 — P1 박제 4건 완료. 4 커밋. P2/P3 (Step 5) 진행 OK?
```

---

## 5. Step 5 — 🟡 P2/P3 박제 3건 (3-4시간)

### 5-1. P2-1: ProfileCompletePreferences (`/profile/complete/preferences`) — 1.5h

**시안**: `Dev/design/BDR v2.2/screens/ProfileCompletePreferences.jsx`
**사이트**: `src/app/(web)/profile/complete/preferences/page.tsx`

**박제 룰**:
- ProfileComplete (P0-4) 완료 후 follow-up
- 4 step (스킬/스타일/요일·시간/목표)
- 진행바 + step + next/back (좌측 nav 없음)

**커밋**:
```bash
git commit -m "feat(design-v2): /profile/complete/preferences Phase 11 박제 (P2-1)

시안 출처: Dev/design/BDR v2.2/screens/ProfileCompletePreferences.jsx (D등급 P2)
4 step (스킬/스타일/요일·시간/목표) wizard

진입: ProfileComplete 완료 화면 '취향 설정 →' / /profile '프로필 보강하기'
복귀: 완료/스킵 → /home"
```

### 5-2. P2-2: HelpGlossary (`/help/glossary`) — 30m

**시안**: `Dev/design/BDR v2.2/screens/HelpGlossary.jsx`
**사이트**: `src/app/(web)/help/glossary/page.tsx`

**박제 룰**:
- 시안이 redirect 박제로 명시 — `/help#glossary` (해시 앵커) 또는 `/help?tab=glossary` 로 redirect
- 또는 별도 페이지로 박제 (PM 결정)

**작업**:
```bash
# 옵션 A — redirect (시안 권장)
# src/app/(web)/help/glossary/page.tsx 8줄 redirect 처리
# 옵션 B — 박제 (시안 그대로)
# 시안 JSX 박제
```

**커밋**:
```bash
git commit -m "feat(design-v2): /help/glossary Phase 11 박제 (P2-2)

시안 출처: Dev/design/BDR v2.2/screens/HelpGlossary.jsx (D등급 P2)
[옵션 A 또는 B 명시]

진입: 더보기 '도움말' 또는 /help 글로사리 탭
복귀: AppNav 뒤로 → /help"
```

### 5-3. P3-1: RefereeInfo (`/referee-info`) — 1.5h

**시안**: `Dev/design/BDR v2.2/screens/RefereeInfo.jsx`
**사이트**: `src/app/(web)/referee-info/page.tsx` (**신규 라우트**)

**중요**: 사이트의 `/referee` 는 심판 플랫폼이라 충돌 → **별도 라우트 `/referee-info` 신규**.

**박제 룰**:
- 심판 제도 SEO 안내 (옵션 B)
- 비로그인 열람 가능
- 메타데이터 (title, description) 박제

**더보기 메뉴 등록**:
`src/components/bdr-v2/more-groups.ts` 의 "둘러보기" 그룹에 추가:
```typescript
{ id: "refereeInfo", label: "심판 센터 안내", icon: "🦓", href: "/referee-info" },
```

**커밋 (2개)**:
```bash
# 1. 라우트 신규
git commit -m "feat(design-v2): /referee-info 신규 라우트 박제 (P3-1)

시안 출처: Dev/design/BDR v2.2/screens/RefereeInfo.jsx (D등급 F→A)
사이트 /referee 는 심판 플랫폼 점유 → /referee-info 별도 신설
공개 SEO 페이지 + 비로그인 열람

진입: 더보기 '심판 센터 안내' / SEO
복귀: AppNav 뒤로 → /"

# 2. more-groups 등록
git commit -m "feat(more-groups): refereeInfo 진입점 등록

href: /referee-info (P3-1 신규 라우트)
group: 둘러보기"
```

### 5-4. Step 5 보고

```
✅ Step 5 — P2/P3 박제 3건 완료. 4 커밋. 통합 검증 (Step 6) 진행.
```

---

## 6. Step 6 — 통합 검증 + PR 생성 (30분)

### 6-1. 전체 검증

```bash
cd "C:\0. Programing\mybdr"

# 1. tsc 무에러
npm run typecheck

# 2. 린트
npm run lint

# 3. 디자인 회귀 검사
npm run check:design

# 4. 빌드 통과
npm run build 2>&1 | tail -20

# 5. 커밋 카운트 확인
git log --oneline @{u}..HEAD | wc -l   # 14~16 기대
git log --oneline @{u}..HEAD
```

### 6-2. 회귀 점검 (사용자 결정 §2 위반 자동 검색)

```bash
# 가짜링크 4건 안 들어갔는지
grep -rE "id:\s*['\"](guestApps|gameResult|gameReport)['\"]" src/components/bdr-v2/more-groups.ts
# → 0건 기대

# 글로벌 카피 위반
grep -rE "서울 3x3|서울을 중심으로 한 3x3" src/app/\(web\)/
# → 0건 기대

# lucide-react import
grep -rE "from ['\"]lucide-react['\"]" src/
# → 1건만 기대 (personal-hero.tsx — 별도 P1-1 작업)

# 사용자 결정 §2-3: 팀 카드 레이팅 노출 금지
grep -rE "rating|레이팅" src/app/\(web\)/teams/_components/team-card-v2.tsx
# → 노출 X (UI 코드 없음) 기대
```

### 6-3. 모바일 종합 시각 검증

Chrome DevTools 390x844 으로 다음 라우트 순회:
- ✅ /community/[id]/edit (테스트 글)
- ✅ /games/[id]/edit (호스트)
- ✅ /live/[id] (finished)
- ✅ /verify → /profile/complete (신규 가입)
- ✅ /profile/growth
- ✅ /profile/bookings
- ✅ /venues/[slug] (테스트 코트)
- ✅ /profile/weekly-report
- ✅ /profile/complete/preferences
- ✅ /help/glossary
- ✅ /referee-info

각 라우트 체크:
- 가로 스크롤 없음
- 글자 단위 줄바꿈 없음
- aside 정상 stack
- 진입·복귀 동작 (브라우저 뒤로가기 + 헤더 햄버거 → 더보기)

### 6-4. push + PR 생성

```bash
# subin 브랜치 push
git push origin subin

# GitHub 에서 subin → dev PR 생성
# PR 제목: "feat(design-v2): BDR v2.2 박제 일괄 — D등급 11 + 회귀 1 + G-10 (Phase 11)"
# PR 설명:
#   - Step 1: 폴더 정리 (1 커밋)
#   - Step 2: globals.css G-10 (1 커밋)
#   - Step 3: P0 박제 4건 (5 커밋 — verify-redirect 포함)
#   - Step 4: P1 박제 4건 (4 커밋)
#   - Step 5: P2/P3 박제 3건 (4 커밋)
#   - 총 ~15 커밋
#
#   사용자 결정 §2 위반 0건 검증 완료
#   회귀 점검 통과 (가짜링크 / 카피 / lucide / 레이팅 stat)
#   D등급 11 → 0 / 박제율 27% → 37%+
#
#   Phase 11 종료 후: archive/phase-11/ 이동
```

### 6-5. 작업 로그 갱신

```bash
# .claude/scratchpad.md 작업 로그 1줄 추가
# .claude/knowledge/architecture.md / decisions.md / lessons.md 갱신 (해당 시)
# Dev/design/README.md 의 활성 Phase 11 + 박제율 갱신
```

### 6-6. 최종 보고

```
✅ Step 6 — BDR v2.2 박제 일괄 작업 완료.

통계:
- 총 ~15 커밋 / subin → dev PR 생성
- D등급 11 → 0 (박제 안 된 페이지 전부 박제)
- 박제율 27% (32/117) → 37%+ (43/117)
- 모바일 G-10 + G-9 보호 사이트 적용
- 사용자 결정 §2 위반 0건 (자체 검증 통과)
- 추가 보너스: verify → /profile/complete 자동 redirect

다음 후보 작업 (PM 결정):
1. dev → main 머지 (원영님)
2. P0-5 /live/[id] finished 추가 회귀 점검 (이미 박제됨, 시각 검증만)
3. Phase 12 시작 — 시즌 통계/슛존/스카우팅 DB 모델
```

---

## 7. 토큰 부족 시 분할 전략

이 프롬프트는 한 세션에 13~17h 작업이라 **Max Ultimate** 권장. 토큰 부족 시:

### 옵션 A — Step 1~3 만 (P0, ~6h)
이 프롬프트의 § "0. 사전 준비" + § "1. Step 1" + § "2. Step 2" + § "3. Step 3" 까지만 진행.
이후 PM 에게 "P0 완료. P1 다음 세션 진행?" 1줄 보고.

### 옵션 B — 페이지 단위 1개씩
이 프롬프트의 P0-1 (PostEdit) 만 작성된 § "3-1" 부분만 진행.
이후 다음 세션에 P0-2 부터.

### 옵션 C — 우선순위 변경
PM 이 "B-1 만 먼저" 같이 우선순위 지정하면 해당 단계만 진행.

---

## 8. 위반 시 즉시 중단 + 보고

다음 발견 시 즉시 작업 중단 + PM 보고:

1. **사용자 결정 §2 위반** — 시안에 가짜링크 / 서울 3x3 등이 새로 들어가려 할 때
2. **CLAUDE.md 룰 위반** — main 직접 push / 운영 DB prisma push
3. **DB 미존재** — 박제하려는 페이지의 DB 컬럼/테이블 없음 → phase-9-future-features 큐로
4. **API 응답 변환 누락** — snake_case 응답에 camelCase 접근
5. **tsc 에러 발생** — 5건 이상 누적 시 즉시 중단

이 경우:
```
🚨 작업 중단 — <위반 항목>
근거: <파일:라인>
조치 옵션: A) <옵션> B) <옵션>
PM 결정 대기.
```

---

## 9. 시작 신호

이 프롬프트 받자마자 다음 응답:

```
✅ BDR v2.2 박제 일괄 위임 확인 완료.

이해한 내용:
- 11 페이지 박제 + globals.css G-10 + 폴더 정리 + verify redirect
- 사용자 결정 §2 위반 절대 금지 (가짜링크, 서울 3x3, 레이팅, 이모지 아이콘 등)
- 견적: 13~17h / Step 1~6
- 분할 가능: P0(Step 1~3) → P1(Step 4) → P2/P3(Step 5)

질문 / 가정 (필요 시):
1. (PM 결정 필요한 항목 1-2개)
2. ...

작업 시작: Step 0 사전 점검 → Step 1 폴더 정리 부터.
```

이후 § "0. 사전 준비" 의 체크리스트 명령부터 실행.

---

## 프롬프트 (복사 끝)

---

## 부록 A — 외부 참조

- **시안**: `Dev/design/BDR v2.2/Dev/design/BDR v2.2/` (Step 1 후 `Dev/design/BDR v2.2/`)
- **사용자 결정**: `Dev/design/user-design-decisions-2026-04-30.md`
- **검토 보고서**: `Dev/design/v2.2-review-2026-04-30.md`
- **의뢰서**: `Dev/design/claude-design-upgrade-prompt-2026-04-30.md`
- **CLAUDE.md**: 프로젝트 룰 (브랜치 / 보안 / 코딩 컨벤션)
- **`.claude/knowledge/conventions.md`**: 모바일 최적화 체크리스트 10항목

## 부록 B — 사용 가이드

1. § "프롬프트 (복사 시작)" ~ § "프롬프트 (복사 끝)" 통째로 복사
2. 클로드 CLI 새 세션에 붙여넣기
3. § "9. 시작 신호" 의 응답 받으면 진행 OK
4. Step 별 PM 보고 받으면 다음 Step 승인
5. 토큰 부족 시 § "7. 분할 전략" 채택

## 부록 C — 견적 요약

| Step | 작업 | 견적 |
|------|------|------|
| 0 | 사전 점검 | 5분 |
| 1 | 폴더 정리 | 5분 |
| 2 | globals.css G-10 | 30분 |
| 3 | P0 박제 4건 | 4-6h |
| 4 | P1 박제 4건 | 5-7h |
| 5 | P2/P3 박제 3건 | 3-4h |
| 6 | 통합 검증 + PR | 30분 |
| **총** | | **13-17h / 2-3일** |
