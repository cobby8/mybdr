# v2.4 시안 재박제 Prompt — 새 다운로드 적용 (2026-05-01)

> **사유**: 사용자가 Claude Design 에서 추가 작업한 풍부한 시안이 share 페이지에는 살아있었으나 zip 다운로드에는 누락. 새 zip 으로 갱신 후 재박제 필요.
> **시안 베이스**: `Dev/design/BDR v2.4/` (새 다운로드 + 보존 5개)
> **작업 환경**: Claude CLI / Windows + PowerShell / `subin` 브랜치
> **사전 조건**: 디자인 터미널이 새 다운로드 적용 commit + push 완료 후 시작

---

## 0. 변경 사항 — 무엇이 달라졌는가?

| 영역 | 이전 (v2.4 옛 버전) | 현재 (v2.4 새 다운로드) |
|---|---|---|
| Profile.jsx 줄수 | 115 | **125** |
| Profile.jsx stats | 3개 (PPG/APG/RPG) | **6개** (경기 47 / 승률 63% / PPG 14.2 / APG 5.1 / RPG 3.8 / 레이팅 1,684) |
| Profile.jsx 디테일 | 단순 카드 4개 | 풍부한 카드 + timeline 5건 + badges 4건 |
| 변경된 파일 | — | **89개 .jsx/.html/.css** 모두 사용자 디자인 작업 반영 |
| 가짜링크 4 시안 | _archived/ 만 | screens/ + _archived/ (중복 — Phase A에서 다시 정리) |

→ 박제 결과를 시안과 일치시키려면 **Phase A+B+C 전체 재실행** 필요.

---

## Phase R-0 — 시작 전 체크 (필수)

```powershell
# 1. 디자인 터미널 commit 받기
git checkout subin
git pull origin subin

# 2. v2.4 시안이 풍부한 버전인지 확인
Select-String -Path 'Dev/design/BDR v2.4/screens/Profile.jsx' -Pattern "경기.*47|승률.*63%|레이팅.*1,684"
# → 3 매칭 기대

# 3. components.jsx — Phase 19 적용 여부 (이전 박제 터미널 작업 손실됐을 가능성)
Select-String -Path 'Dev/design/BDR v2.4/components.jsx' -Pattern "Icon\.mail|app-nav__icon-btn"
# → 매칭 0이면 ❌ Phase A 다시 필요

# 4. screens/ 의 가짜링크 4건 — 정리 상태 확인
ls 'Dev/design/BDR v2.4/screens/' | findstr /i "GameReport GameResult GuestApps Referee.jsx"
# → 가짜링크 4 살아있으면 Phase A에서 _archived 로 이동
```

**보고 형식**:
```
재박제 시작 점검:
- 새 다운로드 적용됨 (Profile 125줄, stats 6개) ✓
- components.jsx Phase 19: [적용/미적용]
- 가짜링크 4 시안: [_archived/만 / screens/+_archived 중복]
이대로 Phase A+B+C 재실행해도 될까요?
```

→ 사용자 승인 후 진행.

---

## Phase R-A — v2.4 시안 자체 Phase 19 룰 재적용

> **이전 작업 결과 손실됨** (새 다운로드로 components.jsx 가 룰 위반 상태로 회귀). 다시 적용.

### R-A-1. components.jsx AppNav frozen 카피

```
[프롬프트]
Dev/design/BDR v2.4/uploads/03-appnav-frozen-component.md §1 코드를
Dev/design/BDR v2.4/components.jsx 의 AppNav 함수에 그대로 카피.

체크 포인트 (Phase 19):
- 9 메인 탭 + 'more' 9번째 (드롭다운 trigger ❌)
- main bar 우측 = 검색/쪽지/알림/다크/햄버거 5개
- 검색·쪽지·알림 .app-nav__icon-btn (.btn .btn--sm ❌)
- "더보기 ▼" / "RDM rdm_captain" 절대 추가 금지

검증:
Select-String -Path 'Dev/design/BDR v2.4/components.jsx' -Pattern "더보기 ▼|RDM rdm_captain|btn btn--sm.*title=.(검색|쪽지|알림)"
→ 0 매칭 기대
Select-String -Path 'Dev/design/BDR v2.4/components.jsx' -Pattern "Icon\.mail|app-nav__icon-btn"
→ 1+ 매칭 기대
```

### R-A-2. ThemeSwitch + CSS 클래스 + moreGroups 카피

```
[프롬프트]
03 §2 (ThemeSwitch viewport 분기) + §3 (.app-nav__icon-btn CSS) + §4 (moreGroups IA) 그대로 적용.
```

### R-A-3. 가짜링크 4 시안 정리 (다시)

```
[프롬프트]
screens/ 안 가짜링크 4 (Referee, GameReport, GameResult, GuestApps) 를 _archived/ 로 이동.
이미 _archived/ 에 같은 이름 있으면 덮어쓰기 (새 시안 버전 우선).

또 라우트 매칭 정리:
- MyBDR.html 의 route handler 에서 'gameResult', 'gameReport', 'guestApps', 'referee' 매칭 제거
- 'mypage' (=Profile), 'refereeInfo' 매칭 추가/유지

검증:
Select-String -Path 'Dev/design/BDR v2.4/MyBDR.html' -Pattern "route === ..(gameResult|gameReport|guestApps|referee)."
→ 0 매칭 기대
```

### R-A-4. RefereeInfo.jsx 보존 확인

```
[프롬프트]
screens/RefereeInfo.jsx 가 살아있는지 확인 (이전 박제 터미널이 만든 신규 시안).
없으면 03 §1 의 refereeInfo 항목 재참조 + 신규 작성.
```

---

## Phase R-B — 카피·토큰 룰 재검증

> 새 다운로드의 모든 .jsx 파일이 룰 11/12 통과하는지 일괄 검수.

```powershell
$base = "Dev/design/BDR v2.4"

# 카피 룰 11 — "서울 3x3 농구 커뮤니티" / "다음카페" 위반
Get-ChildItem -Path "$base/screens" -Include *.jsx | Select-String -Pattern "서울 3x3 농구 커뮤니티|다음카페"

# 카피 룰 12 — placeholder 위반
Get-ChildItem -Path "$base/screens" -Include *.jsx | Select-String -Pattern 'placeholder="예[\) :]'

# 토큰 룰 12 — pill
Select-String -Path "$base/tokens.css","$base/responsive.css" -Pattern "border-radius:\s*(9999|999)px"
```

→ 위반 발견 시 Phase B 패치 (카피 일괄 교체, 999px → 4px).

---

## Phase R-C — `/profile` (마이페이지 hub) 재박제

> 시안 풍부 디테일 (stats 6개, timeline 5건, badges 4건) 이 박제 결과에 모두 반영되도록 1:1 박제.

### R-C-0. 사전 보존 (절대 변경 X)

- /profile 라우트 + 9 서브 라우트
- getWebSession + 7쿼리 병렬 fetch
- /api/web/me + /api/web/profile 응답 (snake_case)
- 공식 기록 쿼리 utils

### R-C-1. 기존 박제 결과 확인 + 차이 분석

```
[프롬프트]
@planner — 현재 src/app/(web)/profile/page.tsx + _components/ 의 박제 결과를 점검.

비교:
- 시안: Dev/design/BDR v2.4/screens/Profile.jsx (125줄, 풍부)
- 운영: src/app/(web)/profile/page.tsx + 15개 _components

차이 매트릭스:
1. 운영 _components/ 안 옛 박제 잔재 (ability-section, activity-ring, court-stamps,
   radar-chart, xp-level-card, streak-card, badge-collection 등)
2. 시안 4-card grid (프로필 / 내 농구 / 내 성장 / 내 활동) 누락
3. 시안 추가 4-card (예약 이력 / 주간 리포트 / 알림 / 배지·업적) 누락
4. 시안 우측 sticky (다음 경기 D-N / 소속 팀 / 최근 활동) 누락
5. stats 6개 (경기/승률/PPG/APG/RPG/레이팅) 빈 값 처리

작업 리스트 (각 ≤30분):
- [ ] _components/ 의 옛 잔재를 _archived/ 로 격리
- [ ] 시안 좌측 320 (profile-id-card / soson-team-card / activity-badge-card) 신규
- [ ] 시안 우측 메인 (시즌 스탯 / 다가오는 일정 / 최근 활동) 신규
- [ ] 추가 4-card grid (예약 이력 / 주간 리포트 / 알림 / 배지·업적) 신규
- [ ] 우측 sticky 3카드 (다음 경기 / 소속 팀 / 최근 활동)
- [ ] 데이터 fetch 매핑 (시안 SAMPLE → API 응답)
- [ ] 빈 상태 UI (BDR_Admin master 같은 게임 0 계정에서도 시안 톤 일관)
```

### R-C-2. 구현 (developer)

```
[프롬프트]
@developer — Phase R-C-1 plan 따라 /profile 1:1 재박제.

작업 룰:
- Dev/design/BDR v2.4/screens/Profile.jsx 의 JSX 구조를 src/app/(web)/profile/page.tsx 본문에 적용
- 시안의 const stats / timeline / badges placeholder → 운영 API 응답으로 교체
  - stats: /api/web/profile 의 필드 (경기/승률/PPG/APG/RPG/레이팅)
  - timeline: /api/web/profile/activity (최근 5건)
  - badges: /api/web/profile/achievements (최근 4건)
- 운영 _components/ 의 옛 컴포넌트 → _archived/ 로 격리 (ability-section, activity-ring,
  court-stamps, radar-chart, xp-level-card, streak-card, badge-collection 등)

⚠️ snake_case 응답 키 — 신규 필드 추가 전 curl 1회로 raw 응답 확인 (errors.md 2026-04-17):
  curl -s http://localhost:3001/api/web/me | jq .
  curl -s http://localhost:3001/api/web/profile | jq .

수정 후 npm run lint && npm run typecheck 통과 확인.
```

### R-C-3. Test (tester)

```
[프롬프트]
@tester — /profile 재박제 검증 — 시안과 1:1 일치 확인.

체크 항목:
1. npm run dev (port 3001) 후 /profile 진입 — 시안 125줄 모든 섹션 표시 확인
2. AppNav 6 케이스 자동 검수 통과 (위 R-A 검증)
3. 좌 320 + 우 1fr 레이아웃 (시안 일치)
4. 4-card grid 메인 + 4-card 추가 + 우측 sticky 3카드 = 11개 카드 모두 표시
5. 데이터 있는 계정 (rdm_captain 같은 시안 샘플) — 풍부한 데이터 표시
6. 빈 계정 (BDR_Admin master) — 빈 상태 UI 시안 톤 일관 ("─" 단순 표시 ❌)
7. /profile/bookings, /growth, /weekly-report 깊은 페이지 진입 동작
8. Vercel preview (mybdr-git-dev-mybdr.vercel.app/profile) 머지 후 검증

실패 → debugger 호출 + errors.md 갱신.

검증 명령:
Select-String -Path 'src/components/bdr-v2/app-nav.tsx' -Pattern "Icon\.mail|app-nav__icon-btn"
→ 1+ 매칭 기대 (운영 헤더도 Phase 19)

Select-String -Path 'src/components/bdr-v2/app-nav.tsx' -Pattern '더보기 ▼|moreOpen|setMoreOpen|kind: "trigger"'
→ 0 매칭 기대 (룰 3 위반 0)
```

### R-C-4. 운영 헤더 (`src/components/bdr-v2/app-nav.tsx`) 재정리

```
[프롬프트]
이전 박제 터미널이 Phase 19 쪽지 아이콘만 추가하고 dropdown trigger 패턴은 그대로 두었음 (룰 3 위반).
다시 03 §1 frozen 코드 그대로 적용:

작업:
- Line 61: { id: "more", kind: "trigger" } → 9번째 메인 탭 (drawer 열기)
- Line 70~175: moreOpen state + setMoreOpen + dropdown JSX 모두 제거
- 9번째 "더보기" 클릭 시 drawer (모바일) 또는 패널 (PC) 열기
- PC 더보기 클릭도 drawer 열림 (PC dropdown trigger ❌)

검증:
Select-String -Path 'src/components/bdr-v2/app-nav.tsx' -Pattern '더보기 ▼|moreOpen|setMoreOpen|kind: "trigger"'
→ 0 매칭 기대
```

---

## Phase R-D — P1 박제 (필요 시)

마이페이지 hub (R-C) 끝나면 P1 4건 (`/profile/{edit,settings,billing,activity}`) 도 새 시안 기준으로 다시 점검.

이전 박제 결과가 v2.4 옛 시안 기반이라 시안 디테일 누락 가능성. 각 페이지 시안 (`Dev/design/BDR v2.4/screens/{EditProfile,Settings,Billing,MyActivity}.jsx`) 과 1:1 비교 후 갱신 필요한 부분만 추가 박제.

---

## Phase R-E — 최종 검수 + commit

### 자동 회귀 검수

```powershell
$design = "Dev/design/BDR v2.4"
$prod = "src/components/bdr-v2/app-nav.tsx"

# 시안 6 케이스
Write-Host "=== 시안 회귀 검수 ==="
Select-String -Path "$design/components.jsx" -Pattern "더보기 ▼|RDM rdm_captain"        # 0 기대
Select-String -Path "$design/components.jsx" -Pattern 'btn btn--sm" title="(검색|쪽지|알림)"' # 0 기대
Select-String -Path "$design/components.jsx" -Pattern "Icon\.mail"                       # 1+ 기대

# 운영 헤더
Write-Host "=== 운영 헤더 검수 ==="
Select-String -Path "$prod" -Pattern '더보기 ▼|moreOpen|setMoreOpen|kind: "trigger"'  # 0 기대
Select-String -Path "$prod" -Pattern "Icon\.mail|/messages"                            # 1+ 기대

# 카피 / 토큰
Write-Host "=== 카피 / 토큰 검수 ==="
Get-ChildItem -Path "$design" -Recurse -Include *.jsx,*.html | Select-String -Pattern "서울 3x3 농구 커뮤니티|다음카페"   # 0 기대
Select-String -Path "$design/tokens.css","$design/responsive.css" -Pattern "border-radius:\s*(9999|999)px"  # 0 기대
```

### commit

```bash
git add Dev/design/BDR\ v2.4 src/app/\(web\)/profile src/components/bdr-v2/app-nav.tsx

git commit -m "feat(profile): re-bake /profile with v2.4 fresh design + Phase 19

- v2.4 시안 새 다운로드 적용 (Profile 125줄, stats 6개, badges 4개, timeline 5개)
- /profile 1:1 재박제 — 좌 320 + 우 1fr + 4-card 메인 + 4-card 추가 + sticky 3
- AppNav 운영 코드 03 frozen 재적용 (룰 3 dropdown trigger 제거)
- 옛 _components/ (ability-section, court-stamps, radar-chart 등) → _archived
- 6 케이스 자동 회귀 검수 통과"

git push origin subin
```

→ subin → dev PR.

---

## 부록 — 새 다운로드에서 변경된 핵심 파일 우선순위

새 다운로드의 89개 파일 중 박제 적용 우선순위:

| 우선순위 | 파일 | 적용 영역 |
|---|---|---|
| P0 | screens/Profile.jsx | /profile (마이페이지 hub) |
| P0 | components.jsx | AppNav (시안 + 03 frozen 동기화) |
| P1 | screens/EditProfile.jsx | /profile/edit |
| P1 | screens/Settings.jsx | /profile/settings |
| P1 | screens/Billing.jsx | /profile/billing |
| P1 | screens/MyActivity.jsx | /profile/activity |
| P2 | screens/Calendar.jsx (1 day ago) | /calendar |
| P2 | screens/PostWrite.jsx (1 day ago) | /community/new |
| P2 | screens/GameResult.jsx (3 days ago — 가짜링크지만 시안 보존) | _archived 보관 |
| P3 | 그 외 80여개 | 후속 큐 — 페이지별 박제 시 시안과 1:1 비교 |

---

## 출처

- 새 다운로드: `C:\Users\user\Downloads\BDR v2.3` ↔ `C:\Users\user\Downloads\BDR v2.4` (100% 동일)
- 워크스페이스 갱신: `Dev/design/BDR v2.4/` (새 다운로드 + 보존 5개)
- 03 frozen + Phase 19: `Dev/design/BDR v2.4/uploads/03-appnav-frozen-component.md`
- 이전 박제 터미널 commit: `0368269 / 11debfc / a1e5837 / 9927e2b / 2487426`
