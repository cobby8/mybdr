# MyBDR 주간 진행률 점검 — 2026-06-21

> 자동 생성(주간 점검 스케줄). 읽기 전용 조사만 수행했고 코드·DB·git은 일절 건드리지 않았습니다.

## 0. 한 문장 요약
이번 주 실제 작업은 4월 UX 플랜도 카페 sync도 아닌 **Track B 관리자 Toss 리스킨(Phase 0~4)과 심판 플랫폼 Toss 리스킨, 매칭 M1~M6**였고, 두 플랜은 사실상 휴면 상태이므로 **지금 시점에 맞게 재정리할지 결정**하는 게 가장 중요합니다.

## 1. UX 워크스트림
한 가지 먼저 짚자면, 이 플랜은 4/19~5/16(4주)짜리였고 오늘은 그로부터 약 5주가 지난 시점입니다. 그래서 "이번 주"라는 틀로 보면 해당 워크스트림은 이미 종료 구간을 한참 넘겼습니다.

- 이번 주 완료: 없음(Q1~Q12 / M1~M7 / L1~L3 ID와 매칭되는 이번 주 커밋 0건).
- 확인된 과거 완료: Q1(대회 상세 고아 라우트 redirect)은 코드에 반영돼 있음(`tournaments/[id]/bracket` 등 redirect 확인). 나머지 ID는 개별 커밋 추적이 어려움(추정 — 당시 작업이 더 큰 리디자인 흐름에 흡수된 것으로 보임).
- 다음 주 예정: 플랜 일정상으로는 L2/L3(장기) 구간이나, 현재 팀이 Toss 리스킨으로 방향을 튼 상태라 플랜 그대로 재개하는 건 비현실적.
- 지연/차단: 전용 트래커 `scratchpad-ui.md`의 마지막 기록이 **2026-04-12**로, UX 플랜(4/19 작성)보다 앞섭니다. 즉 이 파일은 UX 플랜을 추적하지 않고 옛 다크모드 버튼 작업에서 멈춰 있습니다. UX 플랜은 별도 추적 없이 큰 리디자인에 병합·휴면된 것으로 추정.

## 2. 카페 sync 워크스트림
- 이번 주 완료: 없음(마지막 활동 **2026-04-24**, 약 2개월 휴면).
- 과거 완료 상태(파일 실측):
  - Phase 1 ✅ — board-map.ts / fetcher.ts / sync-cafe.ts 모두 존재.
  - Phase 2 ✅ — article-fetcher.ts / upsert.ts / extract-fallbacks.ts 존재, cafe_posts 15건(IVHA 5 / Dilr 5 / MptT 5) 수집 완료 기록.
  - Phase 3 ~부분 완료 — `.github/workflows/cafe-sync.yml`(자동 수집) + `cafe-sync-verify.yml`(검증봇) 존재. 단, **task가 가정한 파일명과 다름**: `api/cron/cafe-sync/route.ts` 없음, `cafe-sync-deep.yml` 없음, vercel.json에 카페 cron 없음, `admin/cafe-sync` UI 페이지 없음(워크플로우는 GH Actions cron 기반으로 설계됨 — Vercel cron 미사용).
- 다음 주 예정: Phase 3 마무리(검증봇 R5 블록커 수정 + 쿠키 자동 갱신 해결)인데, 아래 차단 때문에 보류 중.
- 지연/차단: **다음카페 로그인 쿠키 자동 갱신이 핵심 블로커**(04-24 기록). IVHA 본문 fetch 403 지속, Cowork/Playwright 방법 모두 TIARA/DID 미수집으로 실패. "열린 결정 Z/X/Y"가 수빈 답을 기다리는 상태. 또한 검증봇에 R5 블록커(Linux CI 인자 이중 쿼터링)가 남아 있어 --execute 본격 전환 전 수정 필요.
- 운영 지표: cafe_posts/games 실제 행 수는 운영 DB 조회 금지 가드에 따라 확인하지 않았습니다. 자동 수집이 본격 가동되지 않은 상태라 **운영 시작 후 측정 가능**.

## 3. 미푸시 또는 미완료 작업
- `git status`상 약 2,956개 파일이 "modified"로 뜨지만, 이건 실제 작업이 아니라 **줄바꿈(CRLF/LF) 정규화 노이즈로 추정**됩니다. 근거: `core.fileMode=false`인데 CLAUDE.md가 270+/283− 식으로 churn → 공백 무시 diff(`--ignore-all-space`)로는 1+/14−만 남음. 즉 마운트 환경의 줄바꿈 차이일 뿐, 미커밋 실작업으로 보기 어렵습니다(확인 권장).
- 실질 미완료: 카페 검증봇 커밋(verify-cafe-sync.ts / cafe-sync-verify.yml)이 04-21 기준 "미커밋/조건부"로 기록됨 — 이후 처리 여부 확인 필요.
- 현재 브랜치가 `subin`인데, CLAUDE.md는 **오늘(2026-06-21) subin 폐지·dev 직접 작업** 전환을 명시. 이번 주 커밋들은 아직 subin 경유(#732·#734 등). 전환 첫날 과도기로 보이며, 다음 세션부터 dev 직접 작업 적용 권장.

## 4. PR 현황
- `gh` CLI가 환경에 없어 PR 목록 직접 조회는 불가(추정 표시).
- git 로그 기준 이번 주 머지: 매칭 M1~M6(#717~#722), 관리자 Toss 리스킨(#725·#726·#728·#729·#730·#734), 심판 Toss 리스킨(#731~#733). 현재 PR 번호가 **#734대**까지 와 있어, UX 플랜이 가정한 PR #40~#51은 이미 한참 지난 과거 번호대입니다.
- dev→main 머지(수빈 단독 결재)가 매칭 M1~M6 건으로 **대기 중**(index.md 기록).

## 5. knowledge 갱신 점검
양호합니다. 지난 7일간 knowledge/가 활발히 갱신됨(decisions 06-21, architecture 06-21, index 06-20·06-21, errors 06-19 등). 권장 사항 없음 — 오히려 기록 위생은 잘 유지되고 있습니다.

## 6. 다음 주 우선순위 추천 1~3개
1. **두 플랜의 공식 정리/재베이스라인** — 4월 UX 플랜과 카페 sync 플랜 모두 일정이 끝났거나 휴면. 지금 무엇을 "닫고" 무엇을 "현재 Toss 리스킨 흐름에 다시 태울지" 한 번 정리해야 주간 점검이 실제와 맞물립니다. (사유: 점검 대상과 실제 작업이 어긋나 있음)
2. **매칭 M1~M6 dev→main 결재** — 운영 반영의 마지막 관문이 수빈 단독 권한에 막혀 있음. (사유: 완료된 작업의 가치 실현이 결재만 남음)
3. **카페 sync 가는 길 결정** — 쿠키 자동 갱신 "열린 결정 Z/X/Y"를 매듭짓거나, 안 되면 카페 sync를 보류로 명시. (사유: 2개월 휴면의 단일 원인이 이 결정 하나)

## 7. 수빈에게 던지는 질문
- 4월 UX 개선 플랜(Q/M/L 22개)을 **공식 종료(아카이브)할지, 현재 Toss 리스킨에 흡수해 재정의할지** 결정 부탁드립니다.
- 카페 sync의 쿠키 자동 갱신 "열린 결정 Z/X/Y" 중 어디로 갈지(또는 당분간 보류) 알려주시면 다음 점검부터 반영하겠습니다.
- 이 주간 점검 스케줄의 대상 워크스트림을 현재 진행 중인 **Track B 관리자 리스킨 / 매칭 / 심판**으로 바꿔 잡을까요?
